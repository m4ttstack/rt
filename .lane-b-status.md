2026-08-07 14:11 T6 complete — rt sandbox verbs (create/ls/status/suspend/resume/destroy/answer/logs/flags), controller client to the design contract (fakes only), pool allocator + kubectl forwards + dev-ports mirroring (byte-compat), daemon 5s reconcile → notify(); 849 lib tests green, tsc 0, committed to main (2 commits, not pushed). Contract notes for T4 in lib/sandbox.ts header: handshake ref refs/sandboxes/incoming/<branch>, pod name == sandbox id, bare-array GET shapes, captured payload {file,content}.
2026-08-07 14:20 T7 complete — qa-tunnel (sdm listener preflight, ssh -R via receiver, POSTGRES_URL env-Secret + recycle rule, death → notify), ship relay (receiver fetch → summary → explicit confirm gate → local-identity push; declined path provably pushes nothing), sync-config extended for sandbox-bake-config + repo-browser-flows, secrets upserts for repo-browser-secrets + agent-credentials (overlay-named, stdin-only). 871 lib tests green, tsc 0, grep clean of acme/cswap; 3 more commits on main, not pushed.
