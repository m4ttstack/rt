{
  "name": "console",
  "displayName": "Console",
  "description": "The mattstack pipeline console: what's running, what needs you, and what a run did.",
  "icon": "./public/favicon.svg",
  "port": 11001,
  "commands": {
    "start": "bun run src/server/index.ts",
    "build": "bun run build",
    "deploy": "bun run build && deck restart console"
  }
}
