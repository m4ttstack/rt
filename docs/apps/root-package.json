{
  "name": "local",
  "module": "src/main.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "lucide": "1.27.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build:icons": "bun build core/icons.js --outfile core/vendor/lucide.min.js --minify --target=browser --format=iife",
    "build": "bun build --compile src/main.ts --outfile dist/local",
    "serve": "bun run src/main.ts serve",
    "test": "bun test"
  }
}
