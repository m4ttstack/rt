# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-bin
*.local

# `bun run generate:embedded` output (see scripts/generate-embedded-assets.ts)
# -- rebuilt from `dist/` on every `build:binary` run, never hand-edited.
src/server/embedded/generated

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static

.superpowers

# Consumer friction logs: per-app, untracked by convention (see the
# adopting-mantine-kit skill). Ignored so a `git add -A` cannot sweep one in.
FEEDBACK-*.md
