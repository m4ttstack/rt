# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static

.superpowers

# Consumer friction logs: per-app, untracked by convention (see the
# adopting-mantine-kit skill). Ignored so a `git add -A` cannot sweep one in.
FEEDBACK-*.md

# app-kit tarballs, packed locally from ~/Documents/GitHub/app-kit (matches
# app-kit's probe app, which also gitignores its vendor/ tarballs).
vendor
