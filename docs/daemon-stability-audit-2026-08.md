# rt daemon stability audit and hardening roadmap

**Date:** 2026-08-27 · **Scope:** the daemon process (`lib/daemon.ts`, `lib/daemon/**`, `lib/state/**`, transitive imports, launchd/tray supervision) · **Trigger:** the daemon has grown from a personal convenience process into the backbone of an app about to be shared with other people.

## Method

Eleven parallel audit lenses (lifecycle, transport, state, background work, worktree engine, chat/agent/events, integrations, architecture rot, observability, concurrency sweep, share-readiness) each read their subsystem in full. 210 raw findings deduplicated to 169: 108 bug/hazard plus 61 structural, of which three were same-document duplicates folded into one entry in Appendix B, leaving the 58 listed there. Every bug/hazard finding (108) was independently re-verified by an adversarial "try to refute this" agent that re-read the code (Opus 4.8 for P0/P1, Sonnet for P2/P3); several verdicts rest on empirical probes run in isolation (Bun busy-code behavior, runCapture grandchild survival, unhandledRejection delivery on the prod entry path). Structural rot/gap findings (58 after dedup) were adjudicated by the lead reviewer from finder evidence. Verdicts: **104 confirmed, 4 refuted**. Refuted claims are listed in Appendix C so they are not re-raised.

Severity legend: **P0** active crash/data-loss with user impact · **P1** reliability failure or silent feature drop under realistic use · **P2** latent, bites under specific conditions · **P3** quality/operability.

## Field evidence (from ~/.mattstack/rt/logs, 2026-07-30 through 2026-08-28)

The audit's central claims are not hypothetical; the daemon's own logs show the classes in the wild:

- **A real crash loop, invisible end to end.** 2026-08-25 14:01-14:04Z: 24 consecutive fatal `Failed to start server. Is port 9401 in use?` boots, ~10s apart under launchd KeepAlive. Each iteration unlinked and re-bound `rt.sock` before dying, stealing the socket from the 9401 holder. No surface (tray, status, notification) reported it (S043, R001, R002).
- **Event-loop stalls, mostly but not fully fixed.** 900+ command handlings over 15s in late July and on 08-06 (the async-scanner fix landed mid-window); 8 residual since 08-13. `system-processes` p99 is 6.1s over 293k calls, max 76s. The remaining sync-exec sites in Phase 1 are the same class (S008, S015, S045).
- **The boot path runs many times a day by design.** 5-14 daemon boots/day in the last week; 196 of 217 exits were clean signal exits from deliberate dev-cycle restarts, which is normal workflow here and not itself a problem. The relevance is exposure: a boot path exercised this often must fail loudly when it does fail.
- **Contract noise.** `command rejected` is dominated by `events:head` (287), `project-mrs:read` (183), `runs:list` (131): clients drifting against untyped payloads (R013).
- Zero `unhandledRejection` lines in 30 days of logs, which corroborates that the swallow path (S001) hides failures rather than logging them.

## Executive assessment

The daemon's core seams are genuinely good: one command seam that logs every outcome, factory-built handlers over a shared context, a write-through SQLite store with a stated contention policy, a park gate that keeps a wrong-flavor daemon inert, and a worktree engine whose destructive steps are guarded by registry-first design, per-tree locks, and rename-to-trash. The unit-test base (43 daemon test files) is real. This is a better-than-typical codebase for its age.

The instability lives in four places, and they are consistent:

1. **The supervision contract is fiction on the shipped path.** The crash policy was designed for `bun run lib/daemon.ts` (a throw exits 1, launchd restarts). The prod binary boots through `cli.ts`, where a `startDaemon()` throw is delivered to the `unhandledRejection` listener and the process lives on as a zombie that every surface reports as healthy (S001, P0). Failures earlier than that crash-loop with no log line anywhere, because stderr redirection and crash handlers install mid-boot (S003, S004). There is no restart counter, no last-crash reason, no stall detection, no degraded state (R001-R003, R011). A daemon this central must fail loudly and visibly; today it fails silently in both directions.
2. **The event loop still gets blocked.** The "never execSync on the daemon thread" rule exists because it already caused a wedge (MAT-222) and a tray-flicker incident, but it is enforced by memory, not by a gate. Five sync git spawns per repo still run inside every 5-minute refresh, disposal runs a system-wide sync `lsof`, and `runCapture`'s timeout is defeated by any grandchild that holds the pipe, which can retire the worktree reconciler for the daemon's lifetime (S008, S015, S023/S024). One bug (S011) quietly gives the daemon a 5000ms `busy_timeout`, so the SQLITE_BUSY policy the whole state layer was written against has never actually engaged in production.
3. **The trust boundary on 127.0.0.1 assumes the author's machine.** The WebSocket broadcast has no origin or token check, so any web page in any browser can watch MR bodies, repo paths, and process argv scroll by (S005/S006); several mutating routes skip the token (S040, S041). Fine for one developer who wrote it; not fine for someone who installs mattstack.app and browses the web.
4. **Rot is concentrated, not diffuse.** Fourteen side effects arm at module import; every new subsystem edits daemon.ts in three places because there is no lifecycle seam; 48 of 91 commands are untyped `any` contracts; ordering constraints live in comments; and the previous audit doc (docs/daemon-runner-health.md) describes a subsystem deleted months ago. None of this crashes anything today. All of it raises the probability that the next ten changes do.

The roadmap below is ordered so that visibility lands first (you cannot stabilize what you cannot see), the stall class dies second and stays dead via a gate, the boundary tightens third, and only then does judgment-heavy surgery on the destructive engine and the module structure proceed, with the new observability as the safety net. Portability work (someone else's Mac) closes it out, though a few of its items gate sharing regardless of order.

## Roadmap

Phases are ordered by leverage, smallest and most urgent first. Each item lists the findings it retires, a size, and the superpowers execution mode: **SDD** means the item is specified well enough by this document plus the finding's fixer notes to go straight to `superpowers:subagent-driven-development` (test-first); **B/S/P** means it needs `superpowers:brainstorming` then a spec and plan first, because there are real design decisions. One phase = one lane/branch; items within a phase are commit-sized tasks.

Already-tracked overlaps: RT-56 (home-snapshot push silently suppressed), RT-51 (recoverable disposal), RT-52 (.worktrees location), RT-73 (shim PATH), RT-64 (dev-mode verify false criticals; folds into 0.5's status-verdict work), RT-74 (run reopen; a runs feature defect outside this audit's scope, deliberately deferred). Where a phase touches one, fold the ticket into the lane rather than duplicating it.

### Phase 0 · Honest supervision: crashes become visible and recoverable

The highest-leverage work in the audit. Everything else assumes you can tell when the daemon is broken. Size: one SDD lane, ~10 commit-sized tasks. Mode: **SDD**; make task one a half-page sketch of the status-verdict set and exit-code semantics, since 0.5 and 0.6 carry the lane's design content.

- **0.1 Fatal means exit.** Make every boot failure on the prod path exit non-zero: wrap the boot sequence so a `startDaemon()` throw is fatal (log + exit 1) instead of feeding the `unhandledRejection` swallow; document the policy split (boot throws are fatal, steady-state rejections log and continue). Retires S001 (P0), S037. Trivial diff, highest payoff.
- **0.2 Crash handlers first.** Move `redirectNativeStderr()` + `installCrashHandlers()` above every module-scope side effect in `lib/daemon.ts` (before the legacy-dir migration and the park loop), so pre-boot failures land in the crash log instead of a discarded stderr. Retires S003, S004, R007.
- **0.3 Bounded boot-failure behavior per class.** EADDRINUSE on 9401: do not bind `rt.sock` before the API bind succeeds (the 08-25 loop stole the socket 24 times); log the holder (lsof) and park-retry with backoff instead of crash-looping; honor an `rt.apiPort` setting as the escape hatch (RT-45 precedent: RT_API_PORT exists). state.db open/migrate failure: bounded backoff plus a quarantine marker surfaced by `rt daemon status`. Retires S043, S037, S074 (importer wrapped per-store).
- **0.4 events.db joins the discipline.** Open it inside `startDaemon()` after crash handlers, with busy_timeout, synchronous pragma, and the same corruption quarantine state.db has; guard the sweep timers. Retires S009, S035.
- **0.5 Restart and crash visibility.** Persist a restart counter and last-exit reason (kv table); `rt daemon status` and `/api/status` gain verdicts for `boot-failed`, `crash-looping`, `alive-but-not-serving`, `parked`; the tray dot maps them (fixes the eternal yellow, S026). Rotate/prune `daemon-stderr.log` and `tray-crash.log` and stamp entries so stale content is never shown as current (S029). Retires R001, R002, S026, S029.
- **0.6 start/stop honesty.** `rt daemon start` kickstarts a registered-but-stopped agent instead of register-only; `shutdown`'s exit-0-stays-down semantics get documented and paired with a working `start`; uninstall refuses to unlink `rt.sock`/`rt.pid` while a live pid holds them; eviction and cleanup check file ownership (pid stamped in the file) before unlinking. Retires S028, S036, S060, S027, S030, S012, S044.
- **0.7 Two one-line correctness fixes that gate everything above.** `startHomeSnapshot` opens state.db daemon-flavored (S011); `isBusyError` matches the extended busy codes and read-then-write daemon transactions use `BEGIN IMMEDIATE` (S072, S073). These make the contention policy real for the first time.
- **0.8 Retire the stale audit doc.** Replace `docs/daemon-runner-health.md` with a pointer to this document (R017, R058 comment sweep rides along).

### Phase 1 · The event loop is sacred, and a gate keeps it that way

Kills the wedge/stall class that produced MAT-222, the tray flicker, and the 76s handler tail. Size: one SDD lane. Mode: **SDD**.

- **1.1 Excise sync exec from daemon-reachable code.** Replace `execSync`/`sleepSync` with `runCapture`/`git-async` in: cache-refresh branch listing and remote-url (S008, S045), `git-worktrees.ts` callers on the daemon tick and `repos` verb (S021), freshness `getRemoteUrl` (R032), `/api/repos` worktree listing (S055), worktree-process-kill's lsof/ps trio (S015, S016), repo-index identity resolution reached by `endpoint:claim` (S098), boot-reconcile's `sleepSync` (S044 rides 0.6).
- **1.2 runCapture that actually times out.** Spawn children in their own process group, escalate SIGTERM to SIGKILL on deadline, and bound the post-kill stream drain, so a pipe-holding grandchild cannot hold the promise open. This one fix also un-wedges the reconciler's stuck-ready-step failure mode. Retires S023, S024; revisit S104's shared 60s mutating-verb timeout while in the file.
- **1.3 The gate.** A test in the spirit of `no-eager-tui.test.ts`: walk the daemon's import graph and fail if any daemon-reachable module imports `execSync`/`spawnSync`/`Bun.spawnSync`/`sleepSync` (allowlist for boot-only `user-path.ts` until Phase 6 replaces it). The rule has been re-broken twice; only a gate keeps it dead.
- **1.4 Refresh cannot wedge.** Per-repo deadline plus a whole-cycle deadline on the coalesced refresh promise so one hung GitLab call cannot pin `refreshInFlight` forever (S007); provider cache drops entries when `gitlabToken` changes (S048, S049).
- **1.5 Scan cost sanity.** Skip the 10s system-process scan and 30s port scan when no consumer has asked recently (tray connected, recent CLI call); single failed lsof no longer resets runaway windows (S058, S093, S061, S038/S039/S101 fold in: batch or cache run-DB opens).

### Phase 2 · Health you can see

Small spec first (one page: the health model), then SDD. Retires the observability gap cluster.

- **2.1 A degraded state.** `tray:status`/`/api/status` report degraded when refreshes fail consecutively, with the failing subsystem named (R011); memory/handle/uptime metrics included (R012).
- **2.2 Stall detection.** A self-heartbeat (timer writes a monotonic stamp; status compares) so a blocked event loop is distinguishable from "not running" on every surface (R003).
- **2.3 Log ergonomics.** `rt.logLevel` setting read at boot and on change (R004); cap daily log growth with warn-storm suppression (S031); pino stream gets an error listener so a full disk cannot turn every log call into a throw (S032); resolver console.warn noise demoted (S033, R005).
- **2.4 Request attribution.** A request id plus caller tag (cli/tray/board pid) on every command, logged at the seam, echoed in `ok:false` envelopes (R008); unknown-command replies distinguish version skew via the ping version (R021).

### Phase 3 · The 127.0.0.1 trust boundary

The daemon must assume a hostile browser on the same machine. Size: one SDD lane. Mode: **SDD** (policy decisions are small and named here).

- **3.1 WS auth.** Token (query param or first-message auth) plus Origin allowlist on `/ws`; broadcast send failures counted and dead clients dropped (S005, S006, S042).
- **3.2 Token-gate the strays.** `POST /api/hooks/:repo/repair`, `POST /api/refresh`, and the destructive `GET /api/notifications` drain join the token gate; hooks:repair keeps working from the unix socket untokened (S040, S041, S084).
- **3.3 One token read.** Single loader with mtime re-read shared by api-server and secrets handler (S054).
- **3.4 Input hygiene.** `maxRequestBodySize` on both servers (S092); reject `--`-prefixed branch/ref args before they reach git argv (S010); coerce REST GET query params per catalog types so documented flags work (S085; the path-param decode 500s are S083, owned by the cross-cutting list); redact credential-bearing remote URLs in logs and errors (S050).

### Phase 4 · The destructive engine earns its paranoia

The worktree engine's guards protect against rt's own recorded state but not against the process table, the filesystem, or a dropped db write. These are judgment calls, not mechanical fixes. Mode: **B/S/P** (brainstorm the policy, spec, then SDD). Fold in RT-51 and RT-52; RT-34's pool history is context.

- **4.1 Process-kill correctness.** cwd-prefix matching with the pool root inside the main clone means `autoReturnMain` can kill every sibling tree's dev server, and the blocklist approach SIGTERMs editors, tmux, and the calling CLI. Decide: match on exact tree root, allowlist what rt itself started (endpoint claims know), never kill on the main clone path. Retires S017, S018, and the S015/S016 sync-lsof interplay (mechanics moved by 1.1).
- **4.2 Stash discipline in freshen.** Never pop a stash the current pass did not verifiably push (stamp the stash message, verify before pop); reconsider stash-in-live-checkout entirely for idle-main freshen (S019, S064).
- **4.3 Registry writes are critical.** Worktree registry and claim-state writes use the critical-write path (retry, then refuse the destructive action) instead of warn-and-drop persistOrWarn (S025); dispose re-reads the record under the lock (R040); reconcile treats a missing path as "hold and re-verify next pass" rather than prune (S063).
- **4.4 Trash and reap guards.** `.trash` never materializes inside the user's repo unless the exclude is in place (S078); the reaper only touches directories whose name matches rt's own epoch stamp format and provenance (S079); disposal recoverability lands here (RT-51).
- **4.5 Claims and adoption.** Endpoint claim liveness records boot-id alongside pid so reboot pid-reuse cannot pin ports (S068); `worktree adopt` requires explicit opt-in to auto-dispose semantics (S056); team-declared on-deck pools default off on machines that never opted in (S077).

### Phase 5 · Structure that stops the rot

The architectural phase. Mode: **B/S/P**, executed as two or three lanes; high blast radius, so it goes after observability exists to catch regressions.

- **5.1 A lifecycle seam.** Replace the fourteen module-scope side effects with an ordered boot list of units, each `{name, start(), stop()}`, constructed inside `startDaemon()`; cleanup derives from the same list; the three copy-pasted sweep timers become one helper with per-sweep config; `emit()`'s per-event business logic moves behind a subscriber registration. Retires R006, R009, R019, R020; unlocks in-process lifecycle tests (R031) and closing state.db at shutdown (R022).
- **5.2 Contracts all the way.** The remaining 48 untyped commands (everything the Swift tray calls, plus `cache:read`) join the rt-client catalog with payload validation at the seam (reject with a typed error instead of handler-throw); handler error envelopes converge on one shape (R013, R016, R010, R033, R034, S085's coercion generalizes, R035).
- **5.3 One owner per concept.** Single `RepoIndex` declaration; `repoName`-is-identity encoded in a type alias rather than five comments; canon/patchTree/numericUserId/TERMINAL deduped to one module each (R037, R039, R029, R028, R038 trimmed HandlerContext).
- **5.4 The daemon graph sheds the CLI.** Break the `notifier -> repo-arg -> fzf/ink` import chain (R050) so the daemon bundle stops carrying the TUI; extend `no-eager-tui` to assert it.
- **5.5 State growth and scan bounds.** Retention for `chat_messages` and `agents`; events journal queries take a LIMIT and push the topic glob into SQL; waiter cursors advance (R053, R054, S047, R030, S102); a `state.db` integrity check plus periodic backup answers R055; document the SCHEMA_VERSION claim protocol next to the constant and make column adds self-healing outside the version gate (R015, R056).
- **5.6 Seam tests.** Integration tests for socket-server and api-server (routing, token gate, WS upgrade, 500 envelope, CORS), eviction/shutdown file ownership, pollers wiring, and the boot-order invariants that comments currently carry (R024, R026, R027, R047, R018, R023).
- **5.7 Reconciler decomposition.** Split worktree-reconciler.ts's five duties (reconcile, reactor, freshen, replenish, shrink) into units behind the 5.1 seam; queue kicks instead of dropping them (R014, S065, S094, R041, R049). This is the largest single item and can trail the rest.

### Phase 6 · Someone else's Mac

Share-readiness. Mode: spec-light **B/S/P** for 6.1; the rest **SDD**. Some items gate sharing regardless of phase order; pull them forward if a share date lands first.

- **6.1 PATH resolution rebuilt.** The `$SHELL -ilc` scrape breaks on fish (space-separated PATH), hangs forever on a blocking rc file, and silently degrades when zshrc execs another shell or tmux. Replace with: non-interactive probe with a hard timeout in a killable process group, fish-aware parsing, explicit `rt.daemonPath` setting override, and a status warning when key tools are missing from the resolved PATH (S013, S014, S062; S046 cron spawns inherit the fix).
- **6.2 Identity and layout stability.** Machine settings key from a stable identifier (hardware UUID), migrating the hostname-keyed section (S071); legacy `~/.rt` migration verifies the directory is actually rt's before renaming (S099); foreign `~/.local/bin/rt` shims are detected and reported instead of silently parking prod forever (S020, S067, ties RT-73); link reconcile never unlinks mattstack's own tools (S066).
- **6.3 Optional tools are optional.** herdr resolved via PATH probe with a clear error (S002); agent-status poller and agent:start degrade gracefully when herdr is absent (S039, S051); Intel/unsupported-platform says so at setup (R051).
- **6.4 First-run honesty.** Missing `~/.mattstack/user` diagnosed as "not provisioned" not "git missing" (S090); git identity checked once with an actionable message (R043); keychain/sops spawns get timeouts (S070); poll-only users get MR notifications (S022); branch_cache keys on repo+branch (S069).

### Cross-cutting follow-ups (file as tickets, schedule opportunistically)

Chat/presence hardening (S052, S075, S076, S102 hot-path half, R057, R010 chat-validation half; 5.2 owns the generic seam validation), discussions/diffs timeouts (S053, S086), pane abort budget (S087), mr:by-branch scope (S088), herdr chunk decoding (S095), notifier double-fire (S096), port-scanner path canonicalization (S097), tray View Logs UX (S080, the loggy footgun), home-snapshot push backoff + RT-56 (R042), quarantine handle close (S103), hooks-guard watcher lifecycle and takeover honesty (S057, R044-R046), api-server malformed-encoding 500s (S083), daemon-client attribution flags (S081, S082), cron env (S046 rides 6.1), `@ts-expect-error` pin (R048), busy-warn log surface (R052).

## Suggested execution order and sizing

| Lane | Mode | Est. size | Blocks |
|---|---|---|---|
| Phase 0 | SDD now | ~10 tasks, 1-2 days of lane time | nothing |
| Phase 1 | SDD now | ~8 tasks | none (parallel with 0 if separate files) |
| Phase 2 | 1-page spec, then SDD | ~6 tasks | wants 0.5 |
| Phase 3 | SDD | ~6 tasks | none |
| Phase 4 | brainstorm -> spec -> plan | 1 spec + ~10 tasks | wants 2 (safety net) |
| Phase 5 | brainstorm -> spec -> plan, 2-3 lanes | largest | wants 2; 5.7 last |
| Phase 6 | 6.1 spec, rest SDD | ~8 tasks | pull forward if sharing soon |

Phases 0+1 together retire every P0/P1 crash-and-wedge path found. If only one lane runs this week, run Phase 0.
## Appendix A ... Confirmed findings

Every entry survived an independent adversarial verification pass (reproduce lens; Opus 4.8 for P0/P1, Sonnet for P2/P3), except where fixer notes say otherwise. P3 entries are deliberately terse: full scenario and fix, no extended notes.

#### S001 · P0 · hazard · `cli.ts:184`
**Prod daemon becomes a live zombie when startDaemon() throws: launchd, tray and CLI all see it as up**

Verified on Bun 1.3.13 (scratch script, no rt involved): with an unhandledRejection listener registered, `__main().catch((err) => { throw err; })` is delivered to the listener and the process keeps running (exit never happens; a later setTimeout fires). In the compiled prod daemon (`Contents/MacOS/rt --daemon`), `startDaemon()` installs that listener at line 392 and then can throw synchronously at several sites: `startApiServer` -> `Bun.serve({port: 9401})` throws EADDRINUSE synchronously (verified) whenever the previous daemon has not released 9401 within evictStaleDaemon's 300ms (it is often mid-`execSync` in cache-refresh.ts:119) or another local app holds 9401 (API_PORT is hardcoded, daemon-config.ts:72); `openBranchCacheStore()` -> `runMigrations` throws SQLITE_BUSY after the 5s budget while a CLI holds the write lock, or throws on a disk-full/partially corrupt db. Result: rt.pid already written (line 416), one `unhandledRejection` error line in the log, and a process that stays alive with only the module-scope timers running: no socket (or a socket that the still-exiting old daemon then unlinks), no API, no signal handlers, no pollers. launchd sees a healthy pid and never restarts it; the tray goes red 'Daemon: not running'; `daemonQuery`'s auto-restart and `rt daemon start` only call SMAppService.register (a no-op when already registered) and print 'daemon starting… check logs' forever. Only `rt daemon restart` (kickstart -k) recovers. In source mode (`bun run lib/daemon.ts`) the same throw is a top-level exception and exits 1 (also verified), so the author's dev machine never sees this; every other user's machine runs the prod path.

*Fix (small):* Make boot failure fatal on both entry paths: wrap the body of `startDaemon()` in try/catch that logs `fatal` and `process.exit(1)` (launchd then restarts, and the failure is visible), or have cli.ts call `startDaemon()` inside its own try/catch with the same exit. Independently, write rt.pid only after both servers are bound, and consider a boot-phase flag so `unhandledRejection` exits 1 until `daemon ready` is logged. Add a test that spawns the compiled entry with RT_API_PORT already bound and asserts a non-zero exit.
*Fixer notes:* Root cause is that installCrashHandlers' unhandledRejection handler (lib/daemon-logger.ts:242-244) treats a BOOT-time throw identically to a recoverable runtime stray rejection. Do NOT simply add process.exit(1) to that handler unconditionally: that defeats its intended runtime-recovery purpose. Instead either (a) wrap the body of startDaemon() (lib/daemon.ts:385-514) in try/catch that logs fatal and process.exit(1), or (b) gate the handler on a boot-phase flag so it exits(1) until "daemon ready" is logged (lib/daemon.ts:513). Independently, move the rt.pid write (lib/daemon.ts:416) to AFTER both server binds (after line 469) so a failed boot never leaves a live-pid file. IMPORTANT blast-radius: exiting on boot failure turns the silent zombie into a launchd crash-loop when the trigger persists (e.g., another app permanently on hardcoded 9401 at lib/daemon-config.ts:72, or a wedged write lock) -- so pair the exit fix with (i) a bounded bind retry / port fallback for API_PORT and (ii) replacing evictStaleDaemon's fixed 300ms (lib/daemon/boot-reconcile.ts:24) with actual verification that port 9401 is released before binding; a bounded crash-loop that launchd surfaces is still strictly better than a silent unrecoverable zombie. Tests to add: spawn the compiled --daemon entry with a listener already bound on RT_API_PORT and assert non-zero exit + no stale live rt.pid; and a SQLITE_BUSY test holding the state.db write lock >5s across daemon boot asserting the same. Also note cache-refresh.ts's synchronous execSync git calls on the daemon thread are the reason evictStaleDaemon's SIGTERM is not honored in time -- that is a separate but contributing hazard.
*Partial mitigation today:* Partial only. evictStaleDaemon (lib/daemon/boot-reconcile.ts:16-26) reduces the same-flavor EADDRINUSE-on-restart window but is a fixed 300ms sleep with no port-release verification, so a slow-to-die old daemon (mid-execSync) still wins the port. openStateDb's corruption quarantine (lib/state/db.ts:489-494) covers only the corrupt-db sub-case of throw site A (SQLITE_CORRUPT/SQLITE_NOTADB surfaced by the early PRAGMA user_version probe at line 488); it does NOT cover SQLITE_BUSY, SQLITE_FULL, or corruption that first manifests mid-migration. parkUntilIntended (lib/daemon.ts:103) prevents only the wrong-FLAVOR collision, not same-flavor port contention or a third-party app on 9401. None of these prevent the swallow-and-zombie once a throw does propagate.

#### S002 · P1 · bug · `lib/agent-herdr.ts:28`
**rt agent hardcodes ~/.local/bin/herdr while setup installs herdr via brew**

A teammate with Homebrew clicks Install on the herdr row; `brew install herdr` places the binary at /opt/homebrew/bin/herdr and the tool.herdr row goes green. Later `rt agent start` (surface herdr, the default per handlers/agent.ts:132) runs runCapture(['/Users/x/.local/bin/herdr','workspace','list']); Bun.spawn throws ENOENT, runCapture returns exitCode -1, and the user sees 'herdr workspace list failed (-1): ' with no hint that the path is wrong. The same default is used by herdrAgentWait. Only the author's vendor-script install (which drops herdr in ~/.local/bin) works.

*Fix (trivial):* Resolve like lib/cswap.ts does: `process.env.HERDR_BIN ?? Bun.which('herdr') ?? join(home,'.local','bin','herdr')`, and surface ENOENT as 'herdr not found' with the setup remedy.
*Fixer notes:* Fix at lib/agent-herdr.ts:28 mirroring lib/cswap.ts:28-30: `const bin = process.env.HERDR_BIN ?? Bun.which("herdr") ?? join(home, ".local", "bin", "herdr");`. Bun.which reads process.env.PATH at call time (not Bun.spawn's start-env resolution), and the daemon overlays the user's full login PATH onto process.env.PATH at boot (lib/daemon.ts:120-122, resolveUserPath), so Bun.which("herdr") resolves /opt/homebrew/bin/herdr inside the daemon ... this exact pattern already works for cswapBin() in the same process, so the fix is proven in-context and strictly better (the ~/.local/bin fallback preserves current behavior if resolveUserPath ever returns null). Also surface ENOENT distinctly: when the resolved bin does not exist, throw a "herdr not found (install via `rt setup` / brew) " message instead of the bare "failed (-1)". Add a regression test alongside lib/__tests__/agent-herdr.test.ts that asserts defaultHerdrRunner prefers a PATH-resolved herdr over the ~/.local/bin fallback. Blast radius: agent:start/agent:resume (handlers/agent.ts:93) and rebase-escalation.ts:211 both consume defaultHerdrRunner, so both are fixed by the one change; herdrAgentWait shares it too.

#### S003 · P1 · hazard · `lib/daemon.ts:85`
**Boot failures before startDaemon() crash-loop under launchd with nothing written anywhere**

~/.mattstack/rt/logs is unwritable (EACCES after a restore, disk full, HOME on a read-only volume), or events.db is corrupt (createEventsBus at daemon.ts:191 runs at module scope and has no quarantine path, unlike lib/state/db.ts). The module-scope await rejects before redirectNativeStderr/installCrashHandlers ever run; Bun prints the stack to fd 2, which launchd points at /dev/null; exit 1 -> KeepAlive={SuccessfulExit:false} respawns every 10s (ThrottleInterval) forever. Tray: red 'Daemon: not running'. rt daemon status: 'installed but not running / run: rt daemon start'. rt daemon install: 'the process keeps exiting. Check logs: rt daemon logs' -> 'no daemon logs yet'. A non-author user has no artifact at all to send you.

*Fix (small):* Move redirectNativeStderr() to the very top of lib/daemon.ts (it depends on nothing but rt-paths) and also call it first thing in cli.ts's --daemon branch. Wrap getDaemonLogger() in a try/catch that falls back to a pino instance writing to fd 2 (now the stderr file) and marks the handle degraded. Give createEventsBus the same isCorruptionError+quarantine path db.ts has. Write a one-line boot breadcrumb (~/.mattstack/rt/daemon-boot.json {at,pid,phase}) so a crash loop is detectable by the CLI/tray even when logging is dead.
*Fixer notes:* Fix is safe and matches the finding's suggestion. (a) Hoist redirectNativeStderr() to the very top of lib/daemon.ts module scope, before line 78/85 ... it depends only on logsDir() (rt-paths), does its own mkdirSync+openSync, and already has an internal try/catch, so after hoisting even an uncaught module-scope throw's raw fd-2 write lands in daemon-stderr.log. (b) Give createEventsBus the same isCorruptionError+quarantine+recreate wrapper openStateDb uses (db.ts:483-494) around the open+first-PRAGMA, so a corrupt events.db self-heals instead of throwing. (c) Optionally wrap getDaemonLogger() at :85 in try/catch with a fd-2 pino fallback + degraded flag. Blast radius: (a) changes only startup ordering, no behavior for callers; verify no other module-scope code between the new top and old line 391 depends on fd 2 being unredirected. Tests: add a createEventsBus case opening a deliberately corrupted db file (mirror an events-bus.test.ts fixture) asserting it quarantines rather than throws; add a daemon-boot test asserting redirectNativeStderr runs before createEventsBus. Also worth a boot-breadcrumb (~/.mattstack/rt/daemon-boot.json) so the CLI/tray can detect a crash loop when logging is dead. Note the prior audit doc docs/daemon-runner-health.md does not currently list this item.
*Partial mitigation today:* Partial only. lib/state/db.ts:307-309 (isCorruptionError) + :377-389 (quarantine) + :483-494 (open-and-quarantine-then-recreate) handle a corrupt state.db, but this pattern is NOT applied to events.db in lib/daemon/events-bus.ts:76-86. redirectNativeStderr (lib/daemon-logger.ts:204) exists and would capture the stack, but it is called only at lib/daemon.ts:391 inside startDaemon(), after both module-scope failure points. lazyChildLogger has a process.stderr fallback (lib/daemon-logger.ts:155) but daemon.ts uses `await getDaemonLogger()` directly, not lazyChildLogger, and pre-redirect fd 2 is /dev/null anyway.

#### S004 · P1 · hazard · `lib/daemon.ts:391`
**Module-scope boot failures on the prod path are printed to a stderr launchd discards; a crash loop leaves no error in any log**

On the prod path (`Contents/MacOS/rt --daemon`, no Swift shim in front) any throw or rejection between the first line of lib/daemon.ts and startDaemon() line 391 exits 1 with Bun's error text written to the inherited fd 2, which the plist leaves pointing at /dev/null. Concrete triggers: (a) events.db damaged by a power loss or full disk: `new Database` or the WAL pragma throws SQLITE_NOTADB/CORRUPT at line 191 and, unlike state.db, nothing quarantines it, so this repeats on every boot; (b) `~/.mattstack/rt/logs` unwritable (a `sudo rt ...` once created it root-owned): line 85 rejects before the logger exists. KeepAlive relaunches every 10s indefinitely; each iteration runs an interactive login shell (`$SHELL -ilc`, user-path.ts:37) and creates fnm multishell entries. What the user sees: tray dot red with 'Daemon: not running', `rt daemon status` -> 'installed but not running / run: rt daemon start', `rt daemon start` -> 'daemon starting… check logs', and `rt daemon logs` showing only 'PATH resolved' lines repeating every 10s with no error, because the fatal never went through pino. The dev shim (main.swift:239 freopen) covers source mode only, so the author never experiences this.

*Fix (small):* Call `redirectNativeStderr()` as the very first statement of lib/daemon.ts (before the logger await) so Bun's uncaught-error output lands in daemon-stderr.log on both entry paths; wrap the module-scope boot (or have cli.ts wrap the dynamic import) so a rejection is logged as fatal via a fallback appendFileSync before exit 1. Give events.db the same corruption quarantine state.db has. Consider a boot-attempt counter file so the tray can say 'daemon is crash-looping, see logs' instead of 'not running'.
*Fixer notes:* Order of fixes by blast radius: (1) Give events.db the same corruption quarantine state.db has (wrap events-bus.ts:76-78 to detect SQLITE_CORRUPT/SQLITE_NOTADB, rename aside, recreate) ... this alone converts trigger (a) from a permanent loop into a one-time self-heal and is the single highest-value change. (2) For prod-path stderr capture: calling redirectNativeStderr() as the very first statement of lib/daemon.ts fixes trigger (a) and any module-scope throw WHERE THE LOGS DIR IS WRITABLE, but does NOT fix trigger (b): redirectNativeStderr itself calls logsDir()/mkdirSync/openSync and silently no-ops (its own catch, daemon-logger.ts:216-218) when the logs dir is unwritable, so stderr stays at /dev/null. Trigger (b) needs either the Swift-shim approach extended to prod (redirect fd 2 in native code before bun evaluates the module ... but the target dir is the very thing that's unwritable) or a boot-attempt counter file the tray reads to surface "daemon crash-looping" instead of "not running". (3) Wrapping the module-scope boot (or the cli.ts:91 import) in a fallback that appendFileSync's the error before exit only helps when SOME writable location exists ... pick a path independent of the failing logs dir. Blast radius: changes touch the daemon boot path and the plist contract is unchanged; add a test asserting a corrupt events.db is quarantined-and-recreated (mirror the existing state.db quarantine test), and a test that a module-scope throw is captured on the prod path. Do NOT move createEventsBus/getDaemonLogger into startDaemon() casually ... several module-scope units (cron, home-snapshot, agent-status poller, the setInterval sweeps) capture `log`/`eventsBus` and the flavor gate at daemon.ts:103 must stay above them.
*Partial mitigation today:* Partial, dev-only: rt-tray/Sources-daemon-shim/main.swift:239 freopens fd 2 to daemon-stderr.log before spawning bun, so the DEV bundle captures module-scope crashes. No equivalent exists on the prod path ... the compiled bun (Contents/MacOS/rt) only redirects at daemon.ts:391 inside startDaemon(), after the import that evaluated module scope. state/db.ts:483-494 quarantines a corrupt state.db, but events-bus.ts:76-77 has no such guard for events.db.

#### S005 · P1 · hazard · `lib/daemon/api-server.ts:115`
**WebSocket /ws has no origin or token check; any web page can subscribe to every broadcast frame**

The upgrade runs before the CORS/token block and WebSocket upgrades are not subject to CORS. A page the user visits (or a DNS-rebinding page, or another user account on a shared Mac since 127.0.0.1:9401 is reachable by every local uid) opens ws://127.0.0.1:9401/ws and receives every frame: `mr:update` with full PullRequest objects (freshness.ts:570), `discussions:update` with discussion bodies (discussions-store.ts:205), `notification` text, `system-processes` with fullCommand argv of every repo process (pollers.ts:61), `event` frames carrying chat wake ids and arbitrary events:emit payloads, `home:snapshot` paths. The api-auth.ts rationale ("reads are free") assumed CORS governs reads; it does not govern WS.

*Fix (small):* Gate the upgrade: require the X-RT-Token (via a `?token=` query param or Sec-WebSocket-Protocol subprotocol, since browsers cannot set custom headers on WS) OR reject any upgrade that carries an Origin header not in an explicit local allowlist. Rejecting all browser Origins except the configured console/chat-viewer origins is the smallest change.
*Fixer notes:* Gate the upgrade at api-server.ts:116 before calling server.upgrade: reject any request whose Origin header is present and not in a small local allowlist (console/chat-viewer origins), AND/OR require the api-token via `?token=` query param or a Sec-WebSocket-Protocol subprotocol (browsers cannot set custom headers on WS, so X-RT-Token as a header is not usable there). The token is already available in startApiServer scope (`apiToken` at line 101). Note two adjacent items for the fixer, but they are separate findings: (1) the same read exposure exists on the REST GET routes (CORS `*` + no token) returning process argv / repos / cache, e.g. /api/processes/system ... Chrome PNA now hampers cross-site fetch to loopback but same-origin/local-uid access is open, so consider tightening reads holistically rather than only WS; (2) tokenOk (api-auth.ts:53-54) uses non-constant-time `===`, a minor hardening item, not this finding. Add a test asserting an upgrade with a disallowed/absent Origin and no token is rejected (there is currently no api-server WS auth test).
*Partial mitigation today:* None for /ws. The api-token infra (api-auth.ts loadOrCreateApiToken/needsToken/tokenOk, applied at api-server.ts:133) gates only mutating REST routes and /api/secrets; the /ws upgrade at api-server.ts:116 bypasses it entirely and needsToken returns false for /ws regardless.

#### S006 · P1 · hazard · `lib/daemon/api-server.ts:121`
**Open CORS plus unauthenticated reads and WS expose process argv, repo paths and MR data to any web page**

On a teammate's machine, any website they visit can `fetch('http://127.0.0.1:9401/api/processes/system')` and read the response (CORS *), which carries `fullCommand` (the complete argv of every process rooted in a tracked repo, routinely including --token, DB URLs and passwords), `/api/repos` (absolute paths and username), `/api/cache` (branch names, MR titles, Linear ticket titles), `/api/runs`, `/api/events` (chat/pane bus), and can open `ws://127.0.0.1:9401/ws` (no Origin check) to stream every broadcast including notifications and system-processes frames. POST /api/refresh and /api/hooks/:repo/repair are also ungated (simple requests need no preflight), so a page can force GitLab API churn. The doc comment in api-auth.ts states reads are left open 'for convenience', which was acceptable for one author and is a data leak for a shared product working on corporate repos.

*Fix (medium):* Default-deny CORS (echo only origins listed in a settings key, otherwise no ACAO header), require X-RT-Token on every /api route except `/`, reject WebSocket upgrades whose Origin header is present and not allow-listed, and strip `fullCommand` down to the binary name in the REST/WS payloads.
*Fixer notes:* Highest-value, lowest-blast-radius first: (1) reject WS upgrades whose Origin header is present and not allow-listed (api-server.ts:115-118) ... closes the firehose and cannot break the tray/CLI (non-browser clients send no Origin); (2) default-deny CORS: echo ACAO only for allow-listed origins from a settings key, else emit no ACAO header ... closes the fetch-read vector without touching trusted local clients that don't rely on CORS. Requiring X-RT-Token on the reads too is heavier: audit packages/rt-client consumers first (the Swift tray already reads the token file per api-auth.ts docblock, but the chat viewer and console browser apps likely depend on open reads and would break). Also redact fullCommand to argv0/basename in both the REST payload (system-processes.ts) and the WS broadcast (pollers.ts) so the argv-secret path is closed regardless of transport. Add regression tests: a disallowed Origin gets no ACAO:* on /api/repos, and /ws rejects a foreign Origin. Note the prior audit docs/daemon-runner-health.md does not mention this surface, so it is a genuinely new finding, not a re-report.
*Partial mitigation today:* Partial only, none covering the finding's core: loopback bind (api-server.ts:106) blocks LAN exposure but not same-machine browser requests; the token gate (api-auth.ts:40-50) protects shutdown/secrets/sdm-reconnect/events-emit but not the reads, the WS stream, or the refresh/hooks POSTs. Browser Private Network Access may block the fetch-read path in the very latest Chrome, but it is not a code guard, is absent in Firefox/Safari, and does not close the Origin-less WebSocket vector.

#### S007 · P1 · hazard · `lib/daemon/cache-refresh.ts:57`
**One hung GitLab request wedges the coalesced 5-min refresh, discussions sweep and project sync forever**

Laptop sleeps (or VPN/Wi-Fi drops) while a refresh cycle's GraphQL request to GitLab is in flight; on wake the TCP connection is half-open with no RST and no keepalive, so fetch() never settles (verified: Bun's fetch stays pending indefinitely against a silent socket, >8s in the probe; only Linear has AbortSignal.timeout(15_000) at linear.ts:163). refreshInFlight never clears, so every subsequent timer tick and every `cache:refresh` IPC returns the same dead promise; `cache:read` with maxAgeMs (handlers/cache.ts:51 `await ctx.refreshCache()`) blocks the board/console indefinitely; `lastRefreshAt` freezes; branch-cache GC, notifier transitions, worktree reconciler kicks and doppler sync all stop. The same shape latches `sweeping` (discussions poller), `syncInFlight` per repo (project sync, including watcher-start syncs), and a RepoWatch's `processing` flag, whose `pending` array then grows by every 15s watcher tick until the daemon restarts. No log line distinguishes this from a quiet system.

*Fix (medium):* Bound every outbound GitLab call: wrap provider calls in refreshCacheImpl/sweep/syncImpl/processKeys with a per-call deadline (Promise.race + AbortController if glance exposes a signal option; otherwise ask glance to accept a `signal`/`timeoutMs` in the provider constructor, as it already accepts `onRequest`). Add a cycle deadline to refreshCache (e.g. 4 min) that logs `cache refresh timed out` and clears refreshInFlight so the next tick can start, and cap `runner.pending` (dedupe by kind:ref on push).
*Fixer notes:* The durable fix must bound the DAEMON-side outbound GitLab calls, not only add a cycle deadline. (1) Best: give @mattstack/glance's GitLabProvider a signal/timeoutMs option threaded into runQuery/restRequest/MRDetailFetcher.fetchDetail (all three fetch sites: GitLabProvider.js:1925, 1565, 146) ... glance already accepts an onRequest hook in its constructor, so a timeout option is a natural addition. Cross-repo: glance source is workforge/packages/glance-sdk (per estate memory), and the change fans out to all consumers (mr-board, gitq, console, deck), so it must go out as a coordinated glance version bump. (2) Stopgap without touching glance: wrap each provider call in refreshCacheImpl/sweep/syncImpl/processKeys with Promise.race against a per-call deadline. IMPORTANT caveat for the fixer: Promise.race does NOT cancel the underlying fetch ... the dead socket and its async frame leak until the OS reaps the connection ... but it DOES let the race reject, which clears refreshInFlight/sweeping/syncInFlight/processing so the NEXT tick proceeds; that is the correctness-restoring behavior. Pair with the finding's ~4-min cycle deadline on refreshCache that logs 'cache refresh timed out' (addresses the silent property) and clears refreshInFlight. (3) Independent of timeouts, cap freshness.ts RepoWatch.pending (dedupe kind:ref on push at freshness.ts:393, or bound length) so a wedged processKeys cannot grow memory unbounded. Deadline sizing: set the per-call deadline generously above the slowest legitimate query ... project-sync.ts's own header documents the 592-MR DEEP reconcile hitting GitLab resolver timeouts, so a too-tight deadline would abort real deep syncs; scope it per-operation (deep vs delta vs single). Tests: reuse existing seams ... project-sync.ts fetch* overrides and freshness.ts BatchRunner/MappingOverrides ... with a fetch that never resolves, and assert the guard flag clears after the deadline and the next call starts fresh; add one for pending-cap growth.
*Partial mitigation today:* Partial only, and none bound the daemon-side wedge: daemon-client.ts:31,55 (REQUEST_TIMEOUT_MS=2000 + AbortSignal.timeout(2000)) bounds only the client-facing symptom ... a board/console cache:read errors out after 2s instead of hanging, but the daemon's `await ctx.refreshCache()` in handlers/cache.ts:51 keeps awaiting the dead promise (leaked handler frame) and the timer-driven refresh is unaffected. linear.ts:163 (AbortSignal.timeout(15_000)) covers only the Linear leg of refreshAllMRs, which runs AFTER the unguarded GitLab fetch, so it never bounds this hang.

#### S008 · P1 · hazard · `lib/daemon/cache-refresh.ts:119`
**execSync git calls still run on the daemon thread inside the 5-minute refresh, and Bun's execSync timeout cannot bound them**

A user indexes a repo that lives on an external drive or network share (or a repo whose .git has a stale `index.lock` / very large packed-refs / a worktree path on a disconnected volume that `git worktree list` stats). On the next 5-minute tick the daemon's single thread blocks inside execSync for the whole git lifetime: the unix socket and :9401 stop answering, every CLI call times out, the tray paints red, freshness callbacks and events:wait waiters are starved. No timeout is even passed to these calls, and even one would not help because Bun's execSync waits for exit after SIGTERM. This is four sync spawns per repo per cycle (listWorktrees, for-each-ref, git config, listWorktreeRoots), and the doppler loop at lines 206-226 runs listWorktreeRoots + deriveRepoIdentity + getSetting for EVERY repo in the index regardless of tracking grants, contradicting line 86's 'off: zero background work' promise, so an untracked repo can still stall the daemon.

*Fix (small):* Replace the four execSync sites with the async helpers that already exist: listWorktreesAsync (lib/worktree/git-async.ts) for both worktree listings, runCapture(["git","for-each-ref",...]) and runCapture(["git","config",...]) with a 10s timeout; make getRemoteUrl in freshness.ts async (it is only called from async paths). Gate the doppler loop on `grants(...).caches.size > 0` like the enrichment loop above it.
*Fixer notes:* Swap all four sync sites for the existing async helpers and await them inside refreshCacheImpl (already async): use listWorktreesAsync (lib/worktree/git-async.ts, returns WorktreeEntry[]|null with branch:string|null ... the existing `w.branch && !w.branch.startsWith("on-deck/")` filter and a `.map(e=>e.path)` for roots both adapt cleanly, and its null return already distinguishes "git failed" from "no worktrees" so do not treat null as []) ; use runCapture(["git","for-each-ref",…]) and runCapture(["git","config","--get","remote.origin.url"], {timeoutMs:10_000, stderr:"pipe"}). Make freshness.ts getRemoteUrl async (its callers ensureProvider/getRepoContext/reconcileFreshnessImpl are all already async). Independently of the blocking fix, gate the doppler loop (cache-refresh.ts:206) on `grants(tracking, repoName).caches.size > 0` (or a specific grant) to honor the line-86 "off = zero background work" contract ... this is a correctness fix worth doing regardless. Blast radius: git-worktrees.ts's sync listWorktrees/listWorktreeRoots have other callers (grep before changing their signatures) ... prefer routing cache-refresh through listWorktreesAsync rather than changing the shared sync helpers. Add a regression test that a repo on a fake slow/hanging git dir does not block a concurrent daemon status poll (the async path with a timeout returns; the execSync path hangs). Note the secondary claim (SIGTERM-immune child defeats a timeout) is plausible per the cited 2033ms probe but is not independently verifiable here without spawning; it does not change the verdict since the primary defect (execSync blocks the loop at all) holds regardless of whether a timeout is added.
*Partial mitigation today:* Partial only: existsSync(repoPath) at cache-refresh.ts:83 and :207 short-circuits a fully-disconnected drive (fast ENOENT), but does NOT cover a present-but-slow volume (network share, sleeping external SSD), a very large packed-refs, or a worktree path on a different/disconnected volume that `git worktree list` and the existsSync(path) parse loop (git-worktrees.ts:36,67) then stat. No timeout guards any site; refreshInFlight coalescing (cache-refresh.ts:57) prevents overlap but not the block itself.

#### S009 · P1 · hazard · `lib/daemon/events-bus.ts:76`
**events.db has none of state.db's guarantees and is opened at module scope before crash handlers exist**

A truncated or non-sqlite events.db (disk-full crash, restore from backup, stray file) makes `PRAGMA journal_mode`/`CREATE TABLE` throw SQLITE_CORRUPT/NOTADB during module evaluation, before the pino crash handler is installed: the daemon exits with only raw stderr, launchd relaunches it forever, and daemon.log shows nothing. Unlike state.db there is no `.corrupt-` rename, so the loop never ends without manual deletion. Separately, with no busy handler any write-lock overlap (the outgoing daemon during evictStaleDaemon, a future consumer) makes `emit` throw SQLITE_BUSY immediately out of events:emit and every chat post's wake path; and the default synchronous=FULL fsyncs the WAL on every emit on the event loop, where state.db chose NORMAL.

*Fix (small):* Reuse state/db.ts's isCorruptionError + quarantine pattern in createEventsBus (rename to events.db.corrupt-, recreate, warn), and construct the bus inside startDaemon() where the crash handlers are installed.
*Fixer notes:* Fix has two independent parts, both needed. (1) Give events.db the same corruption escape as state.db: wrap the open+first-pragma in try/catch, reuse db.ts's isCorruptionError + a quarantine-style rename to events.db.corrupt-, recreate empty, warn. events.db is a bounded-retention journal, so total loss on quarantine is harmless ... no migration/data-preservation concern. (2) Move construction inside startDaemon() (after redirectNativeStderr()+installCrashHandlers() at daemon.ts:391-392) so ANY open failure is logged to daemon.log and the crash path is covered; keep the eventsBus.close() in cleanup() (daemon.ts:378) consistent with the later construction. While there, also set `PRAGMA busy_timeout` and `PRAGMA synchronous = NORMAL` in createEventsBus to match applyPragmas (db.ts:344-348) ... cheap and closes the FULL-fsync-per-emit and SQLITE_BUSY-during-eviction gaps. Note the root cause is broader than events.db: cli.ts:80 excludes --daemon from CLI logging on the assumption the daemon self-covers, but nothing covers a throw during lib/daemon.ts module evaluation (any module-scope constructor at daemon.ts:111-304 has the same exposure); consider a top-level try/catch around the daemon import+startDaemon in cli.ts as defense in depth. Tests: add an events-bus corruption test (write garbage bytes to a temp events.db, assert createEventsBus quarantines+recreates rather than throwing), mirroring the existing state.db quarantine test.

#### S010 · P1 · hazard · `lib/daemon/handlers/worktree.ts:360`
**Caller-supplied branch reaches `git fetch origin ` and `rev-list` unescaped (option injection, socket-only)**

A payload with branch "--upload-pack=touch /tmp/x" makes git run that command during the fetch. Reachable only over the unix socket (api-server.ts:53-66 exposes no worktree routes over REST, and mutating REST routes are token-gated), so today the attacker is the local user; but a consumer app (mr-board, console, chat viewer) that forwards an untrusted string as `branch` inherits the hole, and git rejects nothing here because the slug path is the only sanitized one.

*Fix (trivial):* Validate `payload.branch` with `git check-ref-format --branch` (or reject a leading '-') before use, and pass refspecs after `--` where git accepts it.
*Fixer notes:* Fix once, at the source: validate `payload.branch` before the assignment at worktree.ts:282 (e.g. reject anything starting with `-`, or shell out to `git check-ref-format --branch ` and refuse on nonzero). That single guard also covers the weaker secondary instance at divergence() worktree.ts:211-213 (`rev-list ... ${branch}...origin/${branch}`), which is harder to exploit as a single glued token but is the same unvalidated-input class. As defense in depth, pass the ref after a literal `--` in the `fetch`/`rev-list` argv where git's parser supports it, so a future caller that reintroduces an unsanitized branch doesn't regress the same way. Add a regression test in lib/daemon/handlers/ (or wherever worktree handler tests live) asserting `worktree:provision` returns a clean rejection for a branch payload beginning with `-`, and a unit test that `runGit`'s callers never hand it an option-shaped token ... this is worth a repo-wide grep for other `runGit(..., [..., branch/ref, ...])` call sites since the same pattern could exist elsewhere (e.g. `checkout -b branch startPoint` at worktree.ts:398/403 uses `branch` too, though `-b` positioning there is less exploitable since branch is the ref being created, not the remote name ... still worth the same guard for consistency). Do not attempt to fix by only checking `origin`/known-good redirect targets ... the vulnerable field is `branch`, not `origin`.

#### S011 · P1 · bug · `lib/daemon/home-snapshot.ts:272`
**startHomeSnapshot opens state.db at module scope with the CLI flavor; the daemon then runs with a 5000ms busy_timeout forever**

Line 289 runs before startDaemon(); `startHomeSnapshot`'s deps object evaluates `getStateDb()` eagerly with the default `"cli"` flavor, and `loadState(deps.db)` (line 299) reads kv rows right there. The singleton is now open; every later `getStateDb("daemon")` (daemon.ts:153, :450) returns that same connection because the flavor argument is only consulted at open time. So the daemon's steady-state `PRAGMA busy_timeout` is 5000ms, not the 250ms every `persistOrWarn` site, the spec ('the daemon loop must never block long'), and lib/state/busy.ts assume. Concretely: a CLI process holding the write lock for a few hundred ms (`rt repos prune`, an `rt run` enrichment upsert batch, `rt chat` posting in a transaction) now stalls the daemon event loop for up to 5s per contended write instead of warn-and-defer at 250ms; during that stall the tray's 2s status poll times out (red flicker, the exact symptom the tray-flicker memory documents), `events:wait` long-polls miss wakeups, and WS broadcasts queue. Also contradicts the module's own stated rule and the spec's boot ordering: the db is created/migrated at module scope on a blank HOME before `startDaemon` runs.

*Fix (small):* Pass `db: getStateDb("daemon")` explicitly from daemon.ts (and move `startHomeSnapshot` into `startDaemon()` after `openBranchCacheStore()`), or make home-snapshot resolve its db lazily on first use. Harden `getStateDb`: if the singleton is open and a caller asks for a different flavor, either re-apply `PRAGMA busy_timeout` for the stronger policy or throw so the mismatch cannot be silent. Add a test asserting the daemon-side connection reports busy_timeout=250 after boot.
*Fixer notes:* Two bugs share one root: wrong flavor AND module-scope db open. Do NOT just change daemon.ts:289 to pass `db: getStateDb("daemon")` ... that fixes the flavor but still opens/migrates the singleton at module scope, keeping the spec violation. Lowest-risk correct fix: give home-snapshot a lazy db resolver ... replace the eager `db: rawDeps.db ?? getStateDb()` (home-snapshot.ts:272) and the construction-time `loadState(deps.db, ...)` (line 299) with a thunk defaulting to `() => getStateDb("daemon")`, resolved on first use (runNow/status/loadState), so no db opens until startDaemon has already opened it daemon-flavored via openBranchCacheStore (daemon.ts:422). Alternative: move the startHomeSnapshot(...) call into startDaemon() after openBranchCacheStore(); but note homeSnapshot must stay a module-scope binding because cleanup (daemon.ts:378) closes over it and buildRoutedHandlers (daemon.ts:445) passes it ... so use `let homeSnapshot` assigned inside startDaemon, and verify both references. Harden getStateDb (db.ts:522): when the singleton is already open and a caller requests a shorter (stronger) flavor timeout, re-apply the pragma or throw on mismatch so this cannot recur silently. Add a regression test: call getStateDb() (default) then getStateDb("daemon") and assert PRAGMA busy_timeout is 250 on the returned handle; and/or a boot-order integration test asserting the daemon connection reports 250 post-boot. Blast radius of the flavor fix alone is nil (only tightens the timeout to spec; no schema/data change).

#### S012 · P1 · hazard · `lib/daemon/shutdown.ts:44`
**Stale-daemon eviction races the old daemon's cleanup, which unlinks the new daemon's rt.sock and rt.pid**

Two same-flavor daemons exist (a manual `rt --daemon`, a stuck kickstart, or the dual-registration state the flavor spec describes). The new daemon SIGTERMs the pid in rt.pid, sleeps 300ms, unlinks the stale socket, binds rt.sock, writes rt.pid, binds 9401. The old daemon is inside one of the synchronous git `execSync` calls on its event loop (cache-refresh.ts:119/139, freshness.ts:97, worktree-process-kill.ts:132) which routinely exceed 300ms on a large repo, so its SIGTERM handler runs later; `cleanup()` then unlinks whatever rt.sock and rt.pid exist, i.e. the NEW daemon's. The new daemon is now serving on an unlinked socket: every CLI call sees no socket file, `daemonQuery` posts /daemon/start (a no-op re-register), the tray shows down, `rt daemon status` says 'installed but not running', and nothing heals it until a kickstart. If the old daemon still held 9401 at bind time the new one instead hits finding 1's zombie path.

*Fix (small):* Make cleanup ownership-aware: unlink rt.pid only if its content equals `process.pid`, and unlink rt.sock only when that pid check passes (or compare the socket inode captured at bind). Make eviction wait for the pid to actually die (poll `process.kill(pid, 0)` up to a few seconds, then SIGKILL) before binding, instead of a fixed 300ms.
*Fixer notes:* Root cause is the fixed 300ms with no death-confirmation. The highest-leverage fix is making eviction actually wait for the previous pid to die: poll process.kill(pid,0) up to a few seconds, escalate to SIGKILL, and only then write rt.pid / bind. That single change closes BOTH this race AND the 9401 EADDRINUSE crossover (finding 1), because the successor never binds while the predecessor is alive. The suggested ownership-aware unlink is a good belt-and-suspenders addition: in shutdown.ts:cleanup, unlink rt.pid only when readFileSync(DAEMON_PID_PATH) === String(process.pid), and gate the rt.sock unlink on that same check. Tests: (1) evictStaleDaemon with a stubbed slow-to-die pid asserting it waits/escalates before returning; (2) cleanup() no-ops the unlinks when rt.pid contents != process.pid. Blast radius is eviction/shutdown only ... socket-server.ts's unconditional unlink+bind can stay (unix sockets never EADDRINUSE), but adding reusePort or a bounded retry to startApiServer (api-server.ts:103) is worthwhile defense-in-depth. Note the docs/daemon-runner-health.md audit predates this code (it references the old monolithic lib/daemon.ts with process-manager/remedy-engine, all since removed) and does not cover boot/eviction/shutdown, so it offers no coverage here.
*Partial mitigation today:* None for this path. park.ts's standoff (park.ts:63-77) is the only nearby guard and it explicitly excludes same-flavor and unknown-flavor holders (park.ts:67, 'eviction's job, not ours'), so it does not gate evictStaleDaemon here. cleanup() (shutdown.ts:44) has no pid/ownership check; startSocketServer (socket-server.ts:20) has no owner probe; startApiServer (api-server.ts:103) has no reusePort/retry.

#### S013 · P1 · bug · `lib/daemon/user-path.ts:37`
**A fish login shell makes resolveUserPath emit a space-separated PATH that is then split on ':'**

For a user whose login shell is fish (SHELL=/opt/homebrew/bin/fish), `fish -ilc 'echo $PATH'` prints the list elements separated by spaces, not colons. The daemon assigns that string to process.env.PATH and then prepends the bundle Helpers dir and ~/.local/bin with ':'. Every child that inherits `env: process.env` (runCapture, home-snapshot's git, worktree ready steps, cron commands) and every `execSync` via /bin/sh (cache-refresh.ts:119/139, freshness.ts:97) now has a PATH whose real directories are all inside one bogus entry, so `git`, `node`, `pnpm`, `bun` are not found by the shell or by `#!/usr/bin/env` shebangs: cache refresh logs 'local branch listing failed' for every repo, home-snapshot init reports 'could not run git', worktree provisioning fails exactly like the 2026-08-21 spawn-env incident. The 'PATH resolved' info line reports `entries: 1` but nothing warns. Tools resolved by Bun.spawn's own lookup (which uses the process-start PATH) still work, making the failure look inconsistent.

*Fix (small):* Print the PATH in a shell-neutral way: run the user's shell only to obtain the environment (e.g. `$SHELL -ilc 'env'` or `-ilc 'printf %s "$PATH"'` for POSIX shells, and `string join : $PATH` when basename($SHELL) is fish), or read `/etc/shells`-style dispatch on the shell name. Validate the result: if it contains whitespace or no ':' with more than one segment, warn and keep the baseline. Add `hasNode`/`hasGit` to the probe and warn when the scrape fell back.
*Fixer notes:* Two-part fix. (a) Make resolveUserPath shell-neutral: detect fish via basename($SHELL) and emit a colon-joined list (`fish -ilc 'string join : $PATH'`), or scrape the environment rather than `echo $PATH`. (b) Validate before assigning in daemon.ts:122 ... reject a scraped value that contains whitespace or that splits into <2 colon segments, warn, and keep the baseline launchd PATH. Add hasNode to the probe (user-path.ts:58 already omits it; the spawn-env contract doc flags this exact observability gap) and warn when core tools are missing after resolution. Regression coverage: user-path.test.ts currently only exercises probeTools ... add a case that a fish-style space-separated scrape is normalized or rejected. Blast radius to fix/verify is the execSync consumers (cache-refresh.ts, freshness.ts, git-worktrees.ts), not home-snapshot or worktree ready steps. Note the residual uncertainty in the corrected scenario: it rests on the repo-documented claim that Bun.spawn resolves argv[0] via the process-start PATH even when an explicit env is passed (daemon.ts:128-130, and the Bun.spawn PATH-resolution memory); if Bun instead honors the passed env PATH for argv resolution, home-snapshot and worktree git ops would break too, making the bug strictly worse, not better ... so the fix (validate the PATH) is correct either way.

#### S014 · P1 · hazard · `lib/daemon/user-path.ts:37`
**Daemon boot blocks forever if the user's interactive shell hangs: interactive zsh/bash ignore SIGTERM and execSync waits for exit**

On a teammate's machine `.zshrc` does something that blocks under launchd's no-TTY, no-network-yet context: a plugin that waits on a network call (corp DNS/VPN not up at login), `gpg-agent`/`ssh-add` waiting on pinentry, `brew` auto-update, a `direnv`/`nvm` hook stuck on a lock. `zsh -ilc` sources .zshrc, the 30s timeout fires SIGTERM, the interactive shell ignores it, and execSync keeps blocking. The daemon process exists (launchd shows it running, `daemon starting` was never logged because this runs before startDaemon), binds nothing, and logs nothing: precisely the symptom CLAUDE.md says gets misdiagnosed as a permissions/socket problem. No restart helps because every boot re-runs the same shell.

*Fix (small):* Resolve PATH with Bun.spawn plus a hard SIGKILL escalation (or Bun.spawn's `timeout`+`killSignal`), prefer `-lc` (non-interactive login: .zprofile/.zshenv) with `-ic` as a best-effort second probe, and log a warn when the probe is killed so the fallback PATH is visible. Alternatively run the probe after the socket binds and hot-swap process.env.PATH when it lands.
*Fixer notes:* Move the PATH probe off the synchronous boot-critical path. Preferred: replace execSync with Bun.spawn plus an explicit SIGTERM→SIGKILL escalation timer pair (mirror notifyFallback in lib/notifier.ts, per the repo memory rule "never sync-exec on the daemon thread"), and/or run the probe after the socket binds and hot-swap process.env.PATH when it lands so a hanging shell can never gate the socket. A plain timeout increase or killSignal:'SIGKILL' on execSync is NOT sufficient by itself: Bun still blocks the thread until the child actually dies, and SIGKILL to the interactive shell does not reap the grandchild that is actually hanging ... kill the whole process group (detached spawn, signal -pid), as the audit already did for process-manager pgroup reaping. Prefer `-lc` (non-interactive login, sources .zprofile/.zshenv) as the primary probe with `-ic` as a best-effort second attempt, and log a warn when a probe is killed so the fallback PATH is visible. Add a regression test around resolveUserPath's timeout path (user-path.test.ts currently exercises only probeTools) using a `trap '' TERM; sleep N` fixture child to assert the resolver returns within a bounded time and falls back to baseline PATH. Note there is a second execSync in the same file (user-path.ts:48-51, the NVM probe) with a 5s timeout and no killSignal ... smaller blast radius but the same class; fix both.

#### S015 · P1 · hazard · `lib/daemon/worktree-process-kill.ts:132`
**killWorktreeProcesses runs three execSync calls (lsof over all processes) on the daemon thread, including the unattended reactor path**

lsof -d cwd walks every process on the machine; on a busy Mac (or one with a hung network mount, which lsof stats) it takes seconds to tens of seconds. During disposeTree/autoReturnMain the daemon loop is blocked for up to 10s + 5s + 5s; all IPC requests time out and the tray flickers red. Bun's execSync timeout only sends SIGTERM and then waits, so an lsof stuck in a stat on a dead mount wedges the daemon outright (the MAT-222 shape). The reactor triggers this without any user action.

*Fix (small):* Port the three spawns to runCapture (async) and make killWorktreeProcesses async; the callers already await around it. Use `lsof -d cwd -Fpn -p <pids-of-interest>` or restrict with `+D` only after an async pre-filter to bound the walk.
*Fixer notes:* Port `killWorktreeProcesses` to async using `runCapture` from `lib/subprocess.ts` ... `lib/daemon/system-process-scanner.ts:225-235` already runs the identical `lsof -d cwd -Fpn` call that way, so the pattern to copy already exists in this repo. Both call sites (`lib/worktree/dispose.ts:238` and `lib/daemon/worktree-reconciler.ts:447`) are already inside `async` functions that `await` other work, so making the callee async requires no shape change beyond adding `await`. Since Bun's execSync/spawnSync timeout does not escalate to SIGKILL (verified on 1.3.13), any replacement that still wants a hard bound needs its own SIGTERM→SIGKILL timer pair around an async spawn (mirror `notifyFallback` in `lib/notifier.ts`), not a bare `timeout` option. Consider also reusing the scan `system-process-scanner.ts`'s own poller already performs on `SYSTEM_PROCESS_SCAN_INTERVAL_MS` instead of re-shelling a second independent lsof walk. Add a regression test near `lib/daemon/__tests__/worktree-process-kill.test.ts` (or a repo-wide grep-based lint like `lib/__tests__/no-eager-tui.test.ts`) asserting no `execSync`/`spawnSync` import remains in this file, so the fix doesn't silently regress the way it did after the 2026-07-17 tray-flicker fix.

#### S016 · P1 · bug · `lib/daemon/worktree-process-kill.ts:132`
**Worktree process killer runs system-wide lsof via execSync on the daemon thread per disposal**

Every auto-dispose (merge reactor), pool shrink, `rt worktree dispose`, `worktree:adopt`, and auto-return of main runs a full-system `lsof -d cwd` synchronously on the daemon's event loop. On a developer machine with hundreds of processes, open network mounts, or Docker, lsof routinely takes 1 to 10 seconds; during that window every socket/REST/WS request stalls (tray goes red, chat posts and events:wait deliveries are delayed). Per the project's own MAT-222 finding, Bun's sync-exec `timeout` only SIGTERMs and then waits, so a stuck lsof holds the loop indefinitely. system-process-scanner.ts:225-238 already implements the identical scan asynchronously via runCapture, so the sync version is purely a leftover.

*Fix (small):* Make killWorktreeProcesses async and reuse getAllRepoPids/runCapture from system-process-scanner.ts (or accept the scanner's last cwd map plus a targeted `lsof -a -p`), then await it in dispose.ts and the reconciler.
*Fixer notes:* Suggested fix in the finding is sound and low-risk: make `killWorktreeProcesses` async, replace the three `execSync` calls with `runCapture` (lib/subprocess.ts, already the pattern `getAllRepoPids`/`getPackageScripts` use in system-process-scanner.ts:213-239), and `await` it at both call sites (dispose.ts:238, worktree-reconciler.ts:447). `disposeTree` is already `async` so the dispose.ts call site needs no restructuring beyond adding `await`; `autoReturnMain` is also already `async`. Parsing logic (`parseLsofCwdMap`, `parsePackageScripts`) is already shared and untouched. Worth a regression test asserting `killWorktreeProcesses`/its async replacement never calls `child_process.execSync` (grep-based unit test, mirroring the existing "no execSync under lib/daemon" convention referenced in docs/superpowers/plans/2026-08-26-rt-chat-invite-1-rt.md:16). Also worth checking whether other call sites of `execSync` still exist under lib/daemon/ beyond this file (a couple of live is a partial grep result in this session, not exhaustively audited here) ... this may not be the only violation of that convention.
*Partial mitigation today:* Partial only, not a real mitigation: each execSync call is wrapped in try/catch (worktree-process-kill.ts:135-138, :164-167, :182) so a *fast* nonzero exit or a timeout that Bun actually enforces degrades to an empty result with a warn log. This does not cover the documented Bun gotcha where a SIGTERM-immune/D-state child causes the "timeout" to never fire at all ... the call simply never returns, so the catch block is never reached. The worktree-reconciler.ts:444 comment acknowledges this is deliberately sync but adds no technical guard (no isolation, no hard kill, no fallback timer).

#### S017 · P1 · bug · `lib/daemon/worktree-process-kill.ts:144`
**autoReturnMain kills every process in every sibling worktree under the main clone**

Repo uses the default root /.worktrees. The user's MR from the main clone merges; the reactor runs autoReturnMain(main) with killProcesses on. lsof cwd matching keeps every pid whose cwd starts with /, which includes /.worktrees/amber/... where an agent is running a dev server and a test suite for an unrelated ticket. Those processes get SIGTERM then SIGKILL 5s later. The kill is unattended and logged only in the daemon log.

*Fix (small):* When the target is a main clone, exclude cwds under any registered ephemeral tree path (or under cfg.root) before selecting targets; generally match against the set of registry paths and pick the LONGEST matching prefix so a process is attributed to exactly one tree.
*Fixer notes:* Fix is on the discovery side, not selection: when the kill target contains other registered trees (the main clone with a nested .worktrees root), attribute each candidate cwd to exactly one tree by LONGEST matching registry-path prefix and exclude any cwd owned by a different registered tree ... or simply drop cwds under cfg.root / under any other registry path before selecting. Only the autoReturnMain(main-clone) path needs this; disposeTree's ephemeral (leaf) paths are unaffected, so scope the change carefully. Add a killWorktreeProcesses/parseLsofCwdMap-level test (there is currently none ... the existing test file only covers selectKillTargets): fixture with a main-clone cwd plus a nested /repo/.worktrees//... cwd, assert the nested pid is excluded when target is the main clone and included when target is that ephemeral tree. Secondary amplifier to note: the kill at worktree-reconciler.ts:447 runs BEFORE the status/stash/checkout, and every mechanical failure after it returns "retry" (lines 458,465,472,478) which re-arms the edge and re-runs autoReturnMain ... so the erroneous kill can re-fire on each subsequent reactor pass until the return finally succeeds. Blast radius: only autoReturnMain; no schema/registry migration involved.
*Partial mitigation today:* Partial and incidental only: agentSessionPids (worktree-process-kill.ts:64) spares a sibling tree's own claude/codex process and its direct descendants, so an agent process itself is not killed. This does NOT cover workload launched in separate herdr panes / shells (zsh spared at line 106, but its node/bun children are not), which is the normal way dev servers and watch-mode test suites run. disposeTree's caller (dispose.ts:238) is safe because it is guarded to kind==="ephemeral" (dispose.ts:199), whose paths are leaves; the nesting hazard is unique to the main-clone target in autoReturnMain.

#### S018 · P1 · hazard · `lib/daemon/worktree-process-kill.ts:169`
**Process kill is a blocklist: it SIGTERMs the calling rt CLI, tmux servers, and editors with cwd in the tree**

User runs `cd .worktrees/amber && rt worktree dispose amber --force`. The daemon's lsof pass finds the `rt` CLI process (comm "rt", not a shell, not an agent, cwd inside the tree) and SIGTERMs it before the IPC reply is written; the user sees 'Terminated' and no result. Same for a tmux server started in the tree, nvim/vim/less sessions, ssh, docker compose: all SIGKILLed 5s later. On the reactor's auto path this fires unattended when an MR merges. The `protectedPids` comment says 'e.g. the calling CLI process' but the IPC payload never carries the caller pid.

*Fix (small):* Carry the caller pid in the dispose/provision payload and add it (with descendants) to protectedPids; switch the selector to an allowlist of workload signatures (interpreters and package-manager scripts) or at minimum add multiplexers/editors to the spared set.
*Fixer notes:* Two independent fixes needed, matching the two call sites. (1) Interactive dispose: thread the caller's pid through the IPC payload (commands/worktree.ts builds payload -> add `callerPid: process.pid`), plumb it into DisposeDeps/disposeDeps() and from there into killWorktreeProcesses' protectedPids, since the daemon has no other way to learn it. (2) Unattended reactor path (worktree-reconciler.ts autoReturnMain and the merge-driven disposeTree call): there is no 'caller' to protect there, so the real fix is changing selectKillTargets from a blocklist to an allowlist (interpreters/package-manager scripts recognized as workload) or at minimum adding multiplexers (tmux, screen), remote shells (ssh, mosh), and common terminal editors/pagers (vim, nvim, emacs, less, man) to a spared-binaries set alongside SHELL_BINS. Add a test asserting killWorktreeProcesses itself (not just selectKillTargets in isolation) protects a caller pid passed end-to-end, since the current test suite only exercises selectKillTargets with a hand-supplied pid and would not have caught this. Also worth logging (at warn, not just info) the full list of non-package-script-labeled targets before sending SIGTERM, since today's label lookup silently falls back to bare comm for anything ps eww can't attribute to npm_lifecycle_event.
*Partial mitigation today:* None that scopes the hazard. The only lever is the global `killProcesses` boolean in lib/worktree/config.ts (default true, no dedicated CLI surface -- would have to be set through the generic settings store), which disables the entire feature rather than protecting specific processes. No cwd-based self-exclusion, no caller-pid threading anywhere in the IPC payload or DisposeDeps.

#### S019 · P1 · bug · `lib/daemon/worktree-reconciler.ts:737`
**Freshen pops stash@{0} after a failed stash push, applying and dropping the user's unrelated stash**

Idle main is a freshen candidate; the user has an older stash@{0} ('WIP on feature-x'). Between candidacy and freshenOne's re-classify the user edits a file, so blockers > 0. `git stash push -u` fails or is SIGTERMed at the 60s default timeout on a large tree (large untracked set), so no `` marker stash exists; findDesktopStashAsync returns null and stashName falls back to "stash@{0}". After the ff, `git stash pop stash@{0}` applies the feature-x stash onto main and removes it from the stash list. The user's main checkout now silently carries unrelated changes and their stash is gone; the only trace is a daemon debug line.

*Fix (small):* Make stashChangesAsync return the exit code; only set stashName when the push succeeded AND findDesktopStashAsync locates the marker; never fall back to a positional stash ref. If the push fails, abort the freshen with fail() rather than proceeding to the ff.
*Fixer notes:* Two coupled fixes in the freshen path. (1) Make stashChangesAsync return the push GitResult/exit code (currently void, git-async.ts:177-180) so callers can branch on it. (2) In freshenOne (worktree-reconciler.ts:734-743) set stashName only from the resolved findDesktopStashAsync marker name AND only when the push exit code was 0; NEVER fall back to a positional stash@{0} ref. If the push failed, do not proceed to ff+pop: call fail() and return false, leaving the user dirt and stash untouched, mirroring autoReturnMain (460-466) which re-checks git status after the stash and returns retry if the tree did not clear. Consider re-verifying the worktree is clean after the push before the ff, as autoReturnMain does. Blast radius is limited to the freshen path; autoReturnMain stashes-and-leaves (never pops) and is already safe, do not change it. Tests: (a) push fails with a pre-existing non-Desktop stash@{0} present, assert no stash pop issued and the pre-existing stash survives; (b) push succeeds, assert pop targets the resolved Desktop name, not a positional ref; (c) grep-guard that the stash@{0} fallback is removed. Note autoReturnMain stash call (460) has the same swallowed exit code but is defended by its post-stash status re-check, so no positional-pop hazard there.
*Partial mitigation today:* Partial only: findDesktopStashAsync (lib/worktree/git-async.ts:190-201) resolves the correct stash name whenever the push SUCCEEDED, so the positional fallback is bypassed in the common success case. There is no mitigation in the push-failure case: stashChangesAsync (git-async.ts:177-180) swallows the exit code and freshenOne (worktree-reconciler.ts:745-763) pops unconditionally without re-verifying the tree cleared.

#### S020 · P1 · hazard · `lib/dev-mode.ts:76`
**Any foreign #! script at ~/.local/bin/rt parks the prod daemon permanently**

A new user has any script named `rt` in ~/.local/bin from another tool (pipx/uv/npm all install console scripts there; 'rt' is a short, contended name). mattstack.mode is unset on a fresh machine, so resolveIntendedMode() derives 'dev' from the shebang; the prod daemon logs one info line 'parked: this machine's intended mode is dev' and sleeps in a 30 s loop forever, binding nothing. path.link refuses to replace the file ('dev-mode-owns-rt'), so the foreign script stays, `rt` on their PATH is not mattstack's, and the setup verify row says 'intended dev (derived-from-wrapper) · cli dev · daemon n/a' which passes. The tray shows the daemon down with the remedy 'rt settings dev-mode prod', which is TTY-gated and runs the dev handoff.

*Fix (small):* Make the wrapper signal specific: detect dev mode only when the script carries a mattstack marker line (the way LINK_TAG identifies tagged wrappers), treat any other #! file as 'occupied by a foreign tool' in path.link/verify, and have the park loop refuse to park on a derived (not explicitly set) intent when no dev bundle is installed.
*Fixer notes:* Fix must address three seams, not one. (1) Make the dev signal specific: currentMode()/isDevModeWrapper should recognize dev mode only by a mattstack marker line (the LINK_TAG pattern already used for tagged wrappers, links.ts:59), and links.ts should classify a foreign "#!" file as `occupied` (replaceable with force) rather than `dev-mode-owns-rt` (permanently refused). Note currentMode() lives in lib/dev-mode.ts and is depended on by uninstall.ts, services.ts, rt-health.ts:122, and links.ts:46-50 ... changing its detection changes all of them, so audit each. (2) parkUntilIntended (park.ts) should refuse to park a prod daemon on a DERIVED (provenance "derived-from-wrapper") "dev" intent when no dev bundle is actually installed ... a derived dev intent with no dev bundle present is a false signal, not a real toggle. (3) flavorRow (rt-health.ts:374-375) turning daemon-n/a into a hard "ready" is what makes this silent; it needs to stay green for CI's daemon-less clean room yet not mask a prod machine whose intended flavor derived to dev with no dev bundle ... gate the false-green on provenance and bundle presence. Add regression tests mirroring lib/setup/__tests__/validators-rt-health.test.ts (writeDevWrapper) and a park test asserting a prod daemon does NOT park on a derived-dev intent with no dev bundle. Blast radius of the currentMode change is estate-wide (dev/prod toggle correctness), so exercise commands/settings.ts describeTuple/toggleDevMode too.
*Partial mitigation today:* Partial only, and none prevents the failure: parkUntilIntended re-reads intent every 30s (park.ts:44-61), so a user who knows to run the BUNDLED rt directly and `rt settings set mattstack.mode prod` (machine scope) will converge the daemon on the next cycle ... but nothing writes that setting on install, the `rt` on PATH is the foreign tool, and the setup verify is green (rt-health.ts:374-375), so an affected non-author user has no signal pointing them there.

#### S021 · P1 · hazard · `lib/git-worktrees.ts:57`
**git-worktrees.ts runs execSync with no timeout on the daemon's 5-minute tick and the `repos` verb**

A tracked repo lives on a network mount that stalls, or git blocks on a stale lock: execSync has no timeout, so the daemon event loop freezes for the duration on every cache refresh (per repo, twice per tick) and on every `repos` command from the tray. Every IPC/REST request times out, the tray goes red, and per the repo's own MAT-222 note a Bun sync exec cannot be interrupted. Even the fast case is a per-repo synchronous spawn on the loop, which the project rule 'daemon timers must use async runCapture, never execSync' forbids.

*Fix (small):* Replace both daemon call sites with listWorktreesAsync from lib/worktree/git-async.ts (already has the 60s timeout and null-on-failure), and delete or CLI-only the execSync helpers.
*Fixer notes:* Suggested fix is directionally correct and the replacement already exists and is tested: swap cache-refresh.ts:112/:209 and status.ts:107 to `listWorktreesAsync` from lib/worktree/git-async.ts. Note the shape differences a fixer must handle:
- `listWorktreesAsync` returns `WorktreeEntry[] | null` (null on nonzero exit), branch is `string | null`; the sync versions return `[]` on error and branch is `string` (empty for detached). Callers filter on `w.branch` truthiness in both status.ts:107 and cache-refresh.ts:113, so `null` branch is compatible with those existing `.filter((w) => w.branch)` checks, but a `null` return (vs `[]`) needs an explicit `?? []` at each call site to preserve current empty-array-on-failure behavior.
- cache-refresh.ts also has two more unguarded execSync calls in the same loop that are the identical hazard but out of scope for this specific file:line ... line 119 (`git for-each-ref`) and line 139 (`git config --get remote.origin.url`). Worth flagging to whoever fixes this, since fixing only git-worktrees.ts call sites leaves the same freeze reachable two lines later in the same function.
- Once switched, delete or mark git-worktrees.ts CLI-only (it's still legitimately used as the read-only fallback in worktree-each.ts's design per its own doc comment) ... don't delete outright, just stop importing it from daemon code.

#### S022 · P1 · bug · `lib/notifier.ts:384`
**Poll-only users never get MR notifications: userId is resolved only on the live-watch path**

A user tracks every repo with `mode: "poll"` (or the team store grants poll). `ensureUserId()` is only ever called inside the live-only loop of reconcileFreshnessImpl (and from resolveSelfUsername, which project-sync calls only when a demand exists). `getCurrentUserId()` therefore stays null forever, `checkAndNotify` passes null, `isSelfAuthored` returns false for every entry, and `detectBranchTransitions` `continue`s before any transition check. Pipeline failed / MR approved / merged / conflicts notifications never fire, with no log line explaining why. The notifier's own doc calls this the deliberate "suppress" fallback, but here it is the steady state for anyone not on live mode.

*Fix (small):* Resolve the current user id independently of watch mode: in reconcileFreshnessImpl (or cache-refresh), build a provider for any repo with a `branches`/`project-mrs` grant and call ensureUserId once, regardless of `mode`. Log at warn once per boot when transitions are being suppressed because userId is unresolved.
*Fixer notes:* The fix must do BOTH: build a provider for a poll repo (today `ensureProvider` at freshness.ts:683 runs only for live repos, so `providers` is empty and `ensureUserId` early-returns null at freshness.ts:160) AND call `ensureUserId`. Resolving userId for any repo whose grant includes `branches` or `project-mrs`, regardless of mode, is the right shape. Ordering trap: cache-refresh.ts calls `checkAndNotify` at line 201 but `reconcileSubscriptions()` at line 238 (after it), so putting the resolution only inside reconcile leaves the FIRST poll cycle still passing null ... userId would land at the end of cycle 1 and cycles 2+ would work. To fix cycle 1, resolve userId in cache-refresh.ts before line 201 (or at daemon boot). `ensureUserId` is idempotent (userIdResolved guard) and already tolerant of a failed token validation, so calling it more broadly is low-risk; it touches the shared daemon singleton only. Add a regression test: an all-poll (branches-only) tracking config where a self-authored transition must produce a notification, asserting userId resolves and `checkAndNotify` fires (no such test exists ... notifier-fired-hygiene.test.ts is the only checkAndNotify test and doesn't cover this). Also worth adopting the finding's second suggestion: a once-per-boot warn when transitions are suppressed because userId is unresolved, so this class of failure is diagnosable from logs.

#### S023 · P1 · bug · `lib/subprocess.ts:60`
**runCapture's timeout is unenforceable: no SIGKILL escalation and a pipe-holding grandchild defeats it, reporting exitCode 0**

`lsof -d cwd -Fpn` (system-process-scanner.ts:231, every 10s) or `lsof -iTCP` (port-scanner.ts:223, every 30s) blocks in uninterruptible I/O on a stale SMB/NFS mount or a slow FUSE volume (lsof's classic failure mode; SIGTERM is not delivered while in D state). The 10s timer sends SIGTERM, nothing happens, `proc.exited` never resolves, and `processScanInFlight` / `portScanInFlight` (pollers.ts:41, 57) stay true for the life of the daemon: port and process scans are silently dead, the tray shows a frozen port list, and the only trace is the absence of a `debug`-level 'ports scanned' line. Separately, when the timeout does fire but the child exits 0 after being ignored (or a grandchild held the pipe), callers get `{stdout:"", exitCode:0}`: `scanListeningPorts` returns [] (line 227), `checkAndNotify` prunes every port snapshot, and `getAllRepoPids` returns an empty map so `scan` prunes every runaway-tracking window. The mirror copy in packages/rt-client/src/settings/exec.ts:50-60 (deriveRepoIdentity, called from every reconciler pass and the 60s hooks scan) has the identical defect.

*Fix (small):* Use Bun.spawn's own `timeout` + `killSignal: "SIGKILL"` options (or escalate SIGTERM -> SIGKILL after a grace period, as notifier.ts:270-272 already does), race the stdout read against the deadline instead of awaiting EOF unconditionally, and return a distinct `timedOut: true` / exitCode -1 so callers never mistake a kill for empty success. Mirror the change into packages/rt-client/src/settings/exec.ts per its header note.
*Fixer notes:* Fix lib/subprocess.ts and mirror byte-for-byte into packages/rt-client/src/settings/exec.ts (its header says "change there first, mirror here"), then rebuild rt-client (`bun run build` in packages/rt-client) or the dist-freshness test fails and file: consumers copy the stale bundle. Required changes: (a) race the stdout/stderr reads against the deadline instead of awaiting EOF unconditionally ... this is the load-critical part because it lets runCapture RESOLVE while a D-state or grandchild-held child is still stuck, which is what frees the in-flight guards; (b) SIGTERM->SIGKILL escalation modeled on notifier.ts:270-272 (helps SIGTERM-ignoring children that are still killable; will not help D-state, hence (a) is mandatory); (c) return a distinct sentinel on timeout (timedOut:true and/or exitCode -1/124) so callers stop mistaking a defeated call for empty success. Do NOT rely on Bun.spawn's own `timeout` option alone ... it kills the child but a grandchild holding the pipe still blocks the read, so (a) is still required. Blast-radius note for the fixer: changing the timeout return contract to exitCode 124/-1 touches EVERY runCapture caller (grep shows ~20 call sites across lib/ and rt-client); audit callers that branch on `exitCode !== 0 && !stdout` (system-process-scanner.ts:234) and `exitCode === -1` (home-snapshot.ts:361, runs/liveness.ts:56, materialize.ts:97) so a new timeout code is handled deliberately, not silently folded into an existing branch. Tests to add: (1) a spawn that backgrounds a pipe-holding grandchild then exits 0, asserting runCapture returns within ~timeoutMs (not the grandchild's lifetime) with a timeout sentinel; (2) a SIGTERM-ignoring child (`trap "" TERM; sleep N`) asserting SIGKILL escalation bounds the call; (3) a pollers-level test that a hung scan does NOT permanently latch portScanInFlight/processScanInFlight. Also consider a poller-side belt-and-suspenders (Promise.race against a wall-clock cap that force-resets the in-flight flag) so no single runCapture regression can silently kill scans again.

#### S024 · P1 · hazard · `lib/subprocess.ts:60`
**runCapture timeout leaves stdout pending when a grandchild survives; one stuck ready step wedges the reconciler forever**

Reproduced in the scratchpad: Bun.spawn(["/bin/zsh","-lc","sleep 20 & ..."]) with stdout piped, proc.kill() after 1s: direct child exits 143 but new Response(proc.stdout).text() is still pending at 6s because the grandchild inherited the pipe. In production: a freshen or replenish runs `pnpm install` whose postinstall hangs; at 15 min zsh/pnpm is SIGTERMed, the child node process survives holding the pipe, runReadySteps never resolves, runOnce never resolves, `inFlight` stays set so every future kick() returns early, withReconcilerHeld (repos:locate) blocks forever, and the tree lock taken by freshenRepo/createTree is never released. No log line is emitted; the pool silently stops replenishing and freshening until a daemon restart. The same shape applies to the provision handler's post-claim ready steps (that CLI call times out but the daemon-side lock is held for good).

*Fix (small):* Spawn ready steps (and git) with detached:true and on timeout kill the process group (process.kill(-pid)); additionally race the stdout/stderr reads against the timeout so runCapture always settles with exitCode -1 even if a descendant keeps the pipe open.
*Fixer notes:* Fix centrally in lib/subprocess.ts (blast radius: every runCapture caller, including runGit): (1) spawn with detached:true so the child is a pgroup leader, and on timeout send the signal to the group via process.kill(-proc.pid, SIGTERM) then SIGKILL ... the exact pattern already implemented as killGroup(pid, signal) in the deprecated lib/daemon/process-manager.ts (guards pid<=1 and ESRCH), reuse it; (2) additionally race the stdout/stderr reads against a hard deadline so runCapture always settles with exitCode -1 even if a descendant keeps a pipe open (belt-and-suspenders: (1) alone still hangs if a truly detached/reparented process survives SIGKILL of the group). Preserve runCapture's contract: never throws, returns {stdout,stderr,exitCode:-1} on timeout/failure, same shape callers already branch on. Verify detached:true does not regress cwd/env handling or the `stdin:"ignore"` path. Add a regression test spawning a backgrounder (mirror process-manager.test.ts's "kill reaps grandchildren (detached pgroup)") asserting runCapture settles within the timeout and the descendant pid is gone. Consider a top-level watchdog around runOnce as defense in depth, but the runCapture fix is the root cause. Note docs/daemon-runner-health.md is stale (it audits the removed process-manager subsystem) and should not be trusted as current coverage.

#### S025 · P1 · hazard · `lib/worktree/registry.ts:83`
**Registry writes use the cache-flavoured persistOrWarn: a dropped write is permanent and the next pass acts destructively on it**

state.db is WAL and shared with CLI processes; the daemon connection's busy_timeout is 250ms. createTree finishes a 10-minute install and calls saveRegistry(finalTrees) to flip the row from `creating` to `on-deck`; a CLI write transaction holds the lock >250ms; setKvValue swallows SQLITE_BUSY with a warn and returns. The row stays `creating`, the lock is released, and the next reconcile pass (<=5 min) sees an unlocked `creating` row and scrapTree()s the finished tree (rename to .trash-*, detached rm -rf, branch -D). Same mechanism on the provision claim: a dropped `state: claimed` write leaves the user's tree `on-deck`, so freshen ff-merges/installs in it, shrink can dispose it (if clean and pushed), and the next provision hands it to someone else and checks out a different branch under the user. Nothing regenerates this state: kind/state/owner exist only in this row.

*Fix (small):* Route saveRegistry (and saveClaims) through runCriticalWrite, and make saveRegistry return/throw on failure so callers on destructive paths (createTree final flip, provision claim, disposeTree registry filter) abort instead of proceeding; do not bump the epoch on a failed write.
*Fixer notes:* Route saveRegistry (registry.ts:83) and saveClaims (endpoint/store.ts:77) through runCriticalWrite (busy.ts:87) instead of persistOrWarn, and make them return a success boolean (or throw) so destructive callers can abort. Callers to gate on failure: createTree final flip (create.ts:147 ... on failed persist, do NOT return {ok:true}; either retry or leave the row locked/creating rather than releasing into an unlocked creating state), the provision claim write (handlers/worktree.ts:349-354 ... abort the provision and roll back rather than proceeding to checkout), disposeTree's registry filter (dispose.ts:273), and rollbackClaim/patchTree writes on the destructive paths (handlers/worktree.ts:95-101, worktree-reconciler.ts:348-354). Do NOT bump the epoch (registry.ts:84) on a failed write ... an epoch bump on a dropped write both fabricates a false conflict for concurrent reconcile passes and misrepresents that data changed. Note runCriticalWrite still returns undefined after exhausting its 3x20ms budget, so returning a boolean and having callers refuse to advance state on false is the real fix, not just swapping the busy policy. Blast radius of the change: saveRegistry/saveClaims are the single seam every registry/claim write funnels through, so making them fallible touches every caller's error handling ... reconcile's idempotent step-(c) save (worktree-reconciler.ts:250) can keep deferring (it self-heals next pass), so consider a separate deferrable-vs-critical entry point rather than making all callers abort. Add a test that injects SQLITE_BUSY on the flip/claim write and asserts the completed tree is not scrapped and the claimed tree is not re-handed (the onAfterLoad seam at worktree-reconciler.ts:80 and a busy-injecting db stub give the hook).
*Partial mitigation today:* Partial, none closing the core window: (a) the per-tree lock (lib/worktree/locks.ts withTreeLock) serializes operations on one tree but is released before the reconcile scrap runs, so it does not cover the drop-then-scrap window; (b) worktree:create's caller path re-writes state->claimed via patchTree (lib/daemon/handlers/worktree.ts:480-483), so that specific path needs TWO consecutive dropped writes to fail ... but replenish/--on-deck and the provision-claim path each need only ONE; (c) provision's isClaimable rejects a 'creating' row (handlers/worktree.ts:338) so provision returns 'busy' rather than compounding, but the completed tree is still scrapped by reconcile. reconcile's epoch conflict check (worktree-reconciler.ts:249) does not help ... it detects concurrent snapshot writers, not a dropped write, and reconcile's own writes self-heal only because they are idempotent from git ground truth, which the non-idempotent create-flip/provision-claim writes are not.

#### S026 · P1 · bug · `rt-tray/Sources/AppDelegate.swift:810`
**Tray health dot sticks at yellow 'Daemon: starting…' forever when the daemon never comes up after launch**

Fresh user opens mattstack.app; the daemon is crash-looping (finding 1), parked on a flavor mismatch, or sitting in Login Items 'requiresApproval'. Health is set to .starting at line 133; every subsequent 10s poll fails, consecutiveStatusFailures climbs, but the guard's `currentHealth != .starting` clause returns early forever. The menu bar shows a yellow dot and the panel says 'Daemon: starting…' for hours; needsApproval is never evaluated so the approval row never appears; TrayLog never records 'daemon unreachable, marking down'. The only exits from .starting are a successful poll or the user clicking Restart/Stop.

*Fix (trivial):* Record `startingSince` when entering .starting; in refreshStatus treat `.starting` as expired after ~30s (or 3 failed polls) and fall through to setHealth(.down), which also computes needsApproval. Log the transition with the SMAppService status so the tray log says why.
*Fixer notes:* Root cause: the launch path (startNormalOperation Task, lines 132-156) sets .starting with no bounded transition to .down, while the periodic poller (refreshStatus line 810) intentionally refuses to demote from .starting. Fix by giving .starting an expiry: record a startingSince timestamp (or a startFailures counter distinct from consecutiveStatusFailures) when entering .starting, and in refreshStatus treat .starting as expired after ~30s / 3 failed polls, falling through to setHealth(.down) ... which already recomputes needsApproval (line 590) and drives the approval row. Keep the transient-stall protection for the healthy→down case intact; only the .starting window needs the timeout. Note restartDaemon (lines 605-621) already models the bounded pattern (16 polls then setHealth(.down)); the launch Task should adopt the same fallback rather than relying on the poller. Log the transition with daemonLifecycle.status / SMAppService status so the tray log records why (currently line 812 warn is unreachable in this state). Blast radius is tray-only (no daemon/state changes). Tests: rt-tray has no XCTest harness for AppDelegate health today, so verification is by inspection of the guard logic; if adding coverage, extract the .starting-expiry decision into a pure function keyed on (currentHealth, consecutiveFailures, elapsed) and unit-test it.
*Partial mitigation today:* Partial and non-covering: the `currentHealth != .starting` clause at line 810 is a deliberate transient-stall guard (comment lines 807-809), and restartDaemon (lines 605-621) does bound its own .starting window with a 16-iteration poll loop that falls through to setHealth(.down) at line 619. Neither guards the LAUNCH path ... startNormalOperation's Task (lines 132-156) sets .starting and hands off to the periodic poller with no bounded fallback to .down, so nothing rescues the launch scenario.

#### S027 · P2 · bug · `commands/daemon.ts:226`
**`rt daemon uninstall` deletes rt.sock and rt.pid even when it just said the daemon may still be running**

Tray not running (quit, or the other flavor's tray owns tray.sock so the POST goes nowhere) but the daemon is up under launchd. `rt daemon uninstall` prints that the daemon may still be registered, then unlinks the live daemon's socket and pid file and clears daemon.json. The daemon keeps running with launchd supervision, now unreachable by every client (socket file gone), unevictable by the next boot (pid file gone), and with `isDaemonInstalled()` false so every CLI falls back silently. A subsequent `rt daemon install` waits 3s for a socket that will never reappear and reports 'daemon not yet responding'. The mattstack.app 'Stop' path does the unregister correctly; this CLI path is the one non-author users will reach for from a terminal.

*Fix (small):* Only clean sock/pid when the daemon is verified gone (probeSocketHolder() returns null AND the pid is not alive); otherwise print the real remedy (`rt daemon stop` via the tray, or `launchctl bootout gui/$UID/<activeLaunchdLabel()>`) and leave the files alone. Consider having uninstall itself run the bootout when the tray is unreachable, as the dev-mode toggle already does (commands/settings.ts:657).
*Fixer notes:* Fix should live in commands/daemon.ts's `uninstall()` (around line 212-228): after a failed/absent `trayQuery("/daemon/stop")`, call `probeSocketHolder()` (lib/daemon/park.ts, already imported in this file) before touching files. Only run `markDaemonUninstalled()` + `cleanupDaemonFiles()` when the probe returns null (or an old-flavor pid that `process.kill(pid, 0)` shows dead). When a holder is still live, skip the file cleanup and print the real remedy: `launchctl bootout gui/$UID/<activeLaunchdLabel()>` (activeLaunchdLabel() already exists in lib/daemon-config.ts:61-63) ... mirroring the fallback commands/settings.ts:653-655 already uses for the same tray-unreachable case. Also worth checking commands/settings.ts's dev-mode toggle path and any other caller of `cleanupDaemonFiles()`/`markDaemonUninstalled()` for the same missing-guard pattern before calling this fully fixed. A regression test should stub `trayQuery` to fail and `probeSocketHolder` to return a live holder, then assert rt.sock/rt.pid/daemon.json are left untouched.

#### S028 · P2 · bug · `commands/daemon.ts:251`
**`rt daemon start` and daemonQuery's auto-restart cannot start a registered daemon that exited 0: SMAppService.register is a no-op when already registered**

Every daemon exit that launchd did not itself initiate and that ends with status 0 leaves the agent registered but not running, and KeepAlive={SuccessfulExit:false} means launchd will not respawn it: the `shutdown` IPC/REST verb (daemon.ts:358 `process.exit(0)`), any SIGTERM/SIGINT/SIGHUP from outside launchd (`pkill rt`, Activity Monitor Quit, a same-flavor `evictStaleDaemon`, a stray `kill` in a script; shutdown.ts:66 `process.exit(0)`). The user then runs `rt daemon start`: the tray's /daemon/start calls `SMAppService.register()`, which throws kSMErrorAlreadyRegistered, is reported ok, and spawns nothing; after 3s the CLI prints 'daemon starting… check logs' and the logs show nothing new. `daemonQuery`'s step-4 auto-restart (daemon-client.ts:232) takes the same route, so every CLI command warns 'installed but not running. Run: rt daemon start', which does not work. Only `rt daemon restart` (kickstart -k) or a logout/login (RunAtLoad) brings it back, and nothing tells the user that. The tray's own restart menu item does kickstart, so the author's habit path works while the documented CLI path does not.

*Fix (small):* In DaemonLifecycle.startDaemon (or /daemon/start), after register returns 'already registered', run `launchctl kickstart gui//` (without -k) when the daemon socket is unreachable; make `rt daemon start` fall back to the restart path when the tray reports status enabled and the socket stays absent. Separately reconsider exit codes (see the signal-handler finding).
*Fixer notes:* Correct lever is a kickstart fallback, not exit codes. In DaemonLifecycle.startDaemon / TrayServer /daemon/start, when register() returns already-registered (status .enabled) but the socket stays unreachable, run `launchctl kickstart gui//` (Kickstart.arguments already exists at ServicePlists.swift:70-74). Complementarily, commands/daemon.ts start() should fall back to the /daemon/restart route when the tray ack'd but isDaemonRunning() stays false through the 12×250ms loop, and lib/daemon-client.ts:180-181 attemptRestart must not treat a non-null tray reply as proof of spawn ... re-probe liveness before returning true, else the daemonQuery nag keeps misdirecting. Do NOT "fix" this by making the signal handlers exit non-zero: KeepAlive={SuccessfulExit:false} would then respawn on EVERY stop, including intended ones (/api/shutdown, tray-initiated). Blast radius: Swift tray change requires an app-bundle rebuild/re-sign (see CLAUDE.md operating rules ... build into scratch, never re-sign the blessed bundle); the TS-only CLI-side fallback (start()→restart fallback + attemptRestart liveness re-probe) recovers most cases without touching the bundle. Note docs/daemon-runner-health.md is stale ... it audits the removed process-manager/remedy-engine subsystem and does not cover this supervision path; add a test asserting start() escalates to kickstart when status is enabled but the socket is absent.
*Partial mitigation today:* rt-tray/Sources-core/Services/ServicePlists.swift:72 (kickstart -k) via commands/daemon.ts:296-315 → DaemonLifecycle.swift:74-88 ... `rt daemon restart` recovers the stuck state; commands/daemon.ts:271-294 → DaemonLifecycle.swift:60-67 ... `rt daemon stop` unregisters, so the documented stop→start cycle works; lib/daemon-logger.ts:239 ... crashes exit(1), which KeepAlive respawns. None of these are surfaced by the failing `rt daemon start` / daemonQuery nag.

#### S029 · P2 · bug · `commands/daemon.ts:727`
**daemon-stderr.log and tray-crash.log are never rotated or pruned, and stale contents are shown as 'most recent crash'**

Every dev-shim stand-down message, bun warning, and native panic appends forever; nothing truncates or rotates the file and the janitor's pattern excludes it. Months later `rt daemon logs` opens with a red 'native stderr … most recent crash leads the output' block showing a panic from a version that no longer exists, and the file has grown without bound on a machine where the daily JSON logs are dutifully pruned at 14 days.

*Fix (small):* In redirectNativeStderr, if daemon-stderr.log is non-empty rename it to daemon-stderr.<YYYY-MM-DD>..log (matches the janitor pattern) before opening fresh; do the same for tray-crash.log in installTrayCrashHandlers. In showLogs print the block only when the file's mtime is newer than the current daemon's startedAt, and label it with the mtime.
*Fixer notes:* Fix belongs in three places, matching the finding's suggested_fix: (1) redirectNativeStderr in lib/daemon-logger.ts:204-219 should rotate-on-open (rename to a dated name matching LOG_FILE_PATTERN, e.g. daemon-stderr.<yyyy-MM-dd>..log, if the existing file is non-empty) before calling openSync 'a' again -- this also brings it under the existing pruneLogs sweep for free once it has a dated name. (2) The dev-shim's freopen at rt-tray/Sources-daemon-shim/main.swift:239 has the identical gap and needs the same treatment (the finding's evidence only names redirectNativeStderr/TrayLog but the shim is a third writer to the same unbounded file, worth flagging to whoever fixes this). (3) installTrayCrashHandlers in TrayLog.swift:143-152 should do the same rotate-if-nonempty before its open() call. (4) commands/daemon.ts's showLogs should gate the native-stderr block on the file's mtime being newer than the current daemon process's start time and print that mtime in the header so a stale block is at least labeled, not just trusted. No test currently exercises rotation of these two files -- lib/__tests__/log-janitor.test.ts only covers the dated-file case -- so a regression test for the new rotate-on-open behavior belongs alongside whichever function implements it. P2 fits: real unbounded disk growth plus a genuinely misleading crash-diagnosis UX, but not data loss or daemon-down severity.

#### S030 · P2 · hazard · `lib/daemon-config.ts:133`
**`rt daemon uninstall` with the tray unreachable deletes rt.sock and rt.pid from under a live daemon, leaving an unevictable orphan on :9401**

Tray is not running; user runs `rt daemon uninstall`. The SMAppService agent is not unregistered (needs the tray), but rt.sock and rt.pid are unlinked while the daemon is alive. The daemon keeps running pollers, cron, and the home-snapshot auto-committer, now invisible to every client (no socket file). At next login or `rt daemon install`, launchd starts a second daemon: evictStaleDaemon finds no pid file and returns, the socket server binds a fresh rt.sock, then startApiServer throws EADDRINUSE because the orphan still holds :9401, and the new daemon crash-loops (see the port finding) until the orphan dies. The CLAUDE.md operating rule about a squatting daemon that "starts, binds nothing, logs nothing" describes this outcome.

*Fix (small):* In uninstall, if the tray is unreachable, probe rt.sock first (probeSocketHolder) and SIGTERM the answering pid via rt.pid before unlinking; never unlink runtime files while a holder answers. In startDaemon, when rt.pid is absent, probe the socket and, if a same-flavor holder answers, evict by its reported pid.
*Fixer notes:* Minimal fix: in `uninstall()` (commands/daemon.ts:225-227), gate `cleanupDaemonFiles()` (and ideally `markDaemonUninstalled()`) on `isDaemonProcessRunning()` (already exported from lib/daemon-config.ts, just unused here) being false; if it's true, either SIGTERM the pid before unlinking or skip the unlink and tell the user the daemon is still alive so files were left in place for the next start to find and evict. `probeSocketHolder` (lib/daemon/park.ts, already imported into commands/daemon.ts and used elsewhere) is a second, independent signal worth checking too ... the pid file could itself be stale/wrong in other edge cases, so probing the actual socket holder before trusting/deleting the pid file is more robust than the pid file alone. Defense in depth on the other end: `startApiServer` (lib/daemon/api-server.ts) should catch the `Bun.serve` throw and log a clear "port already in use, is another daemon running?" diagnostic rather than falling through to the generic uncaughtException path ... that would at least make the eventual crash loop's cause legible in the logs instead of a bare EADDRINUSE stack. Tests to add: a boot-reconcile test for "pid file missing but a process holds the socket/port" (currently untested ... the only test path implied by the code comments is the pid-file-present case), and a commands/daemon uninstall test asserting `cleanupDaemonFiles` is not called when `isDaemonProcessRunning()` is true and tray was unreachable.

#### S031 · P2 · hazard · `lib/daemon-logger.ts:49`
**No size cap or suppression on the daily log; a warn storm from a polling client fills the disk and triggers the logging crash**

A console tab polls `chat:read` for an archived room every 2s (ok:false each time), or a tray/board client keeps hitting a command that rejects: ~43k warn lines/day, hundreds of MB over the 14-day window on a laptop with a small SSD. When the disk fills, sonic-boom's writeSync throws with no error listener (finding 2) and the daemon aborts in the fatal handler. If the logs dir ever becomes unreadable the janitor silently stops pruning and nothing reports it.

*Fix (small):* Add `size: "50m"` to the pino-roll options (the janitor's pattern already accepts the .N suffix). Add per-(cmd,error) suppression in handleCommand: log the first occurrence, then once per minute with a `suppressed: n` count. Make pruneLogs return/log its errors at warn instead of swallowing.
*Fixer notes:* Fix as suggested: add a `size` cap (e.g. "50m") to the pino-roll options in lib/daemon-logger.ts:49-57 so within-day growth is bounded independent of the janitor's daily/age-based sweep. Add per-(cmd,error) suppression around lib/daemon.ts:334 (log first occurrence, then throttle to e.g. once/minute with a suppressed-count). Change log-janitor.ts's readdir catch (44-48) and unlink catch (62-67) to log at warn via a caller-supplied logger instead of swallowing silently; daemon.ts already has `log` in scope where pruneLogs is called (lines 234, 243), so pruneLogs could accept an optional onError callback rather than reaching for a global logger. Test coverage to add: a pino-roll size-limit integration test confirming rotation triggers before the daily boundary, and a handleCommand test asserting a burst of identical ok:false results produces a bounded number of log lines. Blast radius of the fix is isolated to lib/daemon-logger.ts and lib/daemon.ts's handleCommand, no schema/state.db involvement. Separately, correct the finding's illustrative scenario before it ships in a report: chat:read is not a valid repro path.
*Partial mitigation today:* Partial/insufficient: lib/daemon.ts's daily+boot-time setInterval/setTimeout calling pruneLogs() does provide age-based deletion (default 14 days, configurable via rt.logRetentionDays), which eventually removes any oversized file, but only after up to ~14 days, and it does nothing to bound growth during that window, so it mitigates unbounded accumulation over the long run but not acute single-day/rapid-storm growth or the disk-fill risk the finding describes.

#### S032 · P2 · hazard · `lib/daemon-logger.ts:59`
**A log write error throws out of every log call, including the fatal handler (no 'error' listener on the stream)**

Disk fills (the daily file has no size cap, see the warn-storm finding) or the logs dir loses write permission mid-run. The next fs.writeSync throws ENOSPC -> sonic-boom emit('error') with zero listeners -> EventEmitter throws synchronously from inside log.info/warn. handleCommand's catch (daemon.ts:339-341) calls log.error -> throws again -> every IPC/REST command 500s. Any later uncaught exception enters the uncaughtException handler whose logger.fatal throws inside the handler -> hard abort with no record -> launchd restart -> finding 1. The stderr interceptor's fallback (line 254-257) does not help because the throw originates inside logger.error itself.

*Fix (small):* In createDaemonLogger: stream.on('error', (err) => { degraded = true; origStderr(`daemon-logger: ${err.code} ${err.message}\n`); }) and have flush() swallow. Wrap the bodies of the uncaughtException/unhandledRejection handlers in try/catch that falls back to a raw fs.writeSync(2, ...). Expose `loggerDegraded` on the handle so status/tray can report 'logging disabled: ENOSPC'.
*Fixer notes:* Add stream.on('error', ...) in createDaemonLogger (daemon-logger.ts, after line 57) that sets a degraded flag and writes a raw fs.writeSync(2, ...) so the SonicBoom emit('error') has a listener and never throws out of a log call. Independently, wrap the bodies of the uncaughtException and unhandledRejection handlers (daemon-logger.ts:237-244) in try/catch that falls back to raw fs.writeSync(2, ...) then still runs process.exit(1), so a logging failure cannot abort the crash handler or skip the exit. Consider a bounded maxLength on the pino-roll stream so a wedged disk drops lines rather than throwing. Blast radius is confined to lib/daemon-logger.ts; no schema/registry/settings surfaces involved. Add a test that constructs createDaemonLogger against a stream whose fd write throws ENOSPC and asserts logger.info() does not throw and the degraded fallback fires; a second test that the uncaughtException handler still calls process.exit(1) when logger.fatal throws (inject a throwing logger). Do not test against real ~/.mattstack; use an isolated logDir per the existing preload HOME isolation.
*Partial mitigation today:* Partial only: the stderr interceptor's own logger.error is try/catch-guarded with an origStderr fallback (lib/daemon-logger.ts:249-263), and getDaemonLogger clears its cache on init failure (lib/daemon-logger.ts:103-109), and flush() swallows (lib/daemon-logger.ts:80). Command-path throws are contained as 500s by the server try/catch (lib/daemon/socket-server.ts:48-51, lib/daemon/api-server.ts:203-206). None of these protect direct log.* calls in timers/pollers or the fatal handler at lib/daemon-logger.ts:238.

#### S033 · P2 · bug · `lib/daemon-logger.ts:249`
**stderr intercept promotes every resolver console.warn to ERROR level, repeated on every tick**

A teammate commits `rt.repoTracking` (machine-only) or a newer-binary key into the shared team settings store. On the older user's daemon, `loadRepoTracking()` calls `getSetting("rt.repoTracking")` and `getSetting("mattstack.tracking")` on every events batch, every cache-refresh cycle, every applyMRWriteback and every project-mrs:read. Each call hits `warnInvalid` (or the resolver-throw warn in repo-tracking.ts:142/171), which writes to stderr, which the intercept logs at ERROR. `rt daemon logs` shows an unbounded stream of identical red lines; the tray/health surfaces that count error lines misreport the daemon as unhealthy; real errors are buried. The resolver doc explicitly promises this case must be a degrade, not an alarm.

*Fix (small):* Give the resolver an injectable warn sink (default console.warn) and have the daemon bind it to `log.warn` with per-message dedupe (like repo-tracking.ts's lastTeamTrackingWarning). Independently, make the stderr intercept log at `warn`, not `error`, for text that starts with `rt:`.
*Fixer notes:* Two independent fixes, as the finding's own suggested_fix lists:

1. lib/daemon-logger.ts:249-253 ... the stderr intercept cannot distinguish console.warn from console.error (both hit stderr), so it should not blanket-promote to `logger.error`. Either parse a leading "rt:" convention to route at `warn`, or (more robustly) give rt-client's console.warn call sites an injectable sink so they never go through raw stderr at all in daemon context.

2. packages/rt-client/src/settings/resolve.ts:490-494 `warnInvalid` (and the sibling `console.warn` at line 546 and 586) has no dedupe, unlike repo-tracking.ts's own `lastTeamTrackingWarning` pattern for the resolver-throw case. Given this module is imported by both CLI (one-shot process, dedupe doesn't matter) and the daemon (long-lived, calls this on every tick), the fix belongs in the daemon's usage, not by changing resolve.ts's default for the CLI. Suggested: an injectable warn sink defaulting to `console.warn` (keeps CLI/test behavior unchanged), bound in the daemon to a per-key-per-scope deduped `log.warn`.

Test to add: a settings-resolver test asserting `getSetting` on a key present in a disallowed scope calls the warn sink exactly once per distinct (key, scope, reason) tuple when a daemon-style deduping sink is wired in, and a daemon-logger test asserting a stderr line prefixed "rt:" is *not* promoted to `error`. Blast radius: this generalizes beyond `rt.repoTracking` ... any registered setting a daemon hot path reads via `getSetting`/`listSettings`, found in a scope its `SettingDef.scopes` disallows, hits the identical unthrottled path.

#### S034 · P2 · bug · `lib/daemon.ts:103`
**A serving daemon never re-reads mattstack.mode; the spec's 'park loop converges on the setting' holds only for the parked flavor**

After boot the mode is never consulted again. Flip `mattstack.mode` from dev to prod by hand (the spec's blessed dead-tray repair path): the dev daemon keeps serving indefinitely because nothing in it re-resolves intent, and the prod daemon, once launched, logs one 'standoff … waiting for it to drain' line and loops every 30s forever. The machine does not converge; the user has to know to `launchctl bootout` the dev label or run the full toggle, which is exactly the manual recovery the spec was written to eliminate. The park.ts docblock ('staying alive lets a toggle flip convert this process into the serving daemon within one cycle') is true only for the parked side.

*Fix (small):* Re-run the intent check on the 30s cadence inside the serving daemon (a timer that calls resolveIntendedMode(); on mismatch log, run cleanup(), and exit with the stand-down code) so the standoff partner can take over within one cycle, or document that manual mode writes require a bootout and remove the 'converges' claim from the spec.
*Fixer notes:* Fix as suggested: add a periodic resolveIntendedMode() recheck inside the serving daemon (e.g. a setInterval alongside the other pollers.ts timers, PARK_INTERVAL_MS cadence to match park.ts) that on mismatch runs cleanup() and process.exit() with a distinct "stood down: intent changed to X" log line ... this makes the standoff partner's probeHolder() see the socket go dead within one 30s cycle, exactly as park.ts's docblock already claims. Alternatively, narrow the spec/docs claim: flavor-exclusivity-design.md:52-56 currently states the manual-set escape hatch converges via "the park loop" without qualifying that this only holds when the currently-serving daemon happens to exit for an unrelated reason; that claim should either be fixed in code or corrected in the doc to require `launchctl bootout` of the serving label as part of the manual-escape-hatch procedure. Tests to add: an integration-level test (or a new unit test analogous to park.test.ts but for a "serving mode recheck" function) verifying a serving daemon stub actually exits when resolveIntent() flips away from its own flavor. No blast-radius concern beyond the flavor-toggle subsystem ... this doesn't touch state.db, SCHEMA_VERSION, or the module registry.

#### S035 · P2 · hazard · `lib/daemon.ts:194`
**Hourly events-bus sweep runs unguarded in a timer: a sync sqlite throw exits the daemon**

The disk fills up (a common state on a laptop running builds and worktree pools) and the hourly sweep's DELETE needs to grow the WAL: sqlite throws SQLITE_FULL synchronously inside the timer callback, which is an uncaughtException, so the daemon logs fatal and exits 1. launchd (KeepAlive SuccessfulExit:false, ThrottleInterval 10) restarts it, the 30s boot sweep throws again, and the daemon enters a 40s crash loop, taking chat, events:wait, runs and every consumer down until the disk is freed. The same class applies to any sync throw in a bare timer callback.

*Fix (trivial):* Wrap both sweep timers in try/catch with log.warn like their siblings; set `PRAGMA busy_timeout` on events.db to match the daemon flavor; consider a shared `safeInterval(fn, ms, label)` helper so no future module-scope timer can take the process down.
*Fixer notes:* Wrap both sweep() call sites (daemon.ts:194 and :196) in try/catch + log.warn, matching the pruneRuns/pruneLogs sibling pattern exactly (same file, ~6 lines each). Adding `PRAGMA busy_timeout` to events-bus.ts's db (per daemon-flavor, mirroring lib/state/db.ts's ordering: busy_timeout before journal_mode=WAL) is good hygiene for consistency with the rest of the codebase, but note it does NOT address the actual trigger here (events.db is single-writer, confirmed by grep ... no other module calls createEventsBus ... so SQLITE_BUSY contention isn't the live risk; SQLITE_FULL/IOERR from disk pressure is, and busy_timeout doesn't mitigate those). The try/catch is the load-bearing fix; a shared safeInterval/safeTimeout helper would prevent recurrence for any future module-scope timer in daemon.ts, of which there are already 3 unguarded-by-default sites here alone.
*Partial mitigation today:* None for sweep() itself. lib/state/busy.ts's persistOrWarn/runCriticalWrite is the codebase's established pattern for catching sync sqlite throws elsewhere (state.db writers), and daemon.ts's pruneRuns/pruneLogs timers (lines 200-207, 209-216, 232-239, 241-248) each wrap their body in try/catch+log.warn ... but neither pattern is applied to the two eventsBus.sweep() call sites at daemon.ts:194 and :196.

#### S036 · P2 · hazard · `lib/daemon.ts:355`
**`shutdown` verb exits 0, which launchd treats as do-not-respawn, and `rt daemon start` cannot recover from that state**

A client POSTs /api/shutdown (documented in API_INDEX as "Gracefully stop the daemon") or sends `shutdown` over the socket. Exit 0 means launchd does not respawn, but the agent stays registered. daemonQuery's auto-start POSTs /daemon/start; the tray calls SMAppService.register on an already-registered agent, which throws kSMErrorAlreadyRegistered and is reported ok, so attemptRestart returns true, the 300ms retry fails, and the user sees "installed but not running. Run: rt daemon start". `rt daemon start` follows the same register path and prints "daemon starting… check logs" indefinitely. Only `rt daemon restart` (kickstart) or a logout/login recovers.

*Fix (small):* Either make `shutdown` exit non-zero (so KeepAlive respawns it, matching what every caller of a 'graceful stop' on a supervised service actually wants) or remove the verb from the REST index and route stops through the tray's unregister. Independently, make the tray's /daemon/start kickstart when the agent is already registered but no daemon answers on rt.sock.
*Fixer notes:* Two independent fixes, both cited correctly in suggested_fix:
1. lib/daemon.ts:355-359 ... either exit non-zero from the `shutdown` verb, or route it through the same unregister path DaemonLifecycle.stopDaemon() uses (so the SMAppService record itself goes to notRegistered instead of stranding at enabled-but-dead). Note lib/daemon/shutdown.ts:62-67's signal handler has the identical exit(0) pattern for SIGTERM/SIGINT/SIGHUP ... worth flagging separately since a fix scoped only to the `shutdown` verb would leave that path exposed too.
2. rt-tray/Sources/DaemonLifecycle.swift `startDaemon()` should detect the "registered but not answering on rt.sock" state (e.g. probe the socket, or treat `kSMErrorAlreadyRegistered` as a signal) and fall back to `services.restart(label:)` (kickstart) rather than treating the already-registered error as success. `/daemon/status` (TrayServer.swift:396-398) could also start reporting real liveness (PID/socket probe) instead of only SMAppService's registration enum, which is what let this go undetected end-to-end.
Test to add: a KeepAlive-respecting scenario is hard to exercise without launchd itself; the practical regression test is unit-level ... assert `routeCommand("shutdown", …)` schedules a non-zero exit (or a call into an injected unregister hook), and a Swift-side test asserting `startDaemon()` calls kickstart when register() throws already-registered with status `.enabled`.

#### S037 · P2 · hazard · `lib/daemon.ts:422`
**A state.db open/migrate failure at boot is an unbounded launchd crash loop that also breaks every CLI command**

On a machine that is not the author's: ~/.mattstack/rt (or state.db's -shm) is not writable (SQLITE_CANTOPEN/READONLY), or a legacy importer throws (see the importer finding), or BEGIN IMMEDIATE stays BUSY past the 5s budget. openStateDb throws, the fatal handler exits 1, launchd (KeepAlive SuccessfulExit=false) relaunches every ~10s forever, each run appending the same fatal line to daemon.log (~8.6k lines/day). Every rt command that touches state fails identically, the tray only shows the daemon as down, and there is no backoff, marker, quarantine or user-facing instruction. The spec's 'fail loudly' becomes a silent loop for a non-author user.

*Fix (medium):* Wrap the boot open: on SQLITE_BUSY re-raise (transient, relaunch is right); on any other error log once with the path and code, write a `state.db.boot-failed` marker that `rt daemon status`/the tray can surface, and park (as parkUntilIntended does) instead of exiting, or quarantine-and-recreate for schema-level failures. Add a test that a throwing openStateDb at boot does not exit non-zero repeatedly.
*Fixer notes:* The remediation direction in the finding is still right (classify open/migrate errors; on BUSY re-raise for a clean relaunch; on persistent errors write a state.db.boot-failed marker rt daemon status/the tray can surface; park instead of hanging), but the fix must target the ACTUAL failure, which is a hung zombie on prod, not a crash loop ... so wrapping openBranchCacheStore in a try/catch that logger.fatal + process.exit(1) would, on prod, be a strict IMPROVEMENT toward the finding's own described behavior (it would at least give launchd a non-zero exit and let ThrottleInterval retry transient BUSY). Do NOT rely on the uncaughtException handler to catch this ... an async-captured throw re-thrown from .catch surfaces as unhandledRejection; wrap the boot open explicitly. Note the asymmetry to preserve: the CLI path (installCliLogging) already exits 1 on both handler kinds; only the daemon's unhandledRejection handler is log-only. Any test should assert that a throwing openStateDb at boot on the --daemon path terminates the process (or parks with a marker) rather than leaving a live process with no bound socket. Blast radius is boot-only; no schema/SCHEMA_VERSION interaction.
*Partial mitigation today:* Partial: lib/state/db.ts:489-494 quarantines-and-recreates on SQLITE_CORRUPT/SQLITE_NOTADB, so genuine file corruption self-heals and never reaches the throw path this finding targets. For all other open/migrate errors there is no mitigation. On the prod daemon, the log-only unhandledRejection handler (lib/daemon-logger.ts:242-244) is what actually prevents the claimed exit-1 crash loop ... at the cost of a hung zombie. On the CLI side, installCliLogging (lib/cli-logger.ts:250-251) does cause each state-touching command to fail loudly with exit 1, which is the intended per-invocation behavior (not a loop).

#### S038 · P2 · hazard · `lib/daemon/agent-status-poller.ts:39`
**Agent-status poller opens every pipeline run DB synchronously every 10 seconds**

Runs are retained for 30 days (runs/prune.ts:63 default). A user driving pipelines routinely accumulates hundreds of run directories; every 10 seconds the daemon synchronously readdirs the tree, opens each state.db read-only, runs four queries, and closes it, then the console's runs:list does the same walk again per poll. At a few hundred runs this is on the order of hundreds of milliseconds of blocking work per tick on the daemon thread, competing with every IPC request, and it grows linearly with retained runs. herdr being absent does not help: probeAgents returning null skips the tick, but a healthy herdr means the full walk every tick.

*Fix (medium):* Cache per-run summaries keyed by state.db mtime (or only re-open runs whose mtime changed), and restrict the poller's walk to runs already known to be `running`. Move the walk off the event loop or cap it with an incremental scan.
*Fixer notes:* Two independent hot paths hit the same expensive `listRuns` full-walk-and-open-every-db: the 10s agent-status poller (lib/daemon/agent-status-poller.ts:39,49) and every `runs:list` IPC/REST call (lib/daemon/handlers/runs.ts:25) -- fixing one without the other leaves the compounding half of the failure scenario intact. Cheapest fix: since finished runs' rows never change, memoize `runRow`+`withAttention` output per run keyed by `(repo, id, state.db mtime)` inside store.ts and only reopen when mtime advances; this collapses both call sites' cost to a stat() per finished run instead of an open+5-query cycle. A deeper fix (per the original suggestion) would maintain a small side-index of just the "running" run ids (written when a run starts/ends) so the poller's per-tick work is O(running runs) instead of O(all retained runs) -- but that touches the pipeline-state.sh writer contract on the mattstack-skills side, not just rt, so scope it as a separate follow-up. Any fix should get a regression test with e.g. 300 fixture run dirs asserting `listRuns` runtime stays sub-linear / cached across two calls when no mtime changed -- lib/runs/__tests__/store.test.ts is the natural home.

#### S039 · P2 · hazard · `lib/daemon/agent-status-poller.ts:43`
**Agent-status poller opens every run's SQLite db synchronously every 10s and spawns herdr even where it does not exist**

A heavy pipeline user accumulates 400-800 run dirs inside the 30-day floor. Every 10 seconds the daemon thread synchronously opens each state.db, runs four queries, and closes it: several hundred sync file opens and reads per tick, 0.5-2s of event-loop occupancy on a cold disk cache, recurring forever. That is the tray-flicker signature (status poll latency spikes at a fixed cadence). On a machine with no herdr at all (the shared-user case), the tick still spawns `~/.local/bin/herdr` every 10s (Bun.spawn ENOENT -> exitCode -1 -> null) and never backs off.

*Fix (medium):* Short-circuit: if probe() returns [] (no agents) and the previous tick saw no running runs, skip listRuns; only re-open run dirs whose state.db mtime changed since the last tick (cache per-dir summaries); back off the herdr probe to 60s after N consecutive spawn failures and log once.
*Fixer notes:* Two independent fixes needed, matching the two sub-scenarios: (1) in listRuns/openRun, filter to running-status runs before opening each state.db rather than after (needs a cheap status check, or cache per-run summaries keyed by state.db mtime so unchanged finished runs are never reopened). (2) in probeAgents/the poller, add a consecutive-failure counter that widens the probe interval (or stops probing) after N straight ENOENT/exitCode!=0 results, logged once, so a herdr-less machine doesn't spawn-attempt forever at 10s cadence. Add a test to lib/daemon/__tests__/agent-status-poller.test.ts asserting `list` is not called (or called with a cheaper path) when the run set has no running entries, and a test in lib/runs/__tests__/store.test.ts asserting a finished run's db is not opened when only running runs are requested. Blast radius: agent-status-poller.ts and runs/store.ts only; no schema/IPC contract change needed for either fix.

#### S040 · P2 · hazard · `lib/daemon/api-auth.ts:40`
**POST /api/hooks/:repo/repair and POST /api/refresh are mutating routes exempt from the token gate**

The preflight at api-server.ts:127-129 answers 204 with Allow-Methods POST and Allow-Headers Content-Type, so a cross-origin page can POST JSON to /api/refresh (kicks a GitLab/Linear enrich cycle; refreshCache is coalesced but each burst still costs API quota) and, after listing repo names from the open GET /api/repos, POST /api/hooks//repair, which rewrites core.hooksPath in the user's .git/config. The comment in api-server.ts:131-132 says the token is the CSRF defense for control endpoints; two control endpoints are outside it. Allowlist-by-path also guarantees the next POST route added to REST_ROUTES ships unguarded.

*Fix (small):* Invert the rule: needsToken returns true for every non-GET/OPTIONS request, plus an explicit list of GETs whose body is sensitive or whose handler mutates (secrets, notifications). Keep a test that enumerates REST_ROUTES and asserts every POST is gated.
*Fixer notes:* Fix at api-auth.ts:40 ... invert the allowlist to a denylist (default true for POST/PUT/DELETE, explicit exceptions only for reads that are meant to be open) as the suggested_fix proposes, or at minimum add `/api/refresh` and `/api/hooks/` (prefix match on the `/repair` suffix) to the current allowlist. Either way, add a test that walks `REST_ROUTES` plus the two hardcoded prefix routes in api-server.ts and asserts `needsToken` returns true for every POST ... the current `api-auth.test.ts` enumerates specific paths by hand and would not have caught this, nor would it catch the next unguarded POST added to `REST_ROUTES`. No ordering/blast-radius concerns beyond the routes already named; `hooks:repair`'s bounded blast radius (fixed shimsDir, requires pre-existing rt-managed hooks) is worth noting in the writeup so severity isn't overstated to P1, but it doesn't change that the gate itself is missing.
*Partial mitigation today:* Partial, not a full mitigation: `hooks:repair`'s target value is not attacker-controlled ... `checkAndRepairHooksPath` always sets `core.hooksPath` to the daemon's own fixed `shimsDir` for that repo (hooks-guard.ts:52), and only proceeds at all when the repo already has rt-managed `hooks.json`/`hooks/` present (hooks-guard.ts:38) and is already a daemon-tracked repo in `ctx.repoIndex()` (handlers/hooks.ts:30-32). So the CSRF cannot redirect hooks to an arbitrary attacker path or touch an untracked repo ... worst case it forces a resync to the value the daemon already intends. This bounds impact but does not close the authorization gap: an unauthenticated cross-origin request still triggers real subprocess execution (`git config ...`) and a background external-API refresh cycle with zero token check, which is exactly the class of request `needsToken`/CORS-preflight was built to block.

#### S041 · P2 · bug · `lib/daemon/api-server.ts:58`
**GET /api/notifications drains the queue with no token, contradicting the 'mutations need a token' policy; reachable cross-origin**

A cross-origin page (CORS is `*`, GET needs no preflight) or any other local user account fetches http://127.0.0.1:9401/api/notifications in a loop. Every pending notification is deleted from the durable queue before the tray reads it, so the user's desk alerts (new MR comments, chat mentions, runaway processes) silently never arrive. The page also reads the notification contents. needsToken() cannot express "this GET mutates", so the policy comment in api-auth.ts is false for this route.

*Fix (trivial):* Add `/api/notifications` to needsToken (the tray already speaks the unix socket and can read the token file), or change the REST route to `notifications:peek` and keep the destructive drain socket-only.
*Fixer notes:* Checked feasibility of the suggested fix against the actual tray client: `rt-tray/Sources/DaemonClient.swift` has zero references to a token (grepped `X-RT-Token|api-token|apiToken` across rt-tray/Sources -- no hits), so the claim 'the tray already ... can read the token file' is not true today; it would need new code to attach `X-RT-Token`. However, gating `/api/notifications` via needsToken() would NOT break the tray in practice: DaemonClient.fetchNotifications() -> query() tries queryHTTP() first, and on any non-200 (a 401) falls back to querySocket() (DaemonClient.swift:49-56, 88+), which speaks to the daemon's unix socket -- a transport that bypasses needsToken/tokenOk entirely (only api-server.ts imports those; the socket server has no such check). So the naive fix works via the existing fallback with zero client changes, just one extra failed HTTP round trip per tray call. Cleaner alternative matching HTTP semantics: keep GET `/api/notifications` calling the already-existing non-destructive `notifications:peek` handler (status.ts:158-161), and require a token-gated POST (e.g. `/api/notifications/drain`) for the destructive drain, reserving true drain for the unix-socket-only trusted path the tray already falls back to. Either way, add a regression test asserting `needsToken('GET', '/api/notifications')` (or its replacement route) is true, since api-auth.ts's own module comment ('True when a request mutates state ... must present the local token') is currently false for this route and nothing catches that drift today.

#### S042 · P2 · hazard · `lib/daemon/api-server.ts:84`
**broadcast() ignores ws.send() status; a backpressured or dead client silently loses frames with no log or disconnect**

A console or chat-viewer tab is backgrounded or its socket stalls. `system-processes` frames every 10s and `mr:update`/`discussions:update` frames (full MR and discussion objects) exceed the 1 MB per-socket backpressure limit; subsequent sends return 0 and the frames are dropped. The client stays in wsClients, keeps receiving a subset of frames, and its UI silently diverges from daemon state (a merged MR never updates, a chat wake never arrives) until it happens to reconnect. Nothing is logged because send does not throw.

*Fix (small):* Check the return value: on 0, log at warn and ws.close() so the client's reconnect logic (relay.ts, subscribeToDaemon) restores a consistent stream; on -1, count it and close after N consecutive backpressured sends. Consider trimming `system-processes` and `mr:update` to deltas or ids and letting clients fetch on demand.
*Fixer notes:* Fix belongs in broadcast() at api-server.ts:81-87: branch on the ws.send() return value ... on 0 (dropped) log at warn with the ws's identity and call ws.close() so relay.ts's onclose path drives a real reconnect/resync; on -1 (backpressure) either count consecutive occurrences per-socket and close after N, or set closeOnBackpressureLimit:true in the Bun.serve websocket config (currently unset, so it defaults to false) to let Bun itself sever chronically-backpressured sockets. Note bun-types 1.3.10 is internally inconsistent about the backpressureLimit default (serve.d.ts:439 says 16 MB, but docs/runtime/http/websockets.mdx:361 in the same installed package says 1 MB) ... verify the real threshold against the running Bun version rather than trusting either doc when sizing a fix. Any fix should add a test asserting broadcast() logs/closes on a stubbed ws.send() returning 0/-1, since no existing test in lib/daemon/__tests__ covers this path.

#### S043 · P2 · bug · `lib/daemon/api-server.ts:103`
**Fixed port 9401 crash-loops the daemon when a second user account or app holds it**

A second user account or another app holds 9401. On the dev flavor (bun run lib/daemon.ts) the EADDRINUSE is a top-level throw: exit 1, launchd respawns every 10s, an unbounded silent crash loop; observed live on 2026-08-25 as 24 consecutive fatal boots, each unlinking and re-binding rt.sock before dying. On the prod flavor the same throw is swallowed by the unhandledRejection handler (the S001 zombie mechanism): the process stays alive with rt.sock bound and no API server. Either way the user has no working daemon and no surfaced signal.

*Fix (medium):* Catch the bind failure, fall back to an ephemeral port (or a per-uid port), persist the chosen port in ~/.mattstack/rt/api-port next to api-token, and have rt-client/daemon-client read it instead of the constant. At minimum log the EADDRINUSE with the remedy and stand down (park) instead of crash-looping.
*Fixer notes:* Two separable fixes. (1) Bind robustness: wrap the api bind in try/catch; on EADDRINUSE do NOT process.exit (a deliberate non-zero exit is what WOULD create the crash-loop the finding feared, via KeepAlive{SuccessfulExit:false}+ThrottleInterval) ... instead log the remedy and either retry-with-backoff or park, and make startDaemon resilient so a mid-init throw does not strand the process without installSignalHandlers/pollers (move installSignalHandlers earlier, or run the API bind last after all other subsystems and guard it). (2) The genuinely concerning half ... machine-global port ... deserves its own finding: pick a per-uid/ephemeral port, persist it to ~/.mattstack/rt/api-port next to api-token, and have consumers (packages/rt-client/src/relay.ts:16, rt-tray Sources/DaemonClient.swift:62, board, console) read it instead of the 9401 constant; this fixes both the collision and the cross-user wrong-daemon connection. Add a test asserting startDaemon either completes signal-handler install or exits cleanly when the API port is taken (set RT_API_PORT to a pre-bound port in an isolated HOME).
*Partial mitigation today:* Partial/insufficient: lib/daemon-config.ts:API_PORT = Number(process.env.RT_API_PORT) || 9401 lets an isolated e2e daemon override the port so it does not collide with a live one ... but this is opt-in via env and not wired for the multi-user / port-squatter production case, so it does not mitigate the real scenario. No try/catch or EADDRINUSE handling exists at the bind site (api-server.ts:103) or in cli.ts's --daemon branch. The prior audit docs/daemon-runner-health.md does not cover this and is itself largely stale (it catalogs the removed process-manager subsystem).

#### S044 · P2 · hazard · `lib/daemon/boot-reconcile.ts:20`
**Stale-daemon eviction races the old daemon's cleanup, which can unlink the new daemon's socket and pid**

Old daemon receives SIGTERM; its gracefulExit runs servers.stop(true), disposeFreshness (watcher disposal), hooksGuard.closeAll over N repos, log flush. Under load that exceeds 300ms. The new daemon has meanwhile written rt.pid and bound rt.sock; the old daemon's cleanup then unlinks BOTH by path, unconditionally. Result: a live daemon holding :9401 with no socket file and no pid file. Every CLI/tray call fails (socket missing), `rt daemon` verbs cannot find a pid to evict, and the next launchd restart's Bun.serve on 9401 throws EADDRINUSE -> crash loop. This is the 'starts, binds nothing, logs nothing' symptom CLAUDE.md warns about.

*Fix (small):* Before signaling, confirm the pid is an rt daemon: probe rt.sock identity (park.ts probeSocketHolder already does this) and only evict on a same-flavor answer, or check `/proc`-equivalent via `ps -o comm= -p ` / `proc_pidpath` for the rt executable, and/or store the process start time next to the pid and compare. Unlink a pid file that fails the check instead of acting on it.
*Fixer notes:* The `ping` handler (lib/daemon/handlers/status.ts:23-25) already returns { pid, flavor, version, sourceRev, startedAt }, so the suggested identity-probe fix is feasible with no new plumbing. Two independent hardening points, fix both: (A) shutdown.ts:44-46 must not unlink rt.pid/rt.sock unconditionally ... read rt.pid and only unlink when it still equals String(process.pid) (a compare-and-delete), so a shutting-down daemon can never delete a successor's files. (B) boot-reconcile.ts should stop relying on a blind fixed 300ms sleep: after SIGTERM, poll process.kill(pid,0) until it throws (bounded wait) before proceeding, and/or probe rt.sock identity and only evict on a matching flavor/startedAt. Also align socket-server.ts:19-22 with the ratified flavor-exclusivity spec (probe-connect before the unconditional unlink+bind). Blast radius: shutdown/boot are exercised on every daemon restart, so add regression tests around the compare-and-delete (old daemon must NOT unlink a pid file whose contents differ from its own pid) and around evict returning only after the old pid is gone. Note the API server (api-server.ts:103-110) sets no reusePort, so a concurrent live old daemon also makes startApiServer at daemon.ts:469 throw EADDRINUSE uncaught ... worth handling in the same pass.
*Partial mitigation today:* Partial: lib/daemon/park.ts:63-77 routes a LIVE, DIFFERENT-known-flavor socket holder to standoff and explicitly skips evict, removing the steady-state cross-flavor race. lib/daemon/boot-reconcile.ts:18 guards previousPid===process.pid. But it does NOT cover same-flavor orphans, unknown-flavor holders, or a different-flavor holder caught mid-drain, and lib/daemon/shutdown.ts:44 has no ownership guard at all. Note: the flavor-exclusivity spec (docs/superpowers/specs/2026-08-25-flavor-exclusivity-design.md, 'Socket takeover' bullet) called for socket-server to probe-connect before binding, but socket-server.ts:19-22 still does the OLD unconditional unlink+bind; the probe was placed only in park.ts.

#### S045 · P2 · bug · `lib/daemon/cache-refresh.ts:112`
**5-minute cache refresh shells out synchronously per repo (worktree list, for-each-ref, remote url)**

Each refresh cycle performs 3 to 4 synchronous git spawns per indexed repo with no timeout. With 20 repos that is 60+ blocking spawns every 5 minutes; a single repo on a slow external/network volume, or a git binary blocked on a stale `.git/index.lock` in `worktree list` (which rewrites nothing but still opens the repo), freezes the daemon for the whole duration, delaying every IPC request and events:wait delivery. The module header itself still documents the execSync cost as the reason for coalescing rather than removing it.

*Fix (small):* Replace listWorktrees/listWorktreeRoots with lib/worktree/git-async.ts listWorktreesAsync, and the two execSync calls plus freshness.getRemoteUrl with runGit/runCapture (5s timeout). Add a source-guard test that fails on `execSync` imports under lib/daemon/ outside an explicit allowlist.
*Fixer notes:* The suggested fix's target already exists and is the codebase's own established pattern for this exact problem: lib/worktree/git-async.ts's module doc names cache-refresh.ts's exact symptom class and says "everything reachable from a daemon timer or handler goes through here instead" (runCapture, 60s default timeout). Concrete swap sites: cache-refresh.ts:112 listWorktrees → git-async.ts listWorktreesAsync (note: return shape differs ... WorktreeEntry.branch is `string | null` vs. WorktreeRef.branch `string`, and it returns `null` on git failure instead of `[]`, so the `.filter(w => w.branch && ...)` call site and the `catch` semantics both need adjusting); cache-refresh.ts:119 (for-each-ref) and :139 (config --get remote.origin.url) and freshness.ts:97 getRemoteUrl → runGit()/runCapture with a short timeout (5s is reasonable; DEFAULT_TIMEOUT_MS in git-async.ts is 60s, probably too generous for a background poll); cache-refresh.ts:209 listWorktreeRoots → listWorktreeRoots has no async twin in git-async.ts yet (only listWorktreesAsync exists) ... would need adding, or callers switched to `.map(w => w.path)` off listWorktreesAsync. Also worth folding the doppler-sync loop's per-repo execSync (currently ungated by grants, cache-refresh.ts:206-209) under the same fix so it doesn't remain the widest-blast-radius call site after the others are converted. A source-guard test banning bare `execSync`/`child_process` imports under lib/daemon/ (the suggested fix's second half) is reasonable and would have caught this; git-worktrees.ts itself is outside lib/daemon/ so the guard needs to cover its call sites transitively or ban the import at the daemon layer specifically.

#### S046 · P2 · hazard · `lib/daemon/cron.ts:78`
**Cron triggers spawn with the launchd-frozen environment, repeating the spawn-env defect**

A user configures rt.cron with run: ["pnpm", "lint"] or ["rt", "skills", "compile"]. Under SMAppService the daemon's startup PATH is /usr/bin:/bin:/usr/sbin:/sbin; Bun.spawn without env passes that frozen env to the child, so `#!/usr/bin/env node` shebangs and any non-absolute tool fail with ENOENT, logged only as 'spawn failed' or exit 127. The 2026-08-21 spawn-env spec fixed runCapture and left this second spawn site untouched; agent.ts's defaultSpawnHeadless got the env, cron did not.

*Fix (trivial):* Pass env: { ...process.env } (and document that argv[0] must be absolute or resolved via Bun.which against process.env.PATH before spawning).
*Fixer notes:* Mirror lib/subprocess.ts:51 / lib/daemon/handlers/agent.ts:35: pass `env: { ...process.env }` in the Bun.spawn call at cron.ts:78. Cron starts (daemon.ts:251) after the PATH overlay runs (daemon.ts:120-123), so this fix alone is sufficient without further ordering changes. Absolutizing argv[0] via Bun.which is a nice-to-have but not required to close the hole. Add a regression test in lib/daemon/__tests__/cron.test.ts that spawns via the real (non-overridden) defaultRunCommand path with a minimal env and asserts a PATH-dependent command resolves -- the existing suite likely only exercises the injectable `runCommand` override, which would never catch this.

#### S047 · P2 · hazard · `lib/daemon/events-bus.ts:98`
**events journal reads are unbounded: no LIMIT in SQL, glob filtered in JS, rescanned per waiter on every emit**

Any client calling events:list or events:wait with after: 0, a cursor older than the last sweep, or a cursor from a recreated events.db (handlers/events.ts:45,58 pass `after` through unclamped below head) materializes every journal row, payloads included, synchronously on the daemon thread per call; the journal is max(50k, 7 days of pane, cron, chat-wake and run-updated traffic), so this is tens of MB of JSON per poll on a busy estate. A waiter parked with an old afterId triggers the same full scan on every matching emit. The chat spec (2026-08-23, lines 450-463) documents exactly this and works around it client-side with events:head; the bus itself still has no bound, so any other consumer (viewer relay, shepherdr, deck) can stall the event loop.

*Fix (small):* Push `LIMIT ?` into the statement and page in id order until `limit` matches are found (fetch limit+1 to detect truncation); default list to a hard cap (e.g. 500) when the client omits limit; for waiters, advance afterId to the scanned head after each empty wake scan so repeated emits do not rescan the same tail. Add an events-bus test that list with limit reads at most limit+1 rows (assert via sqlite3_total_changes or a spy on afterStmt).
*Fixer notes:* Suggested fix in the finding is sound and directly implementable: push `LIMIT ?` into `afterStmt` and page in id order (fetch limit+1 to detect truncation, since matching happens after fetch and a naive `LIMIT limit` before the JS glob filter could under-return); give `events:list` a hard default cap (e.g. 500) when the client omits `limit` so `rt events list` and any REST caller stop being able to request the entire journal by omission; in `insertAndWake`, pass a limit into the waiter's `eventsAfter` call too (a waiter only needs enough events to resolve its promise, not the unbounded backlog) and/or advance a waiter's `afterId` after any scan so a broad, frequently-firing pattern with a stale cursor doesn't materialize the full backlog on first match. Add an events-bus unit test asserting `list({pattern, limit: N})` never reads more than N+1 rows (spy/wrap `afterStmt.all` or check `db.query('...').all().length` isn't the whole table) and a test that seeds >retentionFloor rows all within the retention window and asserts `sweep()` leaves them all (documents the floor-not-cap behavior as intentional or flags it as the next fix). Note for whoever picks this up: rt-client (packages/rt-client/src/commands.ts:278-279) only types the payload shape, it does not wrap `events:list`/`events:wait` with any client-side default ... so the cap has to live in the bus/handler, not be bolted onto a client wrapper that other consumers won't necessarily call through.
*Partial mitigation today:* None at the bus/handler level. `events:head` (lib/daemon/handlers/events.ts:49-51) exists as a cheap-cursor alternative and is used by rt's own chat tail logic to avoid ever calling the unbounded `events:list`/`events:wait` path for its wake-dedupe snapshot (per docs/superpowers/specs/2026-08-23-rt-chat-design.md) ... but that is a client-side workaround for one specific internal caller, not a fix to `eventsAfter`, `events:list`, `events:wait`, or the REST/CLI surfaces, all of which remain unbounded by default exactly as described.

#### S048 · P2 · hazard · `lib/daemon/freshness.ts:125`
**GitLabProvider instances are cached with the token forever: a rotated gitlabToken never reaches watchers or discussions/MR handlers**

A teammate's GitLab PAT expires (GitLab now enforces expiry). Every watcher goes `degraded` with cause http-error and retries at the 5-min backoff cap; they run `rt secrets set rt gitlabToken `. The secrets memo (secrets/store.ts:209-215) notices the file change, so the next branch-cache refresh works, but `providers` still holds provider objects built with the dead token: events watchers keep 401ing forever, `getRepoContext` (discussions read/resolve/reply, mr:action, project sync deep/delta) keeps failing, and `mr:status` stays `connecting` until the daemon is restarted. The split state (branch cache fresh, everything else 401) is confusing to debug.

*Fix (small):* Key the providers map by (repoName, token fingerprint) or store the token alongside and rebuild when loadSecrets() returns a different value; additionally on an `onStatus` cause of http-error with 401, drop the provider and stopWatch so the next reconcile rebuilds it.
*Fixer notes:* Fixer should note the mixed-outcome nuance: discussions:reply's actual note-post uses a fresh token (via loadSecrets() at discussions.ts:170) and likely succeeds even with a stale cached provider, but the trailing refreshDiscussions() call at the end of that same handler uses the stale provider and throws, so the RPC still reports ok:false ... a reply can silently succeed while rt reports it failed. Any fix that rebuilds/invalidates the `providers` map on token change (keying by token fingerprint, or dropping+rebuilding on an http-error/401 onStatus cause) should also cover this reply/diffs path since they share getRepoContext even though their own mutation calls don't. Suggested watch-side hook: onStatus's cause classification only exposes 'http-error' generically (EventsWatcher.ts / GitLabProvider.ts have no distinct 401-vs-other-5xx signal today), so 'drop provider on 401' would need either a new SDK signal or an independent probe (e.g. re-run loadSecrets() and compare against what the cached provider was built with) rather than trusting `cause` alone. Test coverage should assert: (1) providers map rebuilds when loadSecrets() returns a changed gitlabToken, (2) stopWatch/restart does not resurrect a stale provider, (3) discussions:reply's reported success/failure matches whether the note was actually posted after a rotation.

#### S049 · P2 · bug · `lib/daemon/freshness.ts:125`
**Rotating gitlabToken has no effect until daemon restart: providers cache the old token per repo**

User's GitLab PAT expires; they run `rt secrets set rt gitlabToken `. The secrets memo is mtime-aware and re-decrypts, but `providers` (and every `watches.*.provider`, and `ephemeralCtx`) hold GitLabProvider instances constructed with the old token. Every mr:action, discussions:*, project-mrs sync and events watcher keeps sending the revoked token: 401s in the log, `mr:status` stays "connecting", handlers return `ok:false` with a 401 message that suggests nothing about the cache. The only recovery is a daemon restart, which nothing tells the user to do.

*Fix (small):* Key the provider cache on (repoName, token) or record the token used at construction and compare it to `loadSecrets().gitlabToken` in ensureProvider/getRepoContext; on mismatch dispose the watch, rebuild the provider and reset `userIdResolved`. Alternatively expose a `secrets:changed` path that calls disposeFreshness() and reconciles.

#### S050 · P2 · hazard · `lib/daemon/freshness.ts:141`
**A credential-bearing remote URL is logged verbatim and echoed in thrown error messages**

A user has a repo cloned as `https://oauth2:glpat-XXXX@gitlab.example.com/g/p.git` or `https://ghp_XXXX@github.com/o/r.git` (common in dotfiles and CI-derived clones). remote.origin.url carries the token. For a non-GitLab host, line 142 writes the full URL, token included, into `~/.rt/logs/daemon.*.log` at info on every reconcile. For a URL parseRemoteUrl rejects (e.g. `ssh://git@host:2222/...` with an embedded password, or any future shape), line 279 puts the URL into the error that every mr/discussions handler returns to the client and that handleCommand logs at error. Logs are the first thing a user pastes into a bug report. The home-snapshot module already has `redactCredentials` for exactly this class of string but it is not shared.

*Fix (trivial):* Hoist `redactCredentials` out of home-snapshot.ts into lib/subprocess.ts or a small util and apply it to every log/error interpolation of `remoteUrl` in freshness.ts (lines 142, 148, 275, 279) and in cache-refresh/enrich wherever a remote URL is echoed.
*Fixer notes:* Natural home for the fix is lib/enrich.ts (already owns parseRemoteUrl/isGitLabRemote) rather than lib/subprocess.ts as suggested ... export a redactCredentials there and have home-snapshot.ts import it too instead of keeping its own private copy. Apply it to freshness.ts:142,148,279 (and getRepoContext's other remoteUrl throw sites: 275). Add a regression test asserting a URL like `https://oauth2:glpat-x@gitlab.example.com/g/p` never appears verbatim in a thrown Error's .message or in a log.info call from ensureProvider/getRepoContext. Also worth a follow-up (separate finding, not this one): parseRemoteUrl's host-capture regex happily swallows `user:token@` into `remote.host` on a successful parse (e.g. gitlab.com HTTPS clone with embedded PAT) ... that credential then flows into `new GitLabProvider(remote.host, token, ...)`, which is a different exposure (wrong-host/token-in-URL confusion for the SDK) worth checking separately.

#### S051 · P2 · bug · `lib/daemon/handlers/agent.ts:98`
**agent:start returns ok with a phantom record when herdr dedups the tab label**

Caller passes `label`/`tab` that matches a live tab in the workspace (mr-board and gitq launchers pass labels on purpose). launchInWorkspace (lib/agent-herdr.ts:84-88) focuses the existing tab and returns `paneId: "", focusedExisting: true` without running any command. agent:start has already inserted rec (line 164) with a freshly minted sessionId, then returns `{ok: true, data: rec}` with no pane ids. The caller believes a new agent launched; `rt agent list` shows a record that never ran; `agent:resume` on it runs `claude --resume ` for a session that does not exist. No test covers focusedExisting and no caller reads it (grep found none).

*Fix (small):* When `out.focusedExisting` is true, deleteAgent(rec.id) and return either `{ok: false, error: "tab '' already open; focused it"}` or an explicit `{reused: true, paneId, tabId}` shape the catalog declares, so consumers can branch on it.
*Fixer notes:* Fix belongs in lib/daemon/handlers/agent.ts's "agent:start" handler, at the res.ok check around line 172: res.ok is true on the focusedExisting path today, so the existing rollback branch never fires. Either (a) have launch() return a distinguishable result for focusedExisting (e.g. an ok:false error, or a tagged ok:true reused:true shape) so agent:start can call deleteAgent(rec.id, db) and either refuse or return the pre-existing tab's identity instead of a phantom row, or (b) extend the AgentRecord/wire shape (packages/rt-client/src/commands.ts "agent:start" data type) with an explicit reused/focusedExisting boolean so callers (mr-board, gitq) can branch instead of silently trusting a sessionId nothing is listening on. Add a case to lib/daemon/__tests__/agent-handlers.test.ts using a herdrRunner mock whose workspace list/tab list responses report an existing tab with a matching label, asserting either that the record is not inserted/is rolled back, or that the response shape flags reuse. lib/rebase-escalation.ts already branches on focusedExisting for a different caller -- worth checking as a pattern to reuse.

#### S052 · P2 · hazard · `lib/daemon/handlers/chat.ts:95`
**chat:post commits the row, then unprotected emit/notify can throw: duplicate post on retry, wakes lost**

postMessage commits to state.db. emitEvent then writes events.db via insertStmt.get (lib/daemon/events-bus.ts:127) which has no busy_timeout and no try/catch; notifyEnabled -> enqueueNotification can also throw a non-BUSY sqlite error. Any of these (SQLITE_FULL on a full disk, a lock held by an orphan daemon during eviction, an I/O error) propagates out of the handler after the message is durable. The caller gets a 500 'command failed', the CLI's unwrap fails, the agent retries and posts the message twice, and the recipients of the first copy never receive their chat/wake events because the loop aborted mid-way.

*Fix (small):* Wrap the post-commit tail in try/catch: log at warn with {err, id}, and still return `{ok: true, data: posted}` (optionally with `warnings: [...]`). Emit the msg topic and all wakes inside one guarded loop so a failure on recipient N does not skip recipients N+1..; consider giving events.db a busy_timeout so transient locks retry instead of throwing.
*Fixer notes:* Fix at the shared `postAndNotify` (lib/daemon/handlers/chat.ts:89-121), not per-handler, since chat:post (line 187) and chat:dm (line 344) both call it. Wrap the emit/notify tail (lines 97-119) in try/catch: log at warn with {err, id, room}, still return `posted` so the caller sees success (the row is already durable) with an optional `warnings` field. Use a loop with per-iteration try/catch for the recipient wake events so one recipient's failure doesn't skip the rest. Separately, `lib/daemon/events-bus.ts` createEventsBus (line 77) should set `PRAGMA busy_timeout` the way lib/state/db.ts does for state.db ... right now events.db has no timeout at all (SQLite default 0), so it is more exposed to a transient SQLITE_BUSY (e.g., a briefly-overlapping second daemon instance) than state.db is. Add a test in lib/daemon/__tests__/chat-handlers.test.ts that passes an `emitEvent` which throws and asserts chat:post/chat:dm still return `ok: true` with the posted id.

#### S053 · P2 · bug · `lib/daemon/handlers/discussions.ts:142`
**discussions:diffs uses raw fetch with no timeout and silently truncates at 100 files**

(a) An MR touching 250 files: the handler returns the first 100 diffs with `ok: true` and no indication of truncation; the console/board renders a partial diff set and a reviewer misses files. (b) GitLab stalls: this fetch has no AbortSignal, the handler promise leaks for the connection's lifetime; the socket client times out but the daemon keeps the pending promise (and the sops-decrypted token in the closure) alive indefinitely. Every other rt-authored fetch in scope (Linear, ntfy, tray) uses `AbortSignal.timeout`; this one does not.

*Fix (small):* Add `signal: AbortSignal.timeout(30_000)` and follow GitLab's `x-next-page` header (or `Link`) to page until exhausted, or return `{ diffs, truncated: true }` when 100 rows come back. Prefer routing through `provider.restRequest` so the onRequest accounting hook sees it too.
*Fixer notes:* Two independent fixes needed, not one:
1. Add `signal: AbortSignal.timeout(30_000)` (or similar) directly to the `fetch()` call at line 143 ... this is the actual timeout fix. Routing through `provider.restRequest` (the suggested fix) does NOT add a timeout since `restRequest` itself (glance src/GitLabProvider.ts:1314-1318) has none either; it only buys the `onRequest` accounting hook. Either add the signal to the raw fetch, or fix `restRequest` upstream in glance and then switch to it ... don't assume switching alone solves the timeout half.
2. Also thread the handler's own `signal` param (already available via `Handler = (payload, signal?) => Promise` in handlers/types.ts:75 and passed by socket-server.ts:46/api-server.ts) into the fetch, so a client giving up (30s client-side timeout) actually cancels the in-flight GitLab request instead of leaving it orphaned.
3. For truncation: either follow `x-next-page`/`link` headers to page through all diffs, or at minimum return `{ diffs, truncated: true }` when exactly 100 rows come back, and have `commands/status/dashboard.tsx` surface that to the user rather than silently rendering a partial file map.
Test coverage to add: a fake GitLab response with 100 items should set `truncated`, and a fetch that never resolves should be observably cancelled when the handler's `signal` fires (can be tested without a real daemon by unit-testing the handler function directly with a mock provider/fetch).
*Partial mitigation today:* Client-side only: lib/daemon-client.ts:341's `DISCUSSIONS_TIMEOUT_MS = 30_000` bounds how long the UI waits and surfaces a "diffs timed out" error to the user ... but this only aborts the client's own connection to the daemon; it does not cancel or bound the daemon's outbound fetch to GitLab, which the handler cannot cancel since it never accepts the `signal` param the daemon plumbing already provides (lib/daemon.ts:329, handlers/types.ts:75).

#### S054 · P2 · hazard · `lib/daemon/handlers/secrets.ts:145`
**API token is loaded at two different times by two modules; a missing, unwritable, or rotated token file makes /api/secrets unrecoverable until restart**

Case A: ~/.mattstack/rt is not writable at boot (fresh machine, FDA not yet granted, permissions damage). api-server holds random token T1 and no file exists; every secrets:read call regenerates a new random token T2, T3..., so even a client that somehow knew T1 passes the HTTP gate and is then refused with `bad-token`. Case B: user deletes api-token to rotate it. The HTTP gate keeps demanding the old boot token (clients reading the new file get 401), while the handler demands the new file token (clients presenting the old one get `bad-token`). The VS Code extension logs `gate-failed` and falls back forever. Nothing tells the operator a restart fixes it.

*Fix (small):* Load the token once in one place (a small module-level singleton in api-auth.ts with an explicit reload) and have both api-server and the secrets handler read the same value; log at warn when persisting the token fails so the condition is visible.
*Fixer notes:* Fix as suggested: make api-auth.ts own a single module-level singleton (load once, cache in memory) with an explicit `reloadApiToken()` export, and have both api-server.ts:101 and secrets.ts:145 call the cached getter instead of api-server capturing a local const and secrets.ts re-invoking loadOrCreateApiToken() per request. Add a `ctx.log.warn` (or equivalent) inside api-auth.ts's write-failure catch (currently a bare `catch { /* best-effort */ }` at api-auth.ts:35) so an unwritable RT_DIR is visible in daemon logs instead of silently regenerating a token every request. Add a regression test that: (1) stubs a tokenPath whose writeFileSync always throws and asserts two successive loadOrCreateApiToken() calls return different values today (proves the hazard), then after the fix asserts they return the same cached value; (2) simulates deleting the token file mid-run and asserts api-server's gate and secrets:read's gate stay in agreement. Blast radius: any consumer of GET /api/secrets (VS Code extension per secrets.ts:14-18, deck, board) silently loses that endpoint until a manual daemon restart; nothing else in the daemon depends on API_TOKEN_PATH per the grep above, so the fix is contained to api-auth.ts/api-server.ts/secrets.ts.

#### S055 · P2 · hazard · `lib/daemon/handlers/status.ts:104`
**GET /api/repos runs execSync `git worktree list` per registered repo on the daemon event loop, unauthenticated**

Each call blocks the single daemon thread for N git spawns (tens to hundreds of ms each on a cold disk, seconds on a network volume or a repo with many worktrees). The route is token-free and CORS-open, so a page polling it, or the tray/console polling it, stalls every other request (events:wait wakeups, chat:pulse's 800ms hook budget, tray:status) and the tray shows the daemon as flapping. The project's own memory records this exact class ("daemon timers must use async runCapture, never execSync") as the cause of tray flicker and a MAT-222 wedge; this is a request-triggered instance rather than a timer-triggered one.

*Fix (small):* Add an async listWorktreesAsync using runCapture (lib/subprocess.ts) and use it here, or serve the worktree list from the registry rows the reconciler already maintains instead of shelling out per request.
*Fixer notes:* Fix as suggested: add an async variant (listWorktreesAsync using lib/subprocess.ts's runCapture, which already exists and is the daemon's documented execSync replacement) and swap it in at lib/daemon/handlers/status.ts:107, or serve from a cache the reconciler maintains instead of shelling out per request. Also worth noting for the fixer: lib/git-worktrees.ts has two near-duplicate functions (listWorktreeRoots and listWorktrees) both doing raw execSync ... an async replacement should probably cover both, since other callers of listWorktreeRoots (grep for it) have the identical hazard even though this specific finding is scoped to the "repos" handler's use of listWorktrees. Severity P2 is reasonable: real and live today (unauthenticated, blocks the sole event loop), but current blast radius is opportunistic rather than continuously triggered by an existing poller, and worst case is a self-recovering stall (KeepAlive/{SuccessfulExit:false} won't even trigger since the process doesn't exit) rather than data corruption or crash.
*Partial mitigation today:* None found. No cache, memoization, rate limit, or worker/thread offload guards this handler; needsToken() deliberately excludes GET /api/repos (by design, since it's a read), so there is no auth gate either.

#### S056 · P2 · hazard · `lib/daemon/handlers/worktree.ts:645`
**`rt worktree adopt` converts every foreign worktree into an rt-owned ephemeral that auto-disposes on MR merge**

A new user runs `rt worktree adopt` once to migrate. Their hand-made `git worktree add ../myrepo-hotfix` checkout becomes kind=ephemeral, state=claimed, disposal=merge. When the hotfix MR merges, the reactor auto-disposes it: kills its processes, renames it into .worktrees/.trash (stripped of node_modules), runs `git branch -D hotfix`, and drops the row. The user never opted that directory into rt management; the branch delete and process kill are not undone by the 14-day trash retention.

*Fix (small):* Adopt foreign trees as kind=unmanaged (or ephemeral with disposal="job") unless the path is under cfg.root; require an explicit --claim flag to take ownership of trees rt did not create.
*Fixer notes:* The suggested fix is directly implementable with types already in the codebase: `TreeKind` already has `"unmanaged"` (used by the reconciler at worktree-reconciler.ts:218) and `DisposalMode` already has `"job"` (worktree-reconciler.ts:506 already treats `disposal==="job"` as "the caller's to end, MR or no MR" and skips auto-dispose). Changing worktree.ts:645-650 to leave non-parked foreign trees as `kind:"unmanaged"` (or `ephemeral`/`disposal:"job"`) would need a matching update to lib/daemon/__tests__/worktree-handlers.test.ts:570-605, which currently hard-asserts the opposite (`kind:"ephemeral", disposal:"merge"` for a plain hand-made worktree) ... that test is effectively the regression test for the current hazardous behavior and will need to flip. If an explicit `--claim` opt-in is added instead, it needs threading through `parseAdoptArgs` (commands/worktree.ts:162-167), the `worktree:adopt` payload, and the handler's per-tree branch at worktree.ts:645. Either way, add a test asserting a foreign, non-parking-lot worktree survives `adopt` without becoming reactor-disposable.
*Partial mitigation today:* Partial, not categorical: dispose.ts guards 2-5 still apply on the auto path (no `force`) ... the tree must be git-clean (guard 2, "dirty"), the branch must show as merged/anchored on origin (guard 3), no fresh MR-attendant lease (guard 4), and a 10-minute post-claim grace window (guard 5, auto-only). These narrow the blast radius (a still-dirty or just-adopted tree survives one pass) but do nothing to protect a clean, already-merged-eventually hotfix tree, which is the reported scenario. Disposal also lands in a 14-day-recoverable trash rather than `rm -rf`, and `git branch -D` is reflog-recoverable pre-gc ... so the damage is not permanent-and-silent, but the process kill is not undoable and nothing warns the user before any of this happens.

#### S057 · P2 · hazard · `lib/daemon/hooks-guard.ts:84`
**fs.watch handles in hooks-guard and home-snapshot have no 'error' listener; an emitted error is an uncaught exception**

FSWatcher is an EventEmitter; an 'error' event with no listener throws to the event loop (EMFILE when a user with many repos exhausts descriptors, ENOSPC-class limits, a watched directory unlinked on some platforms). installCrashHandlers turns that into exit(1) and a launchd relaunch that re-arms the same N watchers and hits the same limit. Separately, refreshWatchedRepos is add-only: after `rt repos locate` moves a repo the old watcher on the dead .git dir is kept forever and status.watchedRepos over-reports.

*Fix (small):* Attach `watcher.on('error', ...)` that logs and closes/re-arms; make refreshWatchedRepos reconcile (close watchers whose configPath is no longer in the index).
*Fixer notes:* - Add `watcher.on("error", (err) => { log.warn({ err, repo: repoName }, "watcher error"); watchedConfigs.delete(configPath); try { watcher.close(); } catch {} })` in hooks-guard's startWatchingRepo, and an analogous handler in home-snapshot's armWatcher that sets `watcher = null` (so status().watching correctly flips false) and logs ... decide deliberately whether to auto-retry-arm or require the next runNow()/tick to lazily re-arm via the existing `!watcher` check at home-snapshot.ts:590.
- home-snapshot.ts's injected `WatchFn` type (line 100) only declares `{ close(): void }` on the returned watcher ... widen it (or cast at the call site) to also expose `.on()`/`.off()` so the error handler is type-safe against the real fs.watch return value, and so tests can inject a fake EventEmitter-like watcher and assert the daemon does NOT crash when 'error' fires.
- Fix refreshWatchedRepos to reconcile, not just add: compute the current index's live configPath set, then close+delete any `watchedConfigs` entry whose key is not in that set (covers both a relocated repo's stale old entry and a repo removed from the index entirely). Needs a test exercising a locate-then-refresh cycle asserting `watchedConfigs.size` returns to the correct count and the old FSWatcher's `.close()` was called.
- Note for reviewer: I could not empirically confirm in-session whether a live EMFILE hits fs.watch()'s synchronous throw path or its async 'error'-emit path in this Bun build (the ulimit-based repro didn't converge and was aborted to avoid burning real machine resources) ... worth a narrower, bounded repro (e.g. fewer, pre-created directories, explicit low `ulimit -n` verified via `ulimit -n` readback inside the child) before writing that detail into any fix commit's rationale.
*Partial mitigation today:* Partial, and only for home-snapshot.ts: tryArm() (home-snapshot.ts:339-350) wraps the armWatcher() call in try/catch, which absorbs a *synchronous* throw from fs.watch() at creation time (per its own comment, aimed at EMFILE/ENOSPC/ENOENT) and marks the module permanently inert rather than crashing. This does not cover an 'error' event emitted asynchronously after a successful watch() call, which is the actual mechanism this finding is about. hooks-guard.ts's startWatchingRepo (lines 65-98) has no try/catch of any kind, so it has no mitigation even for the synchronous case.

#### S058 · P2 · bug · `lib/daemon/pollers.ts:84`
**10s/30s scans run system-wide lsof plus per-repo git spawns at full cadence even with zero consumers**

A teammate installs mattstack with 20 repos indexed and never opens the tray panel. The daemon nonetheless spawns ~6x20 + 2x20 = 160 `git worktree list` processes per minute plus 8 system-wide lsof invocations per minute, forever, on battery. macOS lsof over a few thousand fds routinely costs 1-5s of CPU per run; on a busy machine the 10s scan's lsof takes longer than the tick and the in-flight guard simply drops ticks while CPU stays pegged. The tray's `system-processes` handler adds uncoalesced `scanner.refresh()` calls (handlers/system-processes.ts:92-93, no in-flight guard) on top when the panel is open. This is also the widest exposure surface for the lsof-wedge latch in the runCapture finding.

*Fix (medium):* Gate the two scans on demand: run at 10s/30s only while a WS client is connected or a system-processes/ports read happened in the last N minutes, otherwise back off to 2-5 min. Share one worktreeMap between the two scanners with a short TTL instead of rebuilding it per tick. Make `refresh()` coalesce on the scanner's own in-flight promise. Cache loadNotificationPrefs for the tick.
*Fixer notes:* Everything in suggested_fix is directly actionable against the traced code:
- Demand gating: pollers.ts would need a real signal, since wsClients (api-server.ts) isn't visible to it and the tray doesn't even use the WS channel ... plumb through either a "last REST hit on /api/ports or /api/processes/system" timestamp (track it in api-server.ts's fetch handler) or expose wsClients.size, then have refreshPortCache/refreshSystemProcesses check it before running the expensive gather()/scanListeningPorts() body, backing off to a multi-minute interval otherwise.
- Shared worktreeMap: both port-scanner.ts:222 and system-process-scanner.ts:378 independently call buildWorktreeMap(repos) every tick with no cache between them; a short-TTL (e.g. 30-60s) shared cache keyed by the repo index would cut the git spawns roughly in half without changing either scanner's freshness guarantees materially.
- In-flight coalescing on refresh(): add a promise-memo inside SystemProcessScanner.refresh() itself (not just the poller-level scan() guard in pollers.ts), since concurrent tray-driven HTTP requests hit refresh() directly with no serialization ... this is the more concrete, currently-reachable race, worth fixing regardless of the cadence question.
- notifier prefs cache: loadNotificationPrefs() has no TTL/cache; even a few-hundred-ms memo (invalidated on saveNotificationPrefs) would remove the sync multi-file read from the hot 10s path.
Tests to add: a pollers.ts unit/integration test asserting scan functions are NOT invoked when idle (no WS clients, no recent REST reads) after the backoff lands; a system-process-scanner test asserting concurrent refresh() calls coalesce into one gather(). Blast radius of a fix is confined to lib/daemon/pollers.ts, lib/daemon/system-process-scanner.ts, lib/port-scanner.ts, lib/daemon/api-server.ts (to expose a demand signal), and lib/notifier.ts ... no schema/state.db changes implied.

#### S059 · P2 · hazard · `lib/daemon/pollers.ts:98`
**Hooks-scan poller has no in-flight guard: 60s sweeps overlap unboundedly when git is slow**

checkAndRepairHooksPath does a runCapture git config with a 5s timeout per repo, and primeTeamTrackingIdentityMap runs deriveRepoIdentity (git) per repo. With 15 repos on an unmounted/sleeping volume each git call hits its 5s timeout: one sweep takes 75s+, the 60s interval fires again, sweeps stack, each one spawning N git processes; memory and process count grow until the volume wakes. The same file guards the two neighbouring pollers, so this is an omission, not a design.

*Fix (small):* Add a `hooksScanInFlight` guard like the other two pollers, wrap the body in try/catch -> log.warn, and either memoize path-kind identities with a short TTL or prime the team map only when the repo index changed (compare a hash of the index).
*Fixer notes:* Add a `hooksScanInFlight` boolean exactly matching the `portScanInFlight`/`processScanInFlight` pattern at pollers.ts:37-38 and 41-42/51-53: check-and-return at the top of the interval callback, set true, `try { ... } finally { hooksScanInFlight = false; }`. Wrap the loop body in try/catch → `log.warn` (mirroring lines 49-51/72-74) as defense-in-depth, though `checkAndRepairHooksPath` itself never throws today (its only I/O goes through `runCapture`, which swallows and returns exitCode:-1 rather than throwing ... subprocess.ts:56-58/72-74). Consider also parallelizing the per-repo loop (`Promise.all` instead of sequential `for...await`) to cut worst-case sweep time by ~N×, and/or lowering `runCapture`'s timeoutMs for this specific call so one wedged repo doesn't cost 5-10s. No test file exists for pollers.ts; a regression test should fake `checkAndRepairHooksPath` to hang past one interval tick and assert it's only invoked once concurrently.
*Partial mitigation today:* None for this poller specifically. The codebase has the *pattern* for the fix already present two functions above it in the same file (portScanInFlight/processScanInFlight, pollers.ts:37-54) and previously applied the identical fix to `refreshCache` per docs/daemon-runner-health.md:64-67 ("P2 ... refreshCache has no in-flight guard ✅ FIXED"). `runCapture`'s timeoutMs (lib/subprocess.ts:60-62) prevents any single git call from hanging forever, and `primeTeamTrackingIdentityMap`'s per-repo git derivation is process-memoized for remote-kind repos (identity.ts:110), which caps its contribution to the overlap after the first successful sweep ... neither of these addresses the missing in-flight guard on the interval itself.

#### S060 · P2 · hazard · `lib/daemon/shutdown.ts:66`
**Every signal exits 0, so an externally killed daemon is treated by launchd as a successful exit and stays down**

launchd's contract with {SuccessfulExit:false} is: restart only on non-zero exit. The daemon reports 0 for every signal, so it cannot distinguish 'launchd asked me to stop' (kickstart -k / unregister, where the exit code does not matter) from 'something else killed me' (pkill, a user script, memory pressure tooling, another flavor's eviction). In the second case the user loses the daemon silently and the `start` verb cannot bring it back (previous finding). The tray comment still believes KeepAlive=true would undo an HTTP shutdown, which is the opposite of what the rendered plist does; the next person tuning this will reason from a wrong premise.

*Fix (trivial):* Reserve exit 0 for the explicit `shutdown` verb and the shim's stand-down contract; exit 1 (or a distinct code) from the SIGTERM/SIGINT/SIGHUP handlers so KeepAlive respawns after an external kill (launchd-initiated stops are restarts or unregisters anyway, where the code is irrelevant). Fix the AppDelegate comment to name SuccessfulExit=false.
*Fixer notes:* Two call sites duplicate the same cleanup+exit(0) sequence and both need to stay exit(0) intentionally: the "shutdown" IPC verb (lib/daemon.ts:350-360) and gracefulExit (shutdown.ts:62-70). The fix is narrower than "make shutdown.ts always exit 1" ... it's specifically the signal handlers that need to stop assuming every SIGTERM/SIGINT/SIGHUP is a sanctioned stop. Since the deliberate "stop" path (rt-tray) goes through SMAppService.unregister() (DaemonLifecycle.swift:60-67), not a raw signal, and an unregistered job's exit code is irrelevant to launchd, changing gracefulExit's SIGTERM/SIGINT/SIGHUP handling to exit(1) (or a distinct sentinel code) should not break the intentional-stop flow ... only the explicit "shutdown" IPC verb needs to keep exit(0). Recommend: give gracefulExit a way to know whether the exit was requested through the "shutdown" verb (e.g. a shared flag set by that handler before it calls cleanup) vs. a bare OS signal, and default to non-zero for the bare-signal case. Also fix the stale comment at rt-tray/Sources/AppDelegate.swift:625-627 to say KeepAlive:SuccessfulExit=false, not KeepAlive=true ... a future reader will otherwise reason from a config that hasn't existed since commit 9fd74664 (Aug 21). This can't be verified end-to-end without actually registering a real launchd job and sending it a real SIGTERM (explicitly prohibited by this audit's hard rules), so the eventual fixer should test it live once implemented, not just by unit-testing the TS handler in isolation.
*Partial mitigation today:* Partial only: rt-tray/Sources/AppDelegate.swift:605-621 (restartDaemon action) + DaemonLifecycle.swift:74-88 (launchctl kickstart -k, bypasses KeepAlive/SuccessfulExit entirely) let a human manually revive the daemon once the tray's 10s poll (AppDelegate.swift:785-787) shows it down. This does not restore the automatic-respawn guarantee KeepAlive is meant to provide; it requires a person to notice and act.

#### S061 · P2 · bug · `lib/daemon/system-process-scanner.ts:234`
**A single failed lsof resets every runaway-detection window**

Runaway detection needs 30 consecutive 10s samples above threshold (sustainMs 5 min). One lsof timeout or spurious non-zero exit (lsof exits 1 whenever any fd could not be stat'd, which is routine) returns an empty gathered list, `scan` treats it as 'no processes' and deletes every tracked pid, so `firstSeen` and the sample window restart from zero. On a machine where lsof fails even once every few minutes, the 'Runaway process' notification can never fire, and the tray briefly shows an empty process list.

*Fix (small):* Distinguish 'scan failed' from 'scan found nothing': have gather() return null on lsof/ps failure and make scan() keep `tracked` and `lastResult` untouched (and not advance lastScanAt) on null.
*Fixer notes:* Suggested fix in the finding is correct and minimal: change `gather()`'s failure path to signal "scan failed" distinctly from "found nothing" (e.g. return `null` from `getAllRepoPids`/`gather` on the lsof failure branch), and in `scan()` short-circuit before touching `this.tracked`/`this.lastResult`/`lastScanAt` advancement when that signal is set, instead of running the prune loop against an empty `currentPids`.

Also worth doing while touching this: add a scan()-level test that stubs `runCapture` (or injects a fake `gather`) to fail once and asserts `tracked` map size and `firstSeen` are unchanged across that tick ... `lib/daemon/__tests__/system-process-scanner.test.ts` currently only tests the pure parsing functions, not `scan()`'s failure-resilience behavior, so this class of regression has no coverage today.

Severity: upgraded from P3 to P2. The failure is not a partial degradation ... a single bad tick fully and silently defeats the runaway-process safety notification for as long as failures recur faster than the 5-minute window can complete, which is exactly the scenario (loaded, unfamiliar user machines) this audit is concerned about. It doesn't crash the daemon or corrupt persistent state (self-heals as soon as lsof succeeds again), which is why it isn't P1/P0, but a complete (not partial) silent loss of a named stability feature under plausible real-world load is more than a minor/cosmetic edge case.

#### S062 · P2 · hazard · `lib/daemon/user-path.ts:40`
**PATH scrape via `$SHELL -ilc` silently falls back to launchd's minimal PATH when .zshrc execs another shell or attaches tmux; no warning, node never probed**

`-i` sources ~/.zshrc, which on many machines ends with `exec fish`, `exec nu`, or `[[ -z $TMUX ]] && exec tmux`. The exec replaces zsh before the `-c` body runs; the replacement shell reads stdin (/dev/null under launchd) and exits, or tmux fails without a tty, so stdout is empty and the `|| resolvedPath` keeps launchd's /usr/bin:/bin:/usr/sbin:/sbin. Prompts that block (an ssh-agent passphrase, a `read` in .zshrc) instead hang the boot for the full 30s timeout, on every boot and every crash-loop iteration. Either way the daemon proceeds with a PATH that has no node/pnpm/bun, and the only trace is an info line with `entries: 4` and hasNode absent from the probe list; the worktree pool then wedges exactly as docs/superpowers/specs/2026-08-21-daemon-spawn-env-contract.md describes ('env: node: No such file or directory' across every on-deck tree), now for any user with a common .zshrc idiom.

*Fix (small):* Detect the fallback (output empty, or identical to the baseline) and log at warn with the remedy; add node/git/bun to the probe and warn when a required tool is absent; prefer a non-interactive login shell (`-lc`) first and only try `-ilc` as a second attempt with a short timeout; long term, persist a toolchain contract at setup time as that spec proposes, so the daemon never depends on scraping an interactive shell.
*Fixer notes:* The confirmed, demonstrated hazard alone (silent fallback on `exec`-ing `.zshrc`s, no escalation) is enough to keep this P2 as-is; the suggested_fix in the original finding is directionally right (detect fallback/no-change-from-baseline and log at `warn`; treat missing `node`/`git`/`bun` as a warn-worthy condition, not just an info field) but should drop the "prefer `-lc` before `-ilc`" framing as the primary fix, since `-lc` alone would miss PATH exports many users only put in `.zshrc` (that trade-off is exactly why this file chose `-ilc`, per its own header comment). A regression test should assert `resolveUserPath` given a `.zshrc` that execs into a process producing empty output falls back to the baseline verbatim (this can be done by pointing `HOME`/`ZDOTDIR` at a scratch dir the same way I did here) and that this condition is distinguishable in the logged output. Also correct the finding's title before it's filed: "node never probed" is contradicted by the finding's own quoted evidence.

#### S063 · P2 · hazard · `lib/daemon/worktree-reconciler.ts:137`
**Reconcile prunes rows and runs `git worktree prune` for trees on a temporarily missing path, orphaning the directory and poisoning its name**

A user sets root: "/Volumes/Work/wt" (or a network mount). The volume is unmounted for a minute; the next pass runs `git worktree prune` (git drops the admin entry for the unreachable path) and step (a) deletes the registry row, taking claim/owner/state with it. When the volume returns, /amber is a directory with a `.git` file pointing at a pruned gitdir: unusable as a worktree, invisible to the registry, and holding GBs of node_modules that nothing reaps (not `.trash-`). Its name is now 'unused', so pickName can select it and `git worktree add` fails with "already exists", charging createBackoff up to 30 minutes per attempt.

*Fix (medium):* Treat 'git listed it but the path is missing' as a transient: skip `git worktree prune` and the registry prune for paths whose parent root is unreadable, and mark the row `missing` instead of deleting it; only prune after N consecutive passes.
*Fixer notes:* Suggested fix direction in the finding is on the right track but must cover both proximate causes, not just the explicit `git worktree prune` call: (1) reconcile step (a) at worktree-reconciler.ts:192-207 must not treat "not in `listWorktreesAsync`'s result" as equivalent to "git ground truth says gone" ... that result is already existsSync-filtered by git-async.ts:128, so the registry-side fix (a `missing` state / N-consecutive-pass threshold before dropping the row) is necessary regardless of whether the `worktree prune` call itself is also deferred. (2) Skipping `git worktree prune` conditionally (e.g., only prune entries whose containing root is confirmed readable) still leaves the existsSync filter in git-async.ts as a second path to the same bug, so that filter (or its caller) needs the same "distinguish gone-per-git vs stat-failed-this-pass" treatment. (3) Independent of this finding: scrapTree's collision-cleanup path (create.ts -> trash.ts) does an unconditional rm -rf with no dirty check when it happens to land on a directory it didn't create ... whoever fixes this should also add a guard there (e.g., check the directory is actually a git worktree / has no unexpected extra content before trashing it in `scrapTree`), since that is the actual data-loss vector once the registry desyncs. Add a regression test that simulates a worktree path returning `existsSync === false` for one pass and confirms the registry row survives N-1 misses.

#### S064 · P2 · hazard · `lib/daemon/worktree-reconciler.ts:725`
**Idle-main freshen stashes edits made during the fetch window and runs installs in the user's live checkout every 5 minutes**

The user's main clone sits on main with a clean tree at candidacy time. freshenOne runs `git fetch origin main` (up to 5 min); meanwhile the user starts editing. The re-classify finds blockers, `git stash push -u` removes their edits from the working tree, `merge --ff-only` moves HEAD, `stash pop` conflicts because the ff touched the same file: the edits now live only in a stash the user never made, with a warning only in the daemon log. If pnpm-lock.yaml changed, `pnpm install` then runs in the user's checkout under a login zsh while they may be running their own install or dev server. This repeats on every 5-minute tick.

*Fix (small):* Do not freshen `main` when its mtime/status changed since candidacy (re-run classifyDirtyAsync under the lock BEFORE fetch and abort on any blocker rather than stashing); gate idle-main freshen and ready steps behind an explicit opt-in setting; emit a notification when a stash is left behind.
*Fixer notes:* Fix should move the "is it still clean" decision to right before the merge, inside the lock, using a check that reflects the SAME thing candidacy checked (blockers.length === 0) ... and on failure, abort (call `fail()`, skip merge/ready-steps, no stash) rather than stashing and proceeding. Concretely: in `freshenOne`, split the post-fetch `classifyDirtyAsync` call into "discard-only tidy" (safe, keep as-is) vs. a genuine blockers check that, if nonzero, bails out entirely for a `kind: "main"` tree instead of stashing (on-deck ephemeral trees are a different risk profile since nothing else has a legitimate reason to be editing them).

Also fix: a failed `stash pop` must be treated as a hard failure of the freshen attempt ... call `fail()`, skip the ready-steps phase, and `emit(...)` a user-visible event (not just `log.warn`) so tray/CLI/chat can surface "your edits on `` were stashed and didn't reapply cleanly, run `git stash pop `."

Longer-term: gate idle-main freshening behind its own explicit opt-in, separate from `rt.worktrees.onDeck` for ephemeral pooling, since today declaring an on-deck pool silently opts the user's live primary checkout into unattended fetch+merge+install too.

Tests to add: (a) a `freshenOne`/`freshenRepo` test where the working tree is mutated to become dirty during the fetch (fake a slow fetch, e.g. via a hook or an already-async test double) and assert freshen aborts with no stash and no HEAD move; (b) a test where `stash pop` is forced to conflict and assert ready steps never run and the pass is NOT recorded as `worktree:freshened`.
*Partial mitigation today:* Partial only: `freshenRepo`'s post-candidacy, pre-`freshenOne` revalidation under `withTreeLock` (worktree-reconciler.ts:824-831, tested at worktree-reconciler.test.ts:1104-1153) catches a registry-field change (e.g. an on-deck tree becoming `claimed`) between candidacy and lock acquisition. It does not catch, and was not designed to catch, a working-tree dirt change on the SAME tree during freshenOne's own multi-minute fetch ... that class of staleness is only ever detected post-fetch (line 725), by which point the code stashes rather than aborting.

#### S065 · P2 · bug · `lib/daemon/worktree-reconciler.ts:1121`
**kick() during an in-flight reconciler pass is dropped, defeating replenish-after-claim**

runOnce() is one serial pass over every repo (reconcile, reactor, freshen with a 5-minute fetch timeout, replenish with multi-minute ready steps, reap). A provision that claims the last on-deck tree while that pass is running calls kick(), which returns because inFlight is set and nothing is queued. The pass may have already processed that repo's replenish step, so the pool stays empty until the next cache refresh tail (up to 5 min) plus a full pass; the next provision in that window pays a cold create. The comment in the handler promises the opposite.

*Fix (trivial):* Queue a kick that arrives mid-pass (reuse kickQueued for the inFlight case, firing once in finally), or make kick(repoName) per-repo so the reconciler can re-run just that repo after the current pass.
*Fixer notes:* Fix should extend the existing `kickQueued` coalescing to the `inFlight` branch, not just the `hold` branch: set `kickQueued = true` when `kick()` is called while `inFlight` is set, and check/consume it in the `.finally()` of the running pass (fire one more `runOnce()` if a kick arrived during the pass), mirroring what `withReconcilerHeld`'s release already does for the hold case. A cheaper alternative matching the suggested_fix: make replenish repo-scoped so a mid-pass kick can re-run just the claiming repo's replenish step instead of a whole new pass.

Any fix must not break the existing "two synchronous kicks collapse to one pass" test (`worktree-reconciler.test.ts:416`) ... that case still needs to short-circuit to a single pass when both kicks land before the pass has done any work, only a kick arriving after this repo's replenish step already ran should trigger a follow-up. Add a new test that: starts a pass, lets it run past a target repo's replenish step (e.g. via a slow/mocked create or a multi-repo repoIndex so the target repo is processed early), calls `kick()` again mid-pass, and asserts a second pass actually runs (or that repo's pool actually replenishes) once the first pass completes ... this is the scenario current tests don't cover. Also worth adding the same coalescing for `worktree:dispose`'s kick (line 530 of worktree.ts), which shares the identical gap.

#### S066 · P2 · hazard · `lib/deps/links.ts:152`
**Boot-time link reconcile unlinks mattstack's deck/gitq/rt whenever a same-named binary appears on PATH**

userCopyOnPath matches by NAME only. Kong's API tool is `brew install deck`, a common install on backend teams; a user who has it (or any other `deck`, `gitq`, or `rt` on PATH) gets mattstack's ~/.local/bin/deck link removed at the next daemon boot with only an info log line, and path.link refuses to recreate it ('user-copy'). Skills and docs that tell the user to run `deck ...` now run Kong's deck; deck.managed and cron triage keep working internally because resolveTool prefers the bundle, so the breakage is only visible from the shell and never in `rt verify`.

*Fix (small):* Only auto-unlink when the user copy is verifiably the same product (e.g. ` --version` reports a mattstack banner), never for `rt`, and add a verify row that reports a shadowed DEFAULT_EXPOSED tool.
*Fixer notes:* Fix should live in userCopyOnPath's identity check (resolve.ts) or as a new gate in reconcile() itself ... e.g. only treat a same-named PATH entry as "the user's genuine copy" after some product-identity probe (version-string banner check, or at minimum require the candidate to be a bona fide executable that isn't itself unresolvable/broken), and special-case "rt" the same way link() already does for dev-mode. Also worth extending the existing `tool.rt-link`-style row pattern in lib/setup/validators/tools.ts (or rt-health.ts) to cover the full DEFAULT_EXPOSED set (gitq, deck) rather than adding a net-new mechanism, and consider making that row `required:true` (or at least CI-visible) rather than purely cosmetic, since a shadowed default-exposed tool is a real functional regression for shell/script/skill callers even though internal daemon machinery (which calls resolveTool() directly, not the PATH link) keeps working. Add a regression test mirroring lib/deps/__tests__/links.test.ts:250 but with a *different* fake binary at the colliding name (not a real "gh") to prove identity, not just name, gates the removal.
*Partial mitigation today:* Partial, tool-specific, non-blocking: lib/setup/validators/rt-health.ts:112-137 `tool.rt-link` row surfaces exactly this state for "rt" (only) as a `required:false` warning in `rt verify`/setup status ... it does not fail CI and a user could miss/dismiss it as cosmetic. No equivalent row exists for "gitq" or "deck".

#### S067 · P2 · hazard · `lib/dev-mode.ts:84`
**Any '#!' script at ~/.local/bin/rt parks the prod daemon forever on a fresh machine**

A new user who already has a personal shell script named `rt` in ~/.local/bin (a common personal-tools location) installs mattstack.app before running the CLI post-install. With `mattstack.mode` unset, the prod daemon derives 'dev' from the script's shebang and parks: one info line 'parked: this machine's intended mode is dev … flip with: rt settings dev-mode prod', then silence every 30s. The tray shows 'Daemon: not running', `rt daemon status` says not running, and the remedy text refers to a dev mode the user has never heard of. Post-install's installRtBinary would overwrite the script with a symlink, but a dotfiles manager will put it back.

*Fix (trivial):* Make the wrapper signature specific (check for the `Written by rt settings dev-mode` marker commands/settings.ts:523 writes, not just '#!'), and have the parked log line say what file caused the derivation so the message is actionable.
*Fixer notes:* Fix both call sites together or the trap only half-closes: lib/dev-mode.ts:84 (`currentMode()`) and lib/deps/links.ts:46-50 (`isDevModeWrapper()`) both need to stop trusting a bare "#!" prefix.

The suggested_fix's marker pointer needs correcting before anyone implements it: the "Written by `rt settings dev-mode`" comment (commands/settings.ts:523) lives in `renderDevModePreload()`'s output, written to DEV_MODE_PRELOAD (`~/.rt/dev-restore-cwd.ts`, settings.ts:360) ... a completely different file from the wrapper being checked. The actual wrapper content is `renderDevModeWrapper()` (settings.ts:508-519), currently a bare `#!/bin/zsh` script with zero distinguishing content. A real fix adds a marker line to THAT function's output (mirroring lib/deps/links.ts's own LINK_TAG convention, which already exists precisely to let isDevModeWrapper() exclude rt's own tagged wrapper scripts from this check) and updates both checks to look for it instead of a bare "#!".

Test fallout: lib/__tests__/dev-mode.test.ts's "reports dev when the wrapper exists at ~/.local/bin/rt" and "currentMode reads through the link" tests currently assert the buggy behavior using a generic `#!/bin/sh\nexit 0\n` / `wrapper.sh` fixture with no marker ... these fixtures need updating to use the real marker once one exists, or a correct fix will fail them (correctly).

Also worth adding: a regression test that plants an arbitrary unrelated "#!" script at ~/.local/bin/rt and asserts currentMode() reports "prod" (not "dev"), which is the actual bug scenario and is not covered today.

Independently of the marker fix, lib/daemon/park.ts:53-56's parked log line should name the actual file (`rtBinaryPath()`) it derived "dev" from, per the original suggested_fix ... useful regardless of whether the marker fix lands, since any future misclassification should stay diagnosable.
*Partial mitigation today:* None prevents the park. The only escape hatch is running the CLI via its absolute bundle path (e.g. `/Applications/mattstack.app/Contents/MacOS/rt settings dev-mode prod`), which permanently pins `mattstack.mode` at commands/settings.ts:677 and stops further wrapper-derivation ... but this bypass isn't surfaced anywhere in the tray or in the parked log line, and the printed remedy text ("flip with: rt settings dev-mode prod") does not mention that it must be run via absolute path, since the bare `rt` on the affected user's PATH resolves to their own foreign script.

#### S068 · P2 · hazard · `lib/endpoint/allocator.ts:39`
**Endpoint claim liveness trusts a recorded pid across reboots; pid reuse blocks pool ports indefinitely**

Worktree A claims role 'web' port 3001 with pid 48213. The machine reboots; A's dev server is gone. Days later pid 48213 is reused by an unrelated process (macOS pids climb toward 99999 and wrap). Every endpoint:claim from another worktree computes isLiveClaim(A)=true, adds 3001 to `blocked`, and with a 3-port pool and three such stale claims returns 'no free port in pool ... (3 declared, 0 free)' even though canBind would succeed. The claim is only pruned when that random process exits. No TTL, no boot id, no process identity check.

*Fix (small):* Persist the process start time (ps -o lstart / kinfo_proc) or a boot id alongside pid and treat a mismatch as dead; or require both pid-alive AND a matching cwd/command for liveness when the port is not listening.
*Fixer notes:* Fix should add a second liveness signal at claim time and compare it at prune time, e.g. capture process start time (`ps -o lstart= -p ` or a `kern.proc.pid.` sysctl shim) alongside `pid` in `EndpointClaim`, and require it to match in `isLiveClaim` before trusting a bare pid match ... a mismatch means the pid was recycled, treat as dead. This needs a schema change to `endpoint_claims` (new nullable column, additive per the repo's `IF NOT EXISTS`-only migration rule in CLAUDE.md) and a corresponding change to the `Probes` interface/`defaultProbes`/test fixtures in lib/endpoint/__tests__/allocator.test.ts (the \"ancient claim with a live pid is live\" test would need updating to also carry/match a start-time field). Cheaper stopgap worth calling out separately: wire `endpoint:release` to a CLI verb (e.g. `rt endpoint release --worktree  --force`) as a manual escape hatch, since today there is no way to clear another worktree's stale claim short of editing state.db by hand. Blast radius is confined to lib/endpoint/*, its daemon handlers, and endpoint_claims ... does not touch other subsystems.

#### S069 · P2 · bug · `lib/enrich.ts:103`
**branch_cache is keyed by bare branch name: two tracked repos sharing a branch name silently drop one repo's MR data**

A team's ticket-named branches (`ACME-1287-add-photos`) are cut in both the frontend and backend repos, both tracked. Every 5-minute cycle repo A upserts the row, then repo B overwrites `repo`, `mr` and `ticket` for the same primary key; last index entry wins deterministically. Repo A's MR is never visible in the cache: its pipeline/approval/merge notifications never fire (notifier compares against B's MR), the worktree merge reactor skips it (`entry.repoName !== repoName` at worktree-reconciler.ts:597), the freshness mapping's `branchByIid` never finds it, and the discussions sweep never targets it. Nothing logs. The same applies to `main`/`develop` across every tracked repo. branch-cache.ts:11-15 documents the bare key as 'the cache's semantic key today'.

*Fix (large):* Move the primary key to (repo, branch) (schema bump with IF NOT EXISTS + copy, or a new table) and thread repoName through enrichBranches' `fetchAndCache` path (worktree path -> repo identity via the existing memoized deriveRepoIdentity). Until then, at minimum log a warn when a put() changes `repo` for an existing branch row so the collision is visible.
*Fixer notes:* Reproduction is solid; anchor any fix PR to the same six sites verified here: lib/enrich.ts:103 (writeEnriched), lib/state/branch-cache.ts:95-138 (UPSERT_SQL + put), lib/daemon/cache-refresh.ts:82-164 (per-repo loop), lib/daemon/worktree-reconciler.ts:594-597, lib/daemon/freshness.ts:430-432, lib/daemon/discussions-poller.ts:73-83, lib/notifier.ts:832-870.

Before proposing the `(repo, branch)` PK migration from the finding's suggested_fix, read the rejection rationale in docs/superpowers/specs/2026-08-20-rt-statedb.md:103 first ... the schema review already considered and rejected that exact change over duplicate-row and checkAndNotify-flapping concerns, so a fix needs to either address those specific objections or land the cheaper mitigation (warn-log on repo-changing overwrite in `put()`, and/or namespace the row key as `${repoName}:${branch}` when repoName is known, which sidesteps the flapping concern the spec raised without a schema bump). Don't reopen this as if the tradeoff were never discussed ... engage the documented objection directly.

Test coverage to add: a `cache-refresh` test with two repos in `repoIndex()` sharing one branch name, asserting the losing repo's `worktree-reconciler` merge detection and `discussions-poller` sweep target both still fire (or asserting today's absence of a warn log, if only the stopgap ships first).
*Partial mitigation today:* None functional. docs/superpowers/specs/2026-08-20-rt-statedb.md:103 and lib/state/branch-cache.ts:11-16 document the bare-branch PK as a knowingly-accepted tradeoff (rejected `(repo,branch)` PK because it "mint[s] permanent duplicate rows plus an undefined collapse tie-break that flaps checkAndNotify") ... this is a documented risk acceptance, not a code guard, and it does not include even the suggested-fix's minimal warn-on-overwrite log, which is confirmed absent from branch-cache.ts's `put()`.

#### S070 · P2 · hazard · `lib/home/age-key.ts:291`
**Keychain and sops spawns in the daemon have no timeout; a blocking dialog stalls loadSecrets for every caller**

On another person's Mac the login keychain is locked (screen lock with "lock keychain when sleeping"), or the keychain item's ACL was created by a different signed binary (dev shim vs mattstack.app), so `security find-generic-password -w` pops a GUI unlock/allow dialog and blocks until clicked. The daemon's `loadSecrets()` (freshness.ts:129/269, enrich.ts:298) awaits it with no deadline. reconcileFreshness, the coalesced cache refresh, and every mr/discussions/project-mrs handler queue behind that one spawn. The tray shows the daemon as up but every forge feature is frozen, with no log line, until the user notices a dialog behind other windows. runCapture has a 10s default timeout precisely for this reason; these two seams bypass it.

*Fix (small):* Give createRawAgeKeySeam.run and createRealSecretsExecSeam.run a timeoutMs (30s default) with kill-and-reject, mirroring runCapture; surface the timeout as a distinct error ("keychain prompt pending?") so the handler message tells the user what to look for.
*Fixer notes:* Give both `createRawAgeKeySeam().run` (lib/home/age-key.ts:281) and `createRealSecretsExecSeam().run` (lib/secrets/store.ts:468) a `timeoutMs` with a kill-timer mirroring lib/subprocess.ts's `runCapture` (setTimeout -> proc.kill(), cleared in a finally). Suggested default ~30s per the original finding is reasonable given these are interactive-adjacent (keychain prompts can legitimately take a few seconds for a human to click).

Two things the fixer should know that the original finding didn't call out:
1. Because `domainMemo` only guards against re-decrypting the SAME domain, a fix that only times out the spawn but throws a generic error will surface as a decrypt failure on every subsequent call too (readEncryptedRtSecrets propagates a thrown failure without falling back) - so make sure the timeout error is distinguished (e.g. a `KeychainTimeoutError`) and NOT cached into `domainMemo`, so a later retry (once the user dismisses the dialog) can succeed rather than being permanently poisoned by a one-time timeout.
2. Since callers don't coalesce concurrent slow-path calls across different handler invocations (only project-mrs.ts's mr:by-branch coalesces within one request), a burst of concurrent requests during a cold-start hang will spawn N independent `security`/`sops` children. A timeout fix should still be applied per-spawn (simplest, matches runCapture's existing pattern) rather than assuming a single shared in-flight promise; don't add global request coalescing as part of this fix unless scope is explicitly widened.
Tests to add: a fake `AgeKeySeam`/`SecretsExecSeam` whose `run()` never resolves, asserting the real seam's wrapper times out and rejects distinctly rather than hanging the test runner.

#### S071 · P2 · bug · `lib/rt-paths.ts:148`
**Machine settings key follows os.hostname(), which macOS changes with the network**

macOS's gethostname() returns the dynamic hostname whenever `scutil --get HostName` is unset (the default; this machine reports 'HostName: not set' and hostname 'Matthews-MacBook-Pro.local'). On a corporate LAN or VPN with reverse DNS the same laptop reports e.g. 'jsmith.corp.example.com', so machineKey() becomes 'jsmith-corp-example-com' and the machine store resolves to a different, empty file. Because the resolver re-reads stores on every call and the daemon reads on every tick, the moment the user joins that network: rt.repoTracking reads empty so grants() returns off and background sync stops for every repo; rt.cron triggers vanish; rt.worktreeApp and mattstack.appPath read unset; mattstack.mode reads unset and flavor derivation falls back to the wrapper. Any `rt settings set` while roaming writes a third profile. Nothing logs this; it looks like 'the daemon stopped tracking my repos at the office'.

*Fix (small):* Generate a stable key once at setup (uuid) and write it to the untracked ~/.mattstack/machine-key that machineKey() already honors, or derive from `scutil --get LocalHostName`/IOPlatformUUID rather than gethostname(); mirror the change in packages/rt-client/src/settings/paths.ts (parity test).
*Fixer notes:* The residual, worth-fixing part is that both the FALLBACK and the value the pin CAPTURES come from network-dynamic os.hostname(). Two independent improvements: (1) make the captured/fallback key robust ... mint a randomUUID or derive from IOPlatformUUID / `scutil --get LocalHostName` (user-set, stable, unlike gethostname) instead of the hostname slug; this closes the never-ran-init and pre-pin windows. (2) The fallback is SILENT (readStore->EMPTY_STORE, grants()->off, no log) ... emit a one-time warn when machineKey() falls through to the hostname branch so a stranded profile is diagnosable. Any change must be mirrored in packages/rt-client/src/settings/paths.ts (guarded by lib/__tests__/settings-paths-parity.test.ts) and keep isSafeMachineKeySegment identical on both sides. Do NOT change the key derivation for machines that already have a pin file (that would strand their existing user/local//). Note team-tracked repos (mattstack.tracking, team scope, not keyed by machineKey) survive a machine-store move via loadRepoTracking's team fold (repo-tracking.ts:242-258), so "every repo" is only true for repos tracked purely locally ... the common single-user case.
*Partial mitigation today:* The untracked ~/.mattstack/machine-key pin file: machineKey() reads it first and returns before ever calling hostname() (lib/rt-paths.ts:141-144, and packages/rt-client/src/settings/paths.ts:61-75). It is written by `rt home init` on every machine that lacks it ... fresh or a cloned second machine ... (commands/home.ts:600-708; lib/home/init-plan.ts:208-210 buildInitPlan writeMachineKey; lib/home/init-exec.ts:103-106 writes to ~/.mattstack/machine-key), and home init runs as a setup step in the installer (lib/setup/steps/home.ts:186-192). On this machine the file exists (matthews-macbook-pro). Once present it pins the key so os.hostname() roaming has no effect.

#### S072 · P2 · bug · `lib/state/busy.ts:40`
**isBusyError misses SQLITE_BUSY_SNAPSHOT/RECOVERY, so both write wrappers rethrow real lock conflicts**

Daemon runs chat:read -> readUnread's deferred transaction (chat-store.ts:483) SELECTs membership and max id, then UPDATEs last_read_id. Between those statements a CLI process commits (rt run enrichment via enrich.ts:105 writeEnriched, run-history append, endpoint claim). SQLite raises SQLITE_BUSY_SNAPSHOT (the busy handler is never invoked for this code). isBusyError returns false, so persistOrWarn/runCriticalWrite rethrow: the tray's notifications drain (notifier-store.ts:111, SELECT-then-DELETE under runCriticalWrite) errors instead of retrying, and agents polling chat:read/chat:join get a raw "database is locked" failure. busy.test.ts fabricates `code = "SQLITE_BUSY"` (lines 12, 21), so the suite cannot detect this.

*Fix (trivial):* Match the primary code: `code === "SQLITE_BUSY" || code?.startsWith("SQLITE_BUSY_")` (or `(errno & 0xff) === 5`). Add a busy.test.ts case that opens two real connections on a temp db and provokes a snapshot conflict rather than a fabricated error.
*Fixer notes:* Fix: `code === "SQLITE_BUSY" || code?.startsWith("SQLITE_BUSY_")` (covers SNAPSHOT and RECOVERY), matching the extended-code precedent already used at identity-migrate.ts:94. Add a busy.test.ts case that opens two real bun:sqlite connections on a temp WAL-mode db and provokes SQLITE_BUSY_SNAPSHOT for real (SELECT on conn A inside a transaction, commit a write on conn B, then write on conn A) rather than only fabricating `code = "SQLITE_BUSY"` as the current three tests do. Separately worth flagging to whoever fixes this: readUnread (lib/state/chat-store.ts:476-511) has no busy-error handling at all (not even the narrow one) ... a real gap, but orthogonal to this fix, since fixing isBusyError does nothing for a call path that never invokes it. Also worth a quick audit of other db.transaction() sites under persistOrWarn/runCriticalWrite (project-mrs-store.ts has several) to confirm none lead with a SELECT before a write the way drainNotificationQueue does; the ones checked here (postMessage, project-mrs writes) lead with a write statement and are not exposed the same way.
*Partial mitigation today:* None prevents the bug from firing. Two things bound its blast radius: bun:sqlite's db.transaction() correctly issues ROLLBACK on the uncaught throw (verified empirically: no rows lost from notify_queue, connection remains usable for the next call), so no data loss results; and lib/daemon.ts:329-343's handleCommand catches, logs at error, and rethrows to the request layer only, so no daemon crash results. The impact is bounded to one failed request/response instead of the intended graceful empty-array degrade.

#### S073 · P2 · hazard · `lib/state/chat-store.ts:483`
**Read-then-write daemon transactions use deferred BEGIN, producing snapshot conflicts busy_timeout cannot absorb**

bun:sqlite's db.transaction() issues a plain BEGIN, so the first SELECT pins a read snapshot. Any commit by another process (CLI enrichment, registry write, run-history append) before the first write turns the upgrade into SQLITE_BUSY_SNAPSHOT, which is immediate and unretryable within the transaction: the 250ms daemon busy_timeout never applies. Combined with the isBusyError mismatch the transaction fails outright; even with that fixed, the only correct recovery is to restart the whole transaction, which none of these callers do.

*Fix (small):* Use `db.transaction(fn).immediate(...)` for every transaction that reads before it writes (readUnread, joinRoom, archiveRoom, dmRoomFor, drainNotificationQueue, signIn) so the write lock is taken up front and SQLITE_BUSY (honoring busy_timeout) is the only contention error possible. Document the rule next to the transaction note in db.ts.
*Fixer notes:* Empirically verified fix direction: converting to `db.transaction(fn).immediate(...)` (bun-types sqlite.d.ts documents this exact API) takes the write lock at BEGIN, so the failure becomes an ordinary SQLITE_BUSY that the flavor's busy_timeout pragma genuinely absorbs. There is already in-repo precedent for BEGIN IMMEDIATE for exactly this race-safety reason: db.ts:432 `runMigrations` uses `db.exec("BEGIN IMMEDIATE;")` explicitly to avoid a racing upgrade. Recommend: (1) switch the 5 cited callers (plus presence-store.ts's signIn, which I spot-checked at lines 202-249 and is also read-then-write-shaped: prunePresence + SELECT_PRESENCE_BY_SESSION_SQL before any write) to `.immediate()`; (2) separately, whether or not `isBusyError` is widened to include SQLITE_BUSY_SNAPSHOT, note that for a caller wrapped in runCriticalWrite, retrying via `fn()` does start a brand-new transaction (bun:sqlite's `.transaction()`-returned callable begins a fresh BEGIN/COMMIT on every invocation), so widening isBusyError is a legitimate second layer of defense for whichever callers keep a retry wrapper, not merely cosmetic. Suggest adding a repro test to lib/state/__tests__ to lock in the fix; I built a minimal working one (not committed, per the read-only constraint) at /private/tmp/claude-501/-Users-matt-Documents-GitHub-repo-tools/b4c9633a-8de6-4ab8-ab01-cd48d0877058/scratchpad/busy-snapshot-repro.test.ts.
*Partial mitigation today:* None that actually covers this error code. runCriticalWrite (busy.ts:87-101) exists for drainNotificationQueue but is defeated by the isBusyError code mismatch on its very first attempt. handleCommand (lib/daemon.ts:329-343) plus the try/catch in socket-server.ts:37-48 and api-server.ts:137-203 prevent this from crashing the daemon process -- it only fails the one racing request -- which bounds severity but is not a fix for the underlying race.

#### S074 · P2 · hazard · `lib/state/db.ts:413`
**A throwing legacy importer inside the v0 migration permanently wedges the daemon and every CLI command**

An upgrade machine has a ~/.mattstack/rt/*.json that parses but whose shape makes an importer throw (a PK collision on a plain INSERT in project-mrs/notify-queue, a value bun:sqlite refuses to bind, a NOT NULL violation). The throw escapes importLegacyStores, runMigrations ROLLBACKs, user_version stays 0 and the file is never renamed, so every subsequent openStateDb (daemon boot and every CLI command) repeats the identical failure with no self-heal; the only escape is deleting the file by hand. Fresh users are unaffected (no legacy files). db.test.ts:317 covers only the absent-file case, not a throwing importer on a present file.

*Fix (small):* Wrap each importer in a SAVEPOINT; on throw, ROLLBACK TO the savepoint, warn with the file and error, and still push the path to `consumed` so it is renamed .migrated (spec: 'corrupt = warn + skip'). Add a db.test.ts case with a present file whose importer throws: db lands at SCHEMA_VERSION, other importers' rows survive, file renamed.
*Fixer notes:* 1) The suggested SAVEPOINT-per-importer fix is sound and matches the pattern already used elsewhere in this file for narrow exceptions to a broad rule (see addSectionsColumnIfMissing's carve-out from the "nothing but IF NOT EXISTS in V*_SCHEMA" rule, db.ts:283-296). Scope the SAVEPOINT to legacy-import specifically, not to migrations broadly ... docs/superpowers/specs/2026-08-20-rt-statedb.md:35 deliberately wants schema-DDL migration failures to stay loud/no-swallow; only the one-shot legacy-import step (spec line 127: "corrupt = warn + skip") should gain per-file isolation, so a fix should not blur that spec-level distinction. 2) Add the concrete regression case: a project-mrs.json with a repo whose `mrs` map has two keys normalizing to the same iid ("5" and "05") ... that is a real, reproducible throw today via project-mrs-store.ts:474/503, not a hypothetical. 3) Also close the same gap for any importer, not just project-mrs, since the general defect is "runMigrations has no per-importer isolation" ... a future importer or future legacy-file edge case hits the identical uncaught-throw-then-permanent-retry mechanism regardless of which store it is. 4) New db.test.ts case should assert: db lands at SCHEMA_VERSION, the OTHER five importers' rows still landed (proving one importer's failure doesn't roll back the whole migration under the fix), the offending file is still renamed .migrated (or renamed with a distinguishing suffix) per the "still rename" policy, and a warning names the file and error. 5) Separate hardening gap for the daemon owner: lib/daemon.ts's startDaemon() has zero try/catch around openBranchCacheStore() (line 419) ... even after this fix, ANY other throw during that same call still crash-loops the daemon under launchd with no operator-facing diagnosis beyond the crash log.
*Partial mitigation today:* project-mrs-store.ts:501-502 (`if (!Number.isFinite(iid)) continue;`) guards only the non-numeric-key shape of legacy iid data, per the comment at project-mrs-store.ts:495-500 acknowledging exactly this migration-wedge consequence. It does not guard duplicate-normalizing keys ("5"/"05") or any other importer's potential throw, and no SAVEPOINT/try-catch exists around any of the six LEGACY_IMPORTS entries in db.ts's importLegacyStores (400-417).

#### S075 · P2 · hazard · `lib/state/presence-store.ts:89`
**Presence prune and offline rule ignore the tail heartbeat: a >24h autonomous agent loses its handle mid-run**

last_seen_at is written only by chat:pulse, which the UserPromptSubmit hook fires on user prompts (spec: 'the session heartbeat only advances on user prompts, so a long autonomous turn starves it for hours while the tail keeps touching'). An agent driven by Monitor wakes for 24h+ with no user prompt (an overnight lane) keeps tail_seen_at fresh, but once last_seen_at is older than pruneMs (24h default) buddyStatus (line 150) reports it offline and the next chat:sign-in by ANY session (signIn calls prunePresence at line 227) deletes its row. Its handle is then free; a new session takes it; the agent's next chat:touch fails assertSessionOwnsHandle with 'handle reclaimed', its tail exits, and its pulse injects 'sign in again' into an agent that never left.

*Fix (small):* Make the prune and offline predicates consider the tail heartbeat too: `last_seen_at < ? AND COALESCE(tail_seen_at, armed_at, 0) < ?` in PRUNABLE_SQL, and in buddyStatus treat `max(lastSeenAt, tailSeenAt ?? armedAt ?? 0)` as the liveness timestamp for the pruneMs check. Alternatively have chat:touch also bump last_seen_at when armed (the spec forbids the reverse direction, pulse writing tail_seen_at, not this one).
*Fixer notes:* Suggested fix in the finding is sound and mirrors the pattern already proven correct in RECLAIMABLE_SQL (presence-store.ts:81): fold `COALESCE(tail_seen_at, armed_at, 0)` into PRUNABLE_SQL's second leg, and reorder buddyStatus (lines 148-156) to check armedAt/tail-liveness before the bare lastSeenAt>pruneMs branch (or equivalently compute a single "liveness timestamp" = max(lastSeenAt, tailSeenAt ?? armedAt ?? 0) and test that against pruneMs). Add a regression test alongside lib/state/__tests__/presence-store.test.ts:92-96 ("prune: the ghost path") that arms the row and sets tail_seen_at fresh at the 25h mark, asserting prunePresence returns 0 and buddyStatus returns "live" ... today nothing in the suite covers an armed+touching row past the 24h pruneMs boundary, which is exactly why this shipped. Also worth tracing whether the actual hooks.json (not found in this repo ... likely ships from a separate mattstack-skills/plugin repo) gates chat:pulse invocation on local session-file existence, since that affects whether the "sign in again" notice actually reaches the surviving agent's transcript or just silently exits the hook.

#### S076 · P2 · hazard · `lib/state/presence-store.ts:283`
**chat:sign-in without sessionId stores a NULL-keyed presence row that wedges the handle family**

A consumer (chat viewer via rt-client, or any agent hitting rt.sock) calls chat:sign-in with sessionId missing. The handler (lib/daemon/handlers/chat.ts:278-283) validates only baseHandle, so bun:sqlite binds undefined as NULL and `session_id TEXT PRIMARY KEY` accepts it (probed on 1.3.13). The row holds the UNIQUE handle. After 60 min it becomes reclaimable, but the reclaim path deletes `WHERE session_id = NULL`, which matches nothing, so the INSERT throws `UNIQUE constraint failed: chat_presence.handle`. chat:sign-in has no try/catch, so every sign-in under that base handle (derived from the repo/worktree dir, i.e. every agent in that checkout) returns a 500 for up to 24h until prunePresence removes the row. Reproduced in a scratch test against openStateDb on a temp file.

*Fix (small):* In chat:sign-in reject a non-string or empty sessionId (mirror commands/chat.ts requireValidSessionId), wrap signIn in the same try/catch the other handlers use, and add `session_id TEXT PRIMARY KEY NOT NULL` semantics by guarding the insert (`if (!sessionId) throw`). Optionally make prunePresence also delete rows with `session_id IS NULL`.
*Fixer notes:* Fix at lib/daemon/handlers/chat.ts:278-283: reject `chat:sign-in` when `sessionId` is not a non-empty string (mirror commands/chat.ts's `requireValidSessionId` shape check) before calling `signIn()` ... this is the cheapest fix and closes the hole regardless of which transport let the bad payload through. Belt-and-suspenders: presence-store.ts's `signIn()` itself could also guard (defense in depth for any other future caller), and `prunePresence`'s PRUNABLE_SQL could add an `OR session_id IS NULL` leg so a NULL-keyed row can't survive even the reclaim-only window if one is ever created by a future bug ... but the handler-level guard is the actual root-cause fix since the daemon has no schema-validation layer at all (verified: none of daemon.ts/socket-server.ts/api-server.ts/command-router.ts validate payload shape for any command, so this class of bug is possible for other commands' required fields too, not just this one ... worth a broader follow-up ticket).

Test to add: lib/daemon/__tests__/chat-handlers.test.ts ... `chat:sign-in` with `sessionId` omitted/undefined/empty-string returns `{ok:false}` with a clear error, never reaches presence-store. Also worth a presence-store.test.ts regression test asserting `signIn` refuses (or safely no-ops) rather than ever persisting a NULL/empty session_id row, so the UNIQUE-collision wedge can't reappear if the handler guard is ever bypassed or refactored away.

Note for whoever fixes this: my repro script lives outside the repo at /private/tmp/claude-501/-Users-matt-Documents-GitHub-repo-tools/b4c9633a-8de6-4ab8-ab01-cd48d0877058/scratchpad/presence-repro/repro.ts (HOME-isolated, safe to rerun) and reproduces the full chain in ~1s via `bun run repro.ts`.

#### S077 · P2 · hazard · `lib/worktree/config.ts:408`
**Team-declared on-deck pools start building worktrees on every new member's machine by default**

rt.worktrees is team-scoped and repo-scoped, so a team store declaring onDeck: 3 with ready steps for a monorepo applies to every teammate who clones it. The machine toggle rt.worktreeApp defaults to enabled:true, killProcesses:true when nothing is declared, so within one cache cycle of finishing setup the new member's daemon begins `git worktree add` x3 and runs `pnpm install` in each (multi-GB for the kind of monorepo the spawn-env spec describes), on a laptop that may have 10 GB free and no fnm/node yet. Creates fail, backoff engages, and the on-deck pool reports 'wedged' as in the 2026-08-21 incident, while killProcesses:true means a later dispose will SIGTERM/SIGKILL any process the user has cwd'd into that tree. Nothing asked the user.

*Fix (small):* Default rt.worktreeApp.enabled to false on a fresh machine and have setup (or the first `rt worktree` use) prompt to enable it; cap onDeck by a machine-scope ceiling; check free disk before a create.
*Fixer notes:* Two independent fix surfaces, both confirmed absent from the code:
1. Consent gate: `rt.worktreeApp` (machine-scoped, config.ts:364-417) should default to `enabled:false` on a machine that has never explicitly set it (distinguish "file exists and says false" from "never configured" ... the ownership-latch/ `probeAppConfigStore` plumbing at config.ts:373-380 already has the vocabulary for "unowned" vs "owned", so flipping the *unowned* default is a small, localized change) OR `rt setup` / first `rt worktree` use should write an explicit choice before any repo's pool is allowed to replenish.
2. Resource ceiling: `replenishAndShrink` (worktree-reconciler.ts:899-951) has no disk-headroom check before each `createTree` call and no machine-side cap independent of the team's declared `onDeck`. Add a free-space check (e.g. before the `while (budget > 0 …)` loop at line 910) and/or a machine-scope ceiling that clamps `cfg.onDeck` read at config.ts:199.
Also worth flagging separately to whoever owns this: `cfg.ready` (team-declared, resolved at create.ts:122 and worktree-reconciler.ts:766) is genuinely arbitrary shell command execution sourced from a shared team-scope settings file with no local review step before it runs on every other member's machine ... that is a materially different (and arguably higher-severity, supply-chain-shaped) risk than the on-deck resource-consumption issue this finding names, and may deserve its own ticket rather than being folded into this one's fix.
Test to add: a `config.test.ts`/`worktree-reconciler` test that layers a team-only `rt.worktrees` `onDeck` declaration on a repo with no local `worktreeApp` file/store at all, and asserts today's behavior (`enabled:true` → replenish proceeds) so a future default-flip fix has a red baseline.
*Partial mitigation today:* Partial only: exponential create-backoff (lib/daemon/worktree-reconciler.ts:646-657, 852-869) bounds retry frequency after a failure to one attempt per widening window (cap 30 min), so repeated install failures don't spin tightly. No mitigation exists for the initial, unconsented trigger: no disk-space precheck, no onDeck ceiling, and no setup-time default flip (lib/setup/steps/settings.ts and lib/setup/plan.ts never reference `rt.worktreeApp`).

#### S078 · P2 · bug · `lib/worktree/trash.ts:88`
**Dispose renames trees into /.worktrees/.trash even when nothing excluded .worktrees, polluting the user's repo**

A repo declares root: "~/wt" (or the user only ever ran `rt worktree adopt`, whose parking-lot disposals never go through createTree). disposeTree -> retireTree mkdirs /.worktrees/.trash and renames the whole tree (minus node_modules after strip) into it. `.worktrees/` was never added to info/exclude, so the user's main checkout now shows `?? .worktrees/` in git status, `git add -A` stages an entire second copy of the source tree, and classifyDirtyAsync(main) reports a permanent blocker so idle-main freshen never runs again.

*Fix (trivial):* Call ensureInfoExclude(repoPath, ".worktrees/") in retireTree (or before the first mkdir of the retention root) regardless of cfg.root; alternatively put the retention store under cfg.root and exclude that.
*Fixer notes:* Fix belongs in retireTree (trash.ts), not create.ts: call `await ensureInfoExclude(repoPath, ".worktrees/")` right before the `mkdir(root, ...)` at trash.ts:114, unconditionally (no cfg.root check) ... this is correct in all cases because retainedTrashRoot is *always* `/.worktrees/.trash` regardless of what cfg.root is, so the exclude pattern is always the right one to write. This single change covers both the custom-root case and the adopt-only-disposal case (create.ts's own call at line 58-60 becomes redundant for the default-root createTree case but is harmless/idempotent to leave in place, or can be removed). Add a regression test: dispose a tree with `rt.worktrees.root` set to something other than the default, then assert (a) `/.git/info/exclude` contains `.worktrees/` afterward and (b) `git status --porcelain` on repoPath does not list `.worktrees` as untracked. Also add a case that disposes via the adopt-style direct-disposeTree path with the *default* root to catch the broader gap.

#### S079 · P2 · hazard · `lib/worktree/trash.ts:196`
**Retention reaper treats any trailing - as an rt epoch and the crash sweep reaps `.trash-*` in a user-chosen root**

A user drops `backup-3` into /.worktrees/.trash to keep it 'with the other trash'; epoch parses as 3, `now - 3 > RETENTION_MS`, and it is rm -rf'd on the next pass despite the comment promising non-rt entries are kept. Separately, a repo configured with root: "${repoRoot}/.." makes reapTrashInRoots sweep the parent directory of every sibling repo for `.trash-*` names.

*Fix (trivial):* Require the epoch to be 13 digits in a plausible range (>= 2020) and, for the crash sweep, only reap `.trash--` where  is a registry-shaped single component; refuse roots that are ancestors of repoPath.
*Fixer notes:* The suggested fix is on the right track; two concrete additions worth calling out for whoever implements it:

1. For reapExpiredTrash: require the parsed epoch to look like an actual rt-written ms timestamp (e.g. 13 digits, or `>= some 2020-era floor`) before treating an entry as reapable-by-age; anything else should hit the existing "skipped an entry it did not write" warn path, not fall through. Add a regression test with a name like "backup-3" or "notes-42" (trailing digits, clearly not a real epoch) alongside the existing "not-rt-made" (no digits at all) case in lib/worktree/__tests__/trash.test.ts ... the current suite doesn't distinguish these two failure shapes.

2. For the crash sweep: reapRepoTrash (lib/daemon/worktree-reconciler.ts:1008) should refuse to pass `cfg.root` into reapTrashInRoots when it is not repoPath itself or a descendant of it ... a resolved path check (e.g. `path.relative` doesn't start with `..`) at the point cfg.root is consumed, not just at parse time, since sanitizeRoot (lib/worktree/config.ts) has no such check today and neither does the settings layer (`rt.worktrees` has no `pathGuardFields`, and pathGuardFields' PATH_LIKE check wouldn't catch a `${repoRoot}/..`-style value anyway since it only flags literals starting with `/` or `~`). Worth a test in worktree-reconciler's own suite (or trash.test.ts) that configures root as an ancestor of repoPath and asserts the sweep is refused/skipped, plus a settings-side test that `${repoRoot}/..` resolves without error today (documents the gap independent of the fix).

Both are independent fixes to independent functions and can land as separate small patches; neither touches the dispose/retire happy path.

#### S080 · P2 · bug · `rt-tray/Sources/AppDelegate.swift:641`
**Tray 'View Logs' opens a browser to a stale or dead port with no message when logdy is missing or already running**

A non-author user has no Homebrew/logdy. They click View Logs: `rt daemon logs` exits 1 into a pipe only TrayLog sees, then the tray opens http://localhost:5544 and the browser shows 'connection refused'. Alternatively a logdy from a previous session still owns 5544 following files from days ago (the memory notes record this exact footgun); the tray opens it and the user reads week-old logs believing they are live.

*Fix (small):* Tray: before opening, ask `rt daemon logs --probe --json` (new flag) which reports {viewer: 'logdy'|'none', port, filesTailed, newestLogMtime}; if no viewer is installed open Finder at ~/.mattstack/rt/logs or run the terminal viewer in Terminal.app; if 5544 is held by a logdy whose file set is stale, kill and respawn. CLI: on logdy exit within the wait window print 'port 5544 is already in use (pid N) ... stale viewer?'.
*Fixer notes:* Suggested fix in the finding is directionally right. Two independent gaps to close: (1) isLogdyUp() needs to distinguish "our logdy, tailing current files" from "anything on :5544" -- e.g. have `rt daemon logs` write a pidfile/marker with the file set + start time under ~/.rt/tmp, and have the tray/CLI compare the newest log mtime against what the running logdy was launched with, killing and respawning on mismatch. (2) The tray's failure path needs a real signal from the CLI: today runWebViewer's process.exit(1) is invisible to the tray (stdout nulled, only exit code logged); the CLI should either write a structured status file the tray can poll, or the tray should check the spawned Process's terminationStatus (available via the Process object created in spawnRtDaemonLogs at AppDelegate.swift:707-717, currently fire-and-forget) before falling back to opening the URL, and surface an alert/notification instead of silently opening a dead page. Also add a test for the "logdy binary missing" path specifically -- daemon-logdy-config.test.ts does not cover this branch, and today it degrades to a completely silent no-op from the user's perspective (nothing in TrayLog beyond a bare exit code, nothing on screen, no daemon crash).
*Partial mitigation today:* None found. isLogdyUp() (AppDelegate.swift:661-690) checks only TCP reachability, not file identity or freshness; runWebViewer (daemon.ts:912-955) has no logic to detect or replace a stale/foreign logdy instance already bound to 5544; no test covers the missing-logdy or stale-port branches.

#### S081 · P3 · bug · `lib/daemon-client.ts:33`
**daemon-client's module-level timed-out/refused flags are shared across concurrent queries, so error attribution is wrong under concurrency**

The status dashboard fires a 30s mr:action merge while a 2s `status` poll runs in the same process. The poll completes second and resets both flags to false (lines 43-44 run at the start of every query). When the merge's null result is inspected, lastQueryTimedOut() is false and the user is told "daemon unavailable" for a merge that actually succeeded on GitLab and simply exceeded the client window; the opposite mislabel is equally possible. daemonQuery's step-3 branch (line 229) reads the same shared flag to decide whether to attempt a restart.

*Fix (small):* Return the failure kind from trySocketQuery (e.g. `{ kind: "refused" | "timeout" | "other" }`) and thread it through daemonQuery's result instead of module state; keep lastQueryTimedOut() only as a deprecated shim.

#### S082 · P3 · bug · `lib/daemon-client.ts:232`
**Auto-start retries once after 300 ms but daemon boot takes seconds, so a successful start is reported as 'installed but not running'**

Daemon is down, tray is up. The CLI POSTs /daemon/start, the tray registers/launches, and the daemon spends well over 300 ms in parkUntilIntended's 1.5 s socket probe, state.db open, and the identity migration before rt.sock exists. The retry misses, the CLI prints the yellow "rt daemon is installed but not running. Run: rt daemon start" warning, and returns null so the command falls back to direct execution. The user runs `rt daemon start` and is told it is already running. commands/daemon.ts's own start path polls 12 × 250 ms for the same reason.

*Fix (trivial):* Poll rt.sock for up to ~3 s (existsSync then ping) before the retry, sharing the wait loop with commands/daemon.ts start(), and only warn if the socket never appears.

#### S083 · P3 · bug · `lib/daemon/api-server.ts:145`
**decodeURIComponent on /api/cache/:branch and /api/hooks/:repo/repair throws inside the try, returning 500 plus an error log for any malformed %-encoding**

GET /api/cache/%E0%A4%A produces a URIError, caught by the outer catch at 203-206: the client gets HTTP 500 `{ok:false,error:"URIError: URI malformed"}` and the daemon log gets an error-level line. A scanner or misbehaving page hitting this in a loop fills daemon.log at error level. Commit fabdd16c fixed only the runs route, which shows the routes are hand-parsed one at a time with no shared path-parameter helper; the next parameterized route will repeat it.

*Fix (trivial):* Extract a `pathParam()` helper that decodes and returns undefined on URIError, used by all three routes, and map undefined to the 404 envelope.

#### S084 · P3 · hazard · `lib/daemon/api-server.ts:151`
**hooks:repair REST route writes into a user repo's .git/config without the local token**

CORS is `*` and 127.0.0.1:9401 is reachable from any web page the user visits. A page can POST `/api/hooks//repair` and cause the daemon to run `git config core.hooksPath <rt shims>` inside the user's repo. The write is idempotent to rt's own managed state (only repos with hooks.json), so damage is bounded, but it is a mutating route outside the token gate the API index advertises ("Required on mutating routes"), and the same gap applies to GET `/api/cache?maxAgeMs=0` (cache.ts:50-52 awaits a full refresh) and GET `/api/sdm/recents` (spawns `sdm status`), which can be used to keep the daemon busy from a tab.

*Fix (trivial):* Add `/api/hooks/` (prefix) to needsToken, and treat `maxAgeMs` on the open GET route as requiring the token (or drop it from the REST surface; IPC callers keep it).

#### S085 · P3 · bug · `lib/daemon/api-server.ts:197`
**REST GET query strings reach handlers that type-check for number/boolean, so documented flags like ?maxAgeMs and ?refresh=true silently no-op**

A tray or script calls GET /api/cache?maxAgeMs=60000 expecting the freshness gate, or GET /api/ports?refresh=true expecting a live scan (the handler comment says `rt port` relies on it). Over REST both arrive as strings, the checks are false, and stale data is returned with ok:true. There is no error, so the caller cannot tell the flag was ignored. events.ts and settings.ts each carry their own private coercer (num(), str()), which is the same problem solved three different ways.

*Fix (small):* Coerce at the seam: a single query-to-payload decoder in api-server.ts (numbers, booleans, JSON-ish values) or per-command payload schemas applied to both transports before dispatch.

#### S086 · P3 · hazard · `lib/daemon/handlers/discussions.ts:143`
**discussions:diffs fetches GitLab with no timeout or abort signal**

Every other outbound call in the daemon carries AbortSignal.timeout (linear.ts:163, notifier.ts:226/342, park.ts:87) or goes through the SDK's request hook; this one does not, and the handler ignores the request's own signal. A stalled GitLab connection leaves the fetch pending indefinitely; the client (console) times out and retries, so each retry adds another pending fetch and another open socket for as long as GitLab stays unresponsive.

*Fix (trivial):* Pass `signal: AbortSignal.any([AbortSignal.timeout(30_000), reqSignal])` (or at minimum AbortSignal.timeout) and thread the handler's signal argument through.

#### S087 · P3 · hazard · `lib/daemon/handlers/pane.ts:226`
**pane:spawn worst-case budget exceeds the client timeout and never checks the abort signal**

Register 10s + idle wait 55s + pane.read 5s + send_keys 5s + trust wait 20s + prompt 10s + pane.get 5s is ~110s when herdr is slow; rt-client's paneSpawn default timeout is 90s (packages/rt-client/src/client.ts:403) and the handler ignores the request AbortSignal. The client reports failure while the daemon finishes creating the tab and starting claude; the user retries and a second claude pane is spawned in the same cwd. Same shape, smaller, for chat:invite (30s client vs up to ~30s daemon).

*Fix (small):* Either raise the client timeout above the summed daemon budget, or thread the handler's AbortSignal into the herdr waits and stop after tab.create if aborted (returning the paneId already created).

#### S088 · P3 · bug · `lib/daemon/handlers/project-mrs.ts:246`
**mr:by-branch forge write-back bypasses the demand scope the sync path enforces**

A demand-scoped repo (scope.authors = [me, teammateA]). A client asks mr:by-branch for a branch owned by teammateB. The forge fetch returns B's MR and it is upserted into the store unconditionally. The next delta sync filters only the delta result set, so the stranger row persists until the daily deep reconcile prunes it, then reappears on the next by-branch call. Boards keyed on the store show out-of-scope MRs intermittently; `scope.uncovered` reporting is inconsistent with what is actually in `mrs`.

*Fix (trivial):* Apply the same `scope`/`tagged` gate as upsertProject before the upsert (return the PR to the caller without storing it), or export upsertProject from freshness and call it here.

#### S089 · P3 · hazard · `lib/daemon/handlers/worktree.ts:316`
**Concurrent cold creates in one repo race on git ref locks and the failure is charged to createBackoff**

Two agents provision at once with an empty pool, or one provisions while the 5-minute tick's replenish starts. Both createTree calls pick names (the same name from a small pool yields a bare 'busy' refusal to the second caller with no retry), and with different names both run `git fetch origin main` in the same repoPath concurrently; the loser fails to lock refs/remotes/origin/main, is reported as create-failed, and noteCreateFailure puts the repo's replenish into a 5-to-30-minute backoff for a non-failure.

*Fix (small):* Register provision's create in creationPromises (or serialize createTree per repo with a repo-level async mutex) and retry name selection on 'busy'.

#### S090 · P3 · bug · `lib/daemon/home-snapshot.ts:360`
**Missing ~/.mattstack/user is diagnosed as "could not run git (is it on PATH?)" on every boot**

A new user installs mattstack and has not yet run `rt home init`, so `~/.mattstack/user` does not exist. `Bun.spawn` with that cwd throws ENOENT synchronously; runCapture maps it to exitCode -1; the module logs a warn blaming git's PATH on every daemon start and reports `skipped: "init-failed"` to `rt home snapshot`. The user (or whoever reads their log) chases a PATH problem that does not exist; the honest state is "home repo not initialised".

*Fix (trivial):* Check `existsSync(deps.repoDir)` first and set disabledReason = "not-a-repo" with a message naming `rt home init`; keep the exitCode -1 branch for the genuine spawn-failure case.

#### S091 · P3 · hazard · `lib/daemon/home-snapshot.ts:428`
**home-snapshot timers call readSettings unguarded; a resolver throw kills the janitor permanently**

The module doc guards construction, the debounce arm and status() against `readSettings()` throwing (an authored `${repoRoot}` inside rt.homeSnapshot makes getSetting throw on expansion). The five other call sites are not guarded. If the throw begins after boot: the janitor's `.finally` callback throws before re-arming, so the 30-minute janitor never runs again for the daemon lifetime (one unhandledRejection line, no recovery); `doRun` at line 570 rejects before its own kill-switch logic, so a commit that already landed in a previous run never gets `schedulePush()`; `doPushInner` rejects inside the push timer. All three degrade silently to "snapshots stopped" while status() keeps reporting the last-known enabled=true.

*Fix (small):* Route every timer-path settings read through one `safeReadSettings()` that catches, warns (deduped by lastLoggedSettingsError) and returns the clamped SETTINGS_FALLBACK, matching what armWatcher already does.

#### S092 · P3 · hazard · `lib/daemon/socket-server.ts:41`
**Neither server sets maxRequestBodySize; the unauthenticated unix socket will buffer Bun's default 128 MB per request**

Any same-user process (a runaway script, a buggy consumer that serializes its whole state into a chat:post body) POSTs a very large JSON body; the daemon reads and parses up to 128 MB on the event loop per request, stalling every other client and inflating RSS. Real payloads here are kilobytes, so a 1 MB cap costs nothing and turns the failure into an immediate 413.

*Fix (trivial):* Set `maxRequestBodySize: 1024 * 1024` on both servers (larger only for the discussions:reply body if needed), and log at warn when it is hit.

#### S093 · P3 · hazard · `lib/daemon/system-process-scanner.ts:230`
**Every 10 s system-wide lsof scan is a standing CPU tax on busy machines**

`lsof -d cwd -Fpn` walks every process on the box, and on macOS it is slow (1-4 s with a few hundred processes, longer with Docker/Electron apps). Running it every 10 s, plus a second `ps eww` that dumps every repo process's full environment, keeps a core partially busy and the fans audible on an otherwise idle laptop; when the scan exceeds 10 s the timer SIGTERMs it and the parse runs on truncated output, so rows appear and disappear between ticks. The author's machine tolerates it; a teammate on a loaded MacBook Air perceives 'mattstack makes my laptop hot'.

*Fix (small):* Scan on demand (tray panel open / rt ps) or back off to 60 s when no consumer has asked for system-processes recently; restrict lsof with `-a -c` or per-pid lists derived from the cheaper port scan.

#### S094 · P3 · hazard · `lib/daemon/worktree-reconciler.ts:1126`
**kick() during an in-flight pass is dropped, not queued; a long pass also blocks repos:locate for its whole duration**

A pass is running serial freshens across 5 repos x 3 on-deck trees, each doing a fetch plus a 10-minute `pnpm install` because the lockfile moved. An agent provisions and claims the last on-deck tree; the handler's opts.kick() returns immediately and is forgotten. The next 5-minute tick's kick is also dropped while the pass runs, so replenish waits until the pass ends plus one full tick. `rt repos locate` issued meanwhile blocks in withReconcilerHeld until every install finishes, and the CLI's 2-minute LOCATE_TIMEOUT_MS reports the daemon 'did not answer'.

*Fix (trivial):* Set kickQueued when inFlight is non-null too and fire once in runOnce's finally; consider splitting freshen/replenish into their own per-repo tasks so one repo's install does not serialize the estate.

#### S095 · P3 · hazard · `lib/herdr/client.ts:84`
**herdrRequest decodes each socket chunk independently, corrupting a multibyte character split across chunks**

A pane.read reply (chat:invite probes, pane:peek, pane:spawn's trust-prompt sniff) containing box-drawing or emoji characters that straddles a chunk boundary is decoded as two partial sequences; each becomes U+FFFD. JSON.parse still succeeds, so pane:peek returns garbled lines and the /trust/i sniff in pane:spawn (lib/daemon/handlers/pane.ts:242) can miss a match. Only bites on replies larger than one read (tens of KB), so rare today.

*Fix (trivial):* Accumulate Buffers (`chunks.push(chunk)`) and decode once with `Buffer.concat(chunks).toString()` when a newline is present, or use a `TextDecoder` with `{stream: true}`.

#### S096 · P3 · hazard · `lib/notifier.ts:229`
**Notification can be delivered twice when the post-push queue removal is busy-swallowed**

The tray accepts the push (banner shown). `removeQueuedNotification` hits SQLITE_BUSY and, per runCriticalWrite, gives up after 3 x 20ms and returns undefined with an error log. The fallback timer is only armed when `pushed` is false, so this exact path does not double-fire; but the tray's own startup `drainNotifications()` and the `/api/notifications` route will re-deliver the still-queued event on the next drain, so the user sees the same banner twice, and a later `isNotificationQueued` check by any other path treats it as undelivered.

*Fix (small):* Have pushToTray return the removal outcome and retry the delete on the next notify() or on a short timer; or mark delivered-at in the row so drains skip events already pushed within N minutes.

#### S097 · P3 · bug · `lib/port-scanner.ts:176`
**cwd matching uses raw index paths, so symlinked or case-variant repo roots never match lsof**

lsof reports the kernel's resolved path; the repo index stores whatever path `git worktree list` printed when the repo was registered (the path the user cd'd through). A user who keeps repos behind a symlink (~/code -> /Volumes/Dev/code, a common external-SSD layout), under /tmp (-> /private/tmp), or who registered via a differently-cased path on the default case-insensitive APFS volume, gets no ports, no system-process rows and no runaway detection for those repos. Worse, killWorktreeProcesses finds nothing at dispose time, so dev servers keep running in a deleted worktree (the EPIPE churn the on-deck memory records). All of this is silent: the scan simply returns fewer rows.

*Fix (small):* Canonicalize once per scan: realpathSync each index/worktree path (falling back to the literal on ENOENT, as runs/prune.ts's canon() does) and compare against lsof's cwd, which is already real.

#### S098 · P3 · hazard · `lib/repo-index.ts:143`
**endpoint:claim can reach an execSync git via resolveIndexPathForIdentity**

An endpoint:claim or endpoint:lookup for an identity whose index row is still legacy-named (the state every machine is in right after the identity cutover, per the comment at endpoint.ts:46-49) falls into resolveIndexPathForIdentity, which on a match calls updateRepoIndex and therefore a synchronous `git worktree list` on the daemon thread, with no timeout. The intercept shim fires these claims from dev-server start scripts, so this lands exactly when several processes start at once.

*Fix (small):* Give resolveIndexPathForIdentity an async index write (setIndexPath with listWorktreesAsync) or use the already-derived path without re-observing the main worktree.

#### S099 · P3 · hazard · `lib/rt-paths.ts:302`
**Legacy ~/.rt migration renames any foreign ~/.rt directory into rt's state root**

A new user has never run an old rt, but another tool that uses `~/.rt` as its config directory is installed. The first mattstack CLI or daemon start renames that directory to ~/.mattstack/rt, logs 'migrated legacy ~/.rt state', and from then on treats its contents as rt state (a foreign `daemon.json`, `repos.json` or `state.db` would be parsed or quarantined) while the other tool loses its config with no notice.

*Fix (trivial):* Only migrate when the directory carries an rt signature (daemon.json/logs/state.db written by rt), otherwise report and leave it; record the migration in the setup state so it happens at most once.

#### S100 · P3 · hazard · `lib/runs/prune.ts:101`
**Daily run prune deletes run directories with a synchronous recursive rmSync on the daemon thread**

Run directories can carry stage detail files and logs; on the first boot after a month of use the boot-time prune (60s after start) walks and deletes every expired run in one synchronous pass. Each recursive unlink of a large run tree blocks the event loop for its full duration; a tray poll or chat post during that window times out. The rest of the codebase already learned this lesson for worktrees (lib/worktree/trash.ts uses a detached `rm -rf`).

*Fix (small):* Rename expired run dirs aside and reap them with the detached rm pattern from lib/worktree/trash.ts, or spawn the delete via runCapture and await it.

#### S101 · P3 · hazard · `lib/runs/store.ts:121`
**agent-status poller and runs:list open every run's SQLite file synchronously on the event loop every 10s**

startAgentStatusPoller (lib/daemon/agent-status-poller.ts:39,70) calls listRuns(undefined, liveness) every 10s, and each console tab's runs:list does the same on demand. Each run costs a readonly open, PRAGMA user_version, four table reads and a close, all synchronous. pruneRuns is age-based, so a shared machine with a few hundred runs pays hundreds of file opens per tick on the daemon thread, competing with chat tails and tray probes. Not a crash, but the cost scales with the runs directory rather than with live runs.

*Fix (medium):* Have the poller list only runs whose directory mtime changed or whose last-known status was running (it already keeps `last`), or cache per-run summaries keyed on state.db mtime so unchanged runs are not reopened.

#### S102 · P3 · hazard · `lib/state/chat-store.ts:549`
**unreadWakingCount loads every unread row into JS on the pulse hot path; chat_messages is never pruned**

chat:pulse fires on every user prompt of every signed-in agent and calls unreadSummaryFor -> unreadWakingCount. For each open membership it selects ALL rows above the cursor (no LIMIT) and JSON.parses each. A mention-mode member of a busy room drains its cursor only 20 rows per `rt chat read` (readUnread advances to the highest id returned, not room max), so the unread tail grows without bound; chat_messages has no retention at all. Benchmark on a temp db: 100k unread rows = ~30ms of synchronous event-loop time per pulse per room, repeated on every prompt from every agent. Degrades every other daemon request (tray status probes, tails) rather than crashing.

*Fix (small):* Compute the waking count in SQL (COUNT with the same mention LIKE / wake_on='all' predicate), or cap the scan (LIMIT 1000 and report `1000+`), and add an age/size retention sweep for chat_messages akin to the events bus (keep last N per room or 90 days).

#### S103 · P3 · bug · `lib/state/db.ts:489`
**quarantine() renames state.db while the failed handle is still open**

The first `db` is never closed before rename: its fd (and any -wal/-shm it mapped) stays open on the quarantined inode for the process lifetime (the daemon's, in the boot case), and the sidecar renames in quarantine() happen after the main file moved, so a sidecar rename failure orphans a -wal that belongs to the quarantined file next to the fresh one.

*Fix (trivial):* `try { db.close(); } catch {}` in the catch before quarantine(path); rename sidecars before the main file.

#### S104 · P3 · hazard · `lib/worktree/git-async.ts:33`
**Mutating git verbs share the 60s SIGTERM timeout; a large-repo checkout can be killed half-applied**

Provision on a very large monorepo checks out a branch that touches tens of thousands of files on a cold disk; at 60s runCapture SIGTERMs git mid-checkout. HEAD may already point at the new branch while the index/working tree are partially updated. rollbackClaim sees current !== onDeckBranch and flips the tree to `disposable` with reason `checkout-failed:` and an empty detail (stdout/stderr truncated). The user gets an opaque refusal and a half-switched tree that the dirty guard will then refuse to dispose automatically. Only fetch and worktree-add were given 5 minutes.

*Fix (small):* Give checkout, merge, stash push/pop and status explicit long timeouts (5 min) matching fetch/add, and after a timeout on a mutating verb log at warn with the partial output rather than returning an empty detail.


## Appendix B ... Structural findings (rot and gaps)

Structural claims were adjudicated by the lead reviewer from finder evidence rather than per-item agents. Same terseness rule for P3.

#### R001 · P1 · gap · `commands/daemon.ts:325`
**rt daemon status cannot say 'process alive but not serving' (parked, boot-blocked, wedged)**

The daemon is parked (wrong flavor), blocked in state.db open behind a CLI import, or stuck in `$SHELL -ilc` because a user's .zshrc waits on a prompt. rt daemon status says 'installed but not running / run: rt daemon start'; start POSTs /daemon/start to the tray (already registered), waits 3s, prints 'daemon starting… check logs'. The user repeats this indefinitely. Nothing on the CLI surface says a process is alive and where it is stuck; the only trace is an info line in a log they cannot read.

*Fix (small):* Extend DaemonStatusVerdict with `alive-not-serving`: when ping fails but kill(pid,0) succeeds for rt.pid OR `pgrep -f 'rt --daemon|lib/daemon.ts'` finds one, print 'process  is running but not answering rt.sock (booting, parked, or wedged) ... rt daemon logs -t'. Have the daemon write a boot-phase breadcrumb (finding 1) so the verdict can name the phase. Add a test for the new verdict in lib/__tests__/daemon-status.test.ts.

#### R002 · P1 · gap · `lib/daemon-logger.ts:237`
**No restart counter or last-crash reason on any surface; a flapping daemon looks healthy between crashes**

A handler throws synchronously on every 5-minute refresh (e.g. a bad settings value in doppler-sync). Daemon dies, launchd restarts it in 10s, it runs 5 minutes, dies again: 12 restarts/hour. Tray is green 95% of the time with a brief red blink; board/console websocket clients reconnect silently; rt daemon status shows 'running, uptime 3m' and nothing else. Nobody is told the daemon has restarted 200 times today or what the fatal line said; the fatal is buried in a 14-day JSON log the user cannot open without lnav/logdy.

*Fix (medium):* Persist a boot-failure record (reason, count, timestamp) under rtDir() from the fatal path; have startDaemon back off (park) after N consecutive failures within M minutes instead of exiting; surface the record in `rt daemon status` and `rt verify`.

#### R003 · P1 · gap · `lib/daemon/handlers/status.ts:23`
**No event-loop stall detection; a wedged daemon is reported as 'not running' by every surface**

A refresh over a large repo runs `git for-each-ref` via execSync on an NFS/slow volume, or lsof hangs during a worktree return. The loop stalls 5-60s. Tray: two 2s timeouts -> red 'Daemon: not running' while the pid is alive; the panel's Restart kills a daemon that would have recovered. rt daemon status: if ping squeezes through, 'likely mid-sync' (a guess with nothing behind it); if not, 'not running'. The daemon logs nothing about the stall before, during, or after. The memory notes show the author diagnosing exactly this with a hand-rolled curl latency loop; a user cannot.

*Fix (medium):* Add lib/daemon/loop-monitor.ts: setInterval(100ms) measuring drift; keep {lagMs, maxLagMs, stalls, lastStallAt, lastStallCmd}; log warn when drift > 1s with the command in flight (track current cmd in handleCommand). Expose `eventLoop` in ping/status/tray:status. Tray: when the pid from the last good status is alive (kill 0 via the socket owner or rt.pid) but polls time out, show 'unresponsive (pid N)' orange, not 'not running'. rt daemon status: print maxLag and last stall on the degraded branch instead of 'likely mid-sync'.

#### R004 · P2 · gap · `lib/daemon-logger.ts:102`
**Log level is only settable via RT_LOG_LEVEL, which launchd/SMAppService never provides; command traces are debug-only**

A user reports 'the board goes stale'. Support asks for debug logs. There is no way to raise the level of the launchd-spawned prod daemon without editing the plist inside the signed app bundle (which breaks the signature and Login Items). At the default level, the log contains no record of successful commands or their durations, so a 30s mr:action or a 240s events:wait leaves no trace; only rejections/throws are visible.

*Fix (small):* Read `rt.logLevel` (machine scope, registered per docs/settings-architecture.md) at boot with RT_LOG_LEVEL as override; add a `daemon:log-level` IPC + `rt daemon log-level ` that sets logger.level live and logs the change; additionally log successful commands at info when durationMs exceeds a threshold (e.g. 2s) so latency outliers are visible at the default level.

#### R005 · P2 · gap · `lib/daemon-logger.ts:249`
**All stderr writes become level=error and unhandled rejections are swallowed uncounted, polluting error filters**

Bun runtime deprecation notices, library console.error chatter, and `git` stderr forwarded by a dependency all land as level=error. A user (or the tray's future health computation) filtering on error sees dozens of benign lines per hour and misses the real one. Meanwhile a code path that rejects on every tick (a broken poller) is logged at error and otherwise invisible: no counter reaches status, so a daemon that is functionally dead in one subsystem keeps reporting healthy.

*Fix (trivial):* Map intercepted stderr to warn with source:'stderr' (error only when the text starts with a known panic/exception prefix). Count unhandledRejection and uncaught-but-recovered errors in a process-wide counter exposed in status/health (finding 10).

#### R006 · P2 · rot · `lib/daemon.ts:78`
**lib/daemon.ts arms fourteen live side effects at import; startDaemon() is not the entry point**

Any `import "./lib/daemon.ts"` (a future unit test, a tool, a mis-wired command module, the module registry) starts timers, opens events.db and state.db, spawns git against ~/.mattstack/user, and runs a login shell, before startDaemon() is ever called. The park gate had to be placed 'above every subsystem' precisely because construction is import. Every new subsystem the next ten changes add lands here with the same trap, and the ordering constraints exist only as 14 comment blocks (lines 41, 74, 97, 145, 324, 389, 418, 427, 454, 462 all say MUST/BEFORE/AFTER).

*Fix (medium):* Turn daemon.ts into a pure module: export startDaemon(opts) that constructs subsystems in an explicit array of {name,start,stop} in one function body; keep only the cli.ts/import.meta.main call at module scope. This also makes cleanup() derive from the same list (see lifecycle finding).

#### R007 · P2 · gap · `lib/daemon.ts:85`
**Boot failures before stderr redirection leave no diagnostics anywhere**

If ~/.mattstack/rt/logs cannot be created or written (the directory or state.db is root-owned after a `sudo rt ...` invocation, which repo-index.ts:171 already names as a real case; disk full; a synced home dir with wrong ownership), getDaemonLogger() rejects at module scope, the process exits 1, and launchd respawns it every 10 s. Because fd 2 is only redirected inside startDaemon and the prod plist sets no StandardErrorPath, the exception text goes to /dev/null; `rt daemon logs` shows nothing, `launchctl print` shows only 'last exit code = 1', and the setup row says 'installed but not responding ... approve in Login Items', which is the wrong remedy. The dev shim already redirects fd 2 before any precondition for exactly this reason.

*Fix (trivial):* Call redirectNativeStderr() as the first statement of lib/daemon.ts (it only needs logsDir()), and/or add StandardErrorPath to the prod plist; have `rt daemon status` surface launchd's last exit code and the daemon-stderr.log tail.

#### R008 · P2 · gap · `lib/daemon.ts:329`
**No request id or caller identity across CLI/clients -> daemon; rejected commands are unattributable and unreproducible**

Six clients (rt CLI, tray, mr-board, gitq, console, chat viewer, shepherdr panes) share rt.sock. The log shows 'command rejected worktree:claim error=no such tree' 40 times in an hour with no payload (which repo? which tree?) and no caller. The user's cli..log shows their own `rt worktree claim` succeeded. There is no way to tie a daemon line to the invocation that caused it, so the report becomes 'sometimes it says no such tree'.

*Fix (medium):* Generate a short reqId per socket/REST request; put it in the response envelope and in every handleCommand log line; clients send `X-RT-Client: /` (rt-client transport + daemon-client.ts) and the daemon logs it; on rejected/failed, log a redacted payload digest (top-level keys plus repo/branch/iid/room when present). cli-logger records the reqIds a command issued.

#### R009 · P2 · rot · `lib/daemon.ts:374`
**No lifecycle seam: every subsystem hand-wires construction, cleanup and opts in three places, and half have no stop**

The next subsystem (say a notifications digest) must be added to daemon.ts construction, to buildRoutedHandlers opts, to cleanup(), and possibly to emit(); forgetting the cleanup line is invisible because process.exit follows. Tests cannot start/stop the daemon graph in-process because half the subsystems cannot be stopped, which is why daemon.ts wiring has no unit test at all.

*Fix (medium):* Introduce `interface Subsystem { name; start(ctx): Promise|void; stop(): void }`, register each in an array in startDaemon, run start in order and stop in reverse; pollers, reconciler and the sweep timers become subsystems with handles.

#### R010 · P2 · gap · `lib/daemon/handlers/chat.ts:183`
**chat:post validates neither room, handle nor body: typo'd rooms are a silent black hole and bad shapes 500**

(a) An agent posts to `deck` instead of `deck-main`. postMessage (lib/state/chat-store.ts:465-473) runs REVIVE (no-op), INSERTs the message into a room with no chat_rooms row and no members, recipientsFor returns [], and the verb answers `{ok: true, recipients: []}`. Nobody is woken, `rt chat rooms` never lists it, the message is unreachable except via chat:messages with the exact name. The CLI (commands/chat.ts:728) checks only the name pattern. (b) body missing or non-string: mergeMentions -> parseMentions -> `body.matchAll` TypeError escapes the handler, handleCommand logs at ERROR 'command failed' and the client sees a 500 with a TypeError string. (c) room containing `/` or `*` emits a reshaped `chat//msg` topic; handle is stored unvalidated. (d) `mentions` as a string instead of array TypeErrors the same way. chat:dm has the same shape: a DM to an unknown handle creates a dm room and returns ok (lib/state/dm-store.ts:61-71).

*Fix (small):* At the top of chat:post/chat:dm: isValidChatName(room) and isValidChatName(handle); `typeof body === 'string' && body.length > 0` (and a generous max, e.g. 64KB); `Array.isArray(mentions)`. Refuse a post to a room with no chat_rooms row (`roomArchivedAt(room) === undefined`) with an error naming the closest existing rooms, so a typo fails loudly instead of returning ok with no recipients.

#### R011 · P2 · gap · `lib/daemon/handlers/status.ts:43`
**/api/status and tray:status have no degraded level; a daemon whose refreshes all fail still reports green**

The user's GitLab token expires. Every 5-minute refresh logs 'warn ... 401' per MR, lastRefreshAt still advances, freshness watchers flip to degraded but that field is only in the CLI `status` payload, not tray:status. Tray: green. Board/console via rt-client daemonHealth: reachable=true. rt daemon status: 'running'. The only symptom is stale MR data, which users attribute to rt being broken rather than to the token.

*Fix (medium):* Compute a `health: { level: 'ok'|'degraded'|'unhealthy', reasons: string[] }` in a small lib/daemon/health.ts from: last SUCCESSFUL refresh age > 2 intervals, enrichErrors/failedRepos in the last cycle, busy-skip and critical-write-failure counters, degraded watcher count, event-loop maxLag (finding 4), restarts1h (finding 5), free space under rtDir, loggerDegraded (finding 2). Return it from ping/status/tray:status; tray maps degraded->orange with the first reason as statusText; rt daemon status prints reasons; rt-client daemonHealth returns it.

#### R012 · P2 · gap · `lib/daemon/handlers/status.ts:56`
**No memory/handle metrics or growth alarms; leaks are invisible until the machine is slow**

A user registers and unregisters repos over weeks, or a client leaks WebSocket connections on reconnect; fs.watch handles and ws entries accumulate. After a week the daemon is at 1.5GB RSS and every refresh is slow. Neither the tray (which reads memoryUsage but never displays or thresholds it), rt daemon status, nor the logs show any trend; the report is 'rt gets slow after a while' with no data to act on.

*Fix (small):* Log a periodic (5 min) info line {rss, heapUsed, external, wsClients, waiters, watchers, uptime}; warn when rss exceeds a threshold or grows >50% over an hour; include the same fields in `status` and tray:status; make refreshWatchedRepos close watchers for repos no longer in the index.

#### R013 · P2 · rot · `lib/daemon/handlers/types.ts:75`
**48 of 91 daemon commands, including everything the Swift tray calls, are untyped `payload: any` contracts**

The comment's premise is false: tray:status, system-processes, notifications, ports, repos, worktree:* (agent-facing skills), endpoint:*, mr:action, discussions:* all have out-of-process consumers (Swift tray, CLI, skills, mr-board via daemon-client). Renaming a field in tray:status or worktree:provision's payload compiles clean and shows up as a blank tray panel or a `repo-unknown` refusal at runtime, with no test to catch it (rt-client-commands.test only checks the 43 catalog names exist).

*Fix (medium):* Move every command with a non-daemon consumer into the rt-client catalog (or a daemon-internal typed catalog for the tray), and drop `Handler`'s `any` to `unknown` so each handler must narrow.

#### R014 · P2 · rot · `lib/daemon/worktree-reconciler.ts:1062`
**worktree-reconciler.ts is a 1183-line god module: five duties, one serial pass, 11 internals exported for tests**

Because every duty for every repo runs in one serial pass under one inFlight promise, one repo's freshen (5-minute fetch timeout + multi-minute ready steps) delays the merge reactor for all other repos: an MR merged in repo B is not auto-disposed until repo A's install finishes. The header still says 'Task 12 extends runOnce in place' and 'structure here is deliberately left open', i.e. the file is a growth point by design; the next duty lands as a sixth try/catch in the same loop and a twelfth __test__ export. Comment density is 26 percent with 13 ticket/spec/review citations.

*Fix (large):* Split into reconcile.ts, reactor.ts, freshen.ts, replenish.ts, reap.ts each exporting a per-repo step with its own test file; have the reconciler schedule steps per repo (independent promises with a concurrency cap) instead of one serial loop; move createBackoff into the reconciler instance.

#### R015 · P2 · rot · `lib/state/db.ts:436`
**Idempotent column adds are gated behind user_version, so a cross-lane SCHEMA_VERSION collision has no self-heal**

CLAUDE.md records this happening once. Another lane's daemon stamps user_version 8 first with a different v8; this branch (v8 = chat_rooms.archived_at) merges and its daemon sees 8 < 8 false, so addArchivedAtColumnIfMissing never runs. chat-store.ts:120 `SELECT ..., archived_at FROM chat_members JOIN chat_rooms` then throws 'no such column: archived_at' on every chat:rooms/chat:read for the life of the db, until someone manually resets PRAGMA user_version. The same applies to any future table a colliding lane adds.

*Fix (trivial):* Run the DDL concat and both column helpers unconditionally inside the IMMEDIATE transaction on every open (cost: one schema parse), and keep the `user_version === 0` check only as the legacy-import gate. Then a stamp collision costs nothing because the schema converges on the next open regardless of the number.

#### R016 · P2 · rot · `packages/rt-client/src/client.ts:83`
**Contract-drift guard misses `cache:read`: rt-client calls an uncataloged `any` handler whose wire shape is asserted only by a comment**

The console's run-view rows read `mr.webUrl`, `mr.pipeline.status`, `ticket.identifier` from this response. CacheEntry lives in lib/state/branch-cache.ts and is daemon-internal; a rename there (or a change to toMRInfo's output) compiles clean in rt, passes the COMMAND_NAMES exhaustiveness test (cache:read is not in the list), and only shows up as `undefined` cells in the console after the next rt-client publish. This is the exact 0.5.0-style skew CLAUDE.md warns about, one hop away from the guard that was built to stop it.

*Fix (small):* Add `cache:read` to `Commands` with payload `{ branches?: string[]; maxAgeMs?: number }` and data `Record<string, BranchEnrichment>`, add it to COMMAND_NAMES, and type the handler against `Commands["cache:read"]` so the compile-time proof in buildRoutedHandlers covers it. Same for any other verb rt-client calls by string.

#### R017 · P3 · rot · `docs/daemon-runner-health.md:1`
**docs/daemon-runner-health.md audits subsystems that no longer exist and describes none of the current lifecycle**

Every file:line link in the document points at deleted code (process-manager, remedy-engine, runner.tsx, runner-store, workspace-sync, state-store). A reader asked 'is the daemon's stability audited?' finds a document declaring all P0/P1 items fixed and concludes the lifecycle is covered, while none of the boot/park/eviction/shutdown/crash-policy paths that exist today are mentioned. The prompt for this audit asked whether it is still accurate: it is not, in any section.

*Fix (small):* Delete it or replace it with a lifecycle-focused audit that anchors to lib/daemon.ts, park.ts, boot-reconcile.ts, shutdown.ts and daemon-logger.ts, and record the process-manager era's findings in git history only.

#### R018 · P3 · gap · `lib/__tests__/daemon-logger.test.ts:137`
**No tests cover the crash-handler, logger-init-failure, or alive-but-not-serving paths**

The spec (2026-05-20, Testing) promised an integration test that throws from a setTimeout and asserts a level=60/fatal line was flushed before exit 1; it was never written. Findings 1, 2 and 6 are all in untested paths, so a refactor that moves installCrashHandlers or changes the stream options can silently remove crash capture and nothing fails in CI.

*Fix (medium):* Add a spawned-subprocess test (bun test can spawn `bun run` a tiny harness under an isolated HOME) asserting: (a) an uncaught throw yields a fatal JSON line and exit 1; (b) an unwritable logs dir still yields a stderr-file record and a non-zero exit; (c) a write error after init does not abort the process. Add the alive-not-serving verdict case to daemon-status.test.ts.

#### R019 · P3 · rot · `lib/daemon.ts:200`
**The three prune/sweep timer blocks are one block pasted three times, with the body itself duplicated per block**

A fix to one copy (e.g. adding a duration log, a busy-guard, or moving off module scope) silently misses the other five. The boot-time copy and the interval copy already differ only by delay; a future retention bug will be fixed in one and reappear at the other cadence.

*Fix (trivial):* One `scheduleSweep(name, fn, { bootDelayMs, intervalMs })` helper returning a stop handle; three one-line calls.

#### R020 · P3 · rot · `lib/daemon.ts:252`
**emit() accretes per-event business logic; events-bus frame construction duplicated in two files**

The next cross-subsystem reaction (e.g. 'on worktree:claimed, prime the endpoint shim') will be a second `if (type === ...)` in emit(), with `data as {...}` casts and no test. The two emitEvent copies already differ: daemon.ts routes through emit (cron sees the frame), command-router routes through raw broadcast (cron does not), so an events-bus topic emitted by a handler cannot trigger a cron rule while the same topic from the agent poller can.

*Fix (small):* Give the bus a single `emitEvent` that owns the frame and fan-out; expose an `onBroadcast(type, fn)` subscription so endpoint release and cron register themselves instead of being special-cased in daemon.ts.

#### R021 · P3 · gap · `lib/daemon.ts:362`
**Unknown command and version skew are indistinguishable from validation failures; no capability negotiation despite `ping` exposing version**

A consumer ships rt-client 0.7.0 (chat:archive, chat:dm-open) against a daemon that has not been restarted since before #119 (CLAUDE.md notes the running daemon is whatever was checked out when it started). chatArchive returns `{ok:false, error:"unknown command: chat:archive"}` with HTTP 200; the chat viewer shows a generic failure toast, the daemon logs a warn `command rejected` per attempt, and nobody is told the fix is `rt daemon restart`. The same shape covers a real validation error, so consumers cannot special-case it.

*Fix (small):* Give unknown-command its own envelope field (`code: "unknown-command"`, plus the daemon `version`) and have rtCommand surface a distinct error text ("daemon at version X does not know ; restart or upgrade rt"). Optionally expose `commands: COMMAND_NAMES` on ping so consumers can pre-check.

#### R022 · P3 · gap · `lib/daemon.ts:375`
**state.db is never closed at daemon shutdown, contrary to the spec, leaving WAL recovery to the next CLI opener**

On a clean SIGTERM the daemon's connection is dropped without a close, so no final checkpoint runs; the -wal persists and the next opener (usually a CLI command racing the relaunch) pays WAL recovery, during which a concurrently starting daemon can receive SQLITE_BUSY_RECOVERY, an extended code the current isBusyError does not classify.

*Fix (trivial):* Call closeStateDb() in cleanup after servers stop and pollers are disposed (it runs a passive checkpoint on last close).

#### R023 · P3 · gap · `lib/daemon.ts:456`
**clearAllArmed boot invariant is one-shot and silently skipped on SQLITE_BUSY**

A daemon restart while agents are active is precisely when a CLI process (`rt chat post`, `rt chat tail`'s touch) holds state.db's write lock. If the clear hits SQLITE_BUSY it is warned and dropped, and no later code path retries it, so member and presence rows for tails that died with the old daemon keep their armed_at: the buddy list and wake decisions treat those agents as live tails until each stale row is overwritten by chance. The comment states the ordering requirement but the write has no completion guarantee.

*Fix (trivial):* Run clearAllArmed through runCriticalWrite (bounded retry, error on exhaustion) rather than persistOrWarn, or retry it once the socket is up but before the first chat verb is served.

#### R024 · P3 · gap · `lib/daemon/__tests__/api-auth.test.ts:1`
**No integration test covers socket-server or api-server: routing, CORS, HTTP-layer token gate, WS upgrade, and the 500 envelope are untested**

Every finding above in api-server.ts (open WS upgrade, unguarded POST routes, string query params, decodeURIComponent 500s, ignored send status) would have been caught by a test that starts startApiServer on a random port with a fake handleCommand and asserts responses. Without one, a change to REST_ROUTES or needsToken ships on tsc alone; the picker-conformance and COMMAND_NAMES gates do not see this layer.

*Fix (medium):* Add api-server.test.ts and socket-server.test.ts that bind on RT_API_PORT=0 / a temp socket, drive requests with fetch, and assert: token gate per route (enumerating REST_ROUTES), CORS headers, WS upgrade rejection for a foreign Origin, 404/405/500 envelopes, and signal abort reaching the handler on client disconnect.

#### R025 · P3 · gap · `lib/daemon/agent-status-poller.ts:70`
**agent-status poller swallows every tick error with an empty catch below the seam**

`opts.emitEvent` -> `eventsBus.emitAt` (daemon.ts:300) throws (events.db SQLITE_FULL / IOERR), or `livenessFrom`/`primeLivenessCache` hits an unexpected shape: the tick rejects, the catch drops it, and the run-updated mirror silently stops for that tick with no log line, violating the repo's catch policy ('anything else logs at warn with { err }').

*Fix (trivial):* Replace `.catch(() => {})` with `.catch((err) => opts.log.warn({ err }, "agent-status tick failed"))`.

#### R026 · P3 · gap · `lib/daemon/boot-reconcile.ts:16`
**No tests cover eviction, cleanup file ownership, signal exit codes, or the boot-failure path**

The three P1 lifecycle hazards above (eviction/cleanup race, exit-0 policy, zombie-on-throw) all live in code with zero coverage, and none is observable in the author's day-to-day because the dev path differs from the prod path. A regression in any of them ships silently; the e2e harness starts a compiled daemon but does not exercise a second daemon, a signal, or a bind failure.

*Fix (medium):* Add unit tests with injected fs/kill seams for evictStaleDaemon (only kills a verified rt pid; waits for death) and createCleanup (never unlinks another pid's files), and an e2e case that starts the compiled binary with RT_API_PORT pre-bound and asserts exit code 1 plus a fatal log line.

#### R027 · P3 · gap · `lib/daemon/command-router.ts:40`
**Ten daemon modules have no test file; failure paths (BUSY, timeout, throw) are exercised in a minority of suites**

The three transports (socket, REST, WS) and the shutdown/eviction sequence, i.e. the parts a non-author user hits first, are covered only by e2e/tests/daemon.test.ts against a compiled binary. The boot-order defect in this audit (state.db flavor) and the eviction race are both untestable today because daemon.ts cannot be started in-process, and handleCommand's throw-to-500 path has no assertion. hooks-guard's fs.watch paths and the hooks-scan overlap have no coverage at all.

*Fix (medium):* After the lifecycle refactor, add: a boot test that starts the graph under a temp HOME and asserts busy_timeout, pid/socket ownership, and clean stop; router tests for throw -> envelope; transport tests for the REST string-coercion and 500 paths; a hooks-guard test with an injected watch factory that emits 'error'.

#### R028 · P3 · rot · `lib/daemon/command-router.ts:70`
**Factories return a `db` test seam inside the handler map; router strips it by destructuring at three sites**

A fourth factory that copies the chat pattern and is spread without the strip puts a Database object under key "db" in routedHandlers; `POST /db` on the unix socket then calls `routed(payload)` on a non-function, throws TypeError, and returns a 500. The seam exists only because the factories take a db they were already given; tests can hold their own reference.

*Fix (trivial):* Remove `db` from the factory return types (tests already pass the db in); make buildRoutedHandlers' return type reject non-function values.

#### R029 · P3 · rot · `lib/daemon/discussions-store.ts:60`
**Copy-pasted helpers with drifting semantics: canon x4, patchTree x2, numericUserId x2 (different return types), TERMINAL x3**

The two numericUserId implementations already disagree on 'gitlab:user:12a' (regex tail '12' vs parseInt NaN) and on type; a self-author comparison fixed in one (notifier's strict null-means-suppress rule) is not mirrored in the other. patchTree is a load-mutate-save with no epoch check, duplicated so a concurrency fix in the reconciler copy misses the handler copy.

*Fix (small):* One lib/worktree/patch.ts for patchTree, one lib/fs-canon.ts for canon, one identity helper for numericUserId, one MR_TERMINAL_STATES constant in enrich.ts.

#### R030 · P3 · rot · `lib/daemon/events-bus.ts:185`
**Waiter afterId stays at the caller's stale cursor, so every matching emit rescans the whole journal**

wait() already proved nothing matches in (effAfter, head] via the catch-up query, but registers the waiter with afterId = effAfter. On the next matching emit, insertAndWake (line 135) calls eventsAfter(w.pattern, w.afterId), which runs `SELECT ... WHERE id > ?` with no LIMIT and glob-filters every row in JS. A consumer whose persisted cursor is far behind (after=0 from a fresh job dir, or a shepherdr cursor from days ago) costs a full-journal scan per wake: ~6ms at 50k rows on the emit path (measured), inside chat:post's per-recipient loop. The empty-return cursor was fixed for this exact reason (comment at lines 177-183) but the waiter's own cursor was not.

*Fix (trivial):* Register with `afterId: head` (the catch-up already covered everything up to head, and settle still returns the last delivered id as the cursor). Also add `LIMIT` to afterStmt for the waiter path.

#### R031 · P3 · rot · `lib/daemon/freshness.ts:87`
**Module-scope singleton state in freshness, project-sync, discussions-poller, locks, registry and notifier prevents in-process lifecycle tests**

None of these can be constructed twice in one process, so freshness's watcher lifecycle (startWatch/stopWatch/reconcile coalescing, cursor resume) has only a mapping test (freshness-mapping.test.ts) and discussions-poller has none; tests that need isolation rely on HOME swaps and __test__ reset hooks. A second daemon graph in-process (the natural shape of an integration test) is impossible.

*Fix (medium):* Convert each to a factory returning an instance (createFreshness(env), createDiscussionsPoller(deps)) owned by the daemon's subsystem list; keep module-level wrappers only for the CLI paths that need them.

#### R032 · P3 · rot · `lib/daemon/freshness.ts:95`
**freshness.getRemoteUrl uses execSync on the daemon thread inside every forge handler**

getRepoContext (mr:action, discussions:*, project-mrs, mr:by-branch) and reconcileFreshnessImpl call this for every repo without a cached provider. `git config` is fast on a local disk, but a repo on a network volume, an iCloud-evicted folder, or a machine where git's first invocation triggers the Xcode CLT dialog blocks the entire event loop for the duration (no timeout: execSync's `timeout` only SIGTERMs and waits, per the MAT-222 note). The tray's status poll times out and the red-dot flicker documented in memory recurs. The repo already has runCapture for exactly this and every other git call in scope uses it.

*Fix (trivial):* Replace with `await runCapture(["git","config","--get","remote.origin.url"], { cwd: repoPath, timeoutMs: 5000 })` and cache the result per repoPath for the process lifetime (remotes rarely change; invalidate on repo:moved).

#### R033 · P3 · gap · `lib/daemon/handlers/chat.ts:167`
**chat:join wakeOn and agent:start surface are not checked against their enums**

wakeOn: 'sometimes' is stored on chat_members and, when the join creates the room, stamped into chat_room_defaults for every future joiner (lib/state/chat-store.ts:225). recipientsFromMembers treats it as neither 'none' nor 'all' (mention-only), unreadWakingCount does not skip it, and nothing ever reports the bad value. agent:start (lib/daemon/handlers/agent.ts:132) accepts any `surface` string: a value that is neither 'herdr' nor 'headless' falls through to the headless spawn path with headless=false, spawning an interactive claude with stdin ignored and recording surface 'bogus'.

*Fix (trivial):* Validate `wakeOn` against ['mention','all','none'] and `surface` against ['herdr','headless'] at the handler boundary and return `{ok: false}` with the allowed values.

#### R034 · P3 · gap · `lib/daemon/handlers/chat.ts:194`
**chat:read / chat:messages accept any limit: negative or huge values return the whole room**

`limit: -1` reaches `ORDER BY id ASC LIMIT ?` (lib/state/chat-store.ts:145-149) where SQLite treats a negative LIMIT as unlimited, so a viewer or agent bug returns and JSON-serializes every message in a 100k-row room on the event loop; readUnread then advances last_read_id to the room max in one shot. A non-numeric limit produces a datatype error surfaced as a 500.

*Fix (trivial):* Coerce with the events handler's `num()` pattern and clamp to [1, 500] in both handlers.

#### R035 · P3 · rot · `lib/daemon/handlers/sdm.ts:59`
**Handler error conventions are inconsistent: five reply shapes and three error-string styles reach clients**

A consumer cannot write one error handler: `ok:true` may carry `data`, top-level fields, or an `error` field alongside ok:true (sdm:catalog); `ok:false` may be a typed code, a prose message, an 'Error: ' prefixed String(err), or an HTTP 500 for a thrown handler. The daemon.ts handleCommand seam logs `command rejected` on ok:false, so sdm:catalog's soft error is invisible to the log while a thrown chat handler is a 500 with a stack string leaked to the socket client.

*Fix (medium):* Enforce `{ok:true,data}` / `{ok:false,error:{code,message}}` via the TypedHandlers type for every command, and have handleCommand convert throws into the same envelope instead of rethrowing to the transports.

#### R036 · P3 · rot · `lib/daemon/handlers/types.ts:14`
**Comments narrate history and cite tickets/review findings instead of constraints; 20-36 percent comment density in the god modules**

A reader of project-mrs-store.ts is sent to a file that exists only on the author's machine; a reader of discussions-store.ts is told snapshots live in a JSON file that no longer exists; 'review r1 finding 11' is meaningful to nobody outside one session. The volume of prose hides the few comments that state real invariants (epoch checks, ordering), so the next editor skims all of them, which is how the module-scope state.db rule got violated while being documented three times.

*Fix (medium):* Sweep lib/daemon/** and lib/state/**: delete history/citation comments (git blame carries them), keep invariant comments, fix the two stale location claims; add a lint that flags `RT-\d+|MAT-\d+|review r\d|finding \d+|\.local-dev/` in comments.

#### R037 · P3 · rot · `lib/daemon/handlers/types.ts:22`
**RepoIndex is declared three times and one declaration still documents repos.json as the source**

The key is a serialized identity, the store is state.db kv, and `rt repo add` is not a command; a new contributor reading types.ts writes a name-keyed lookup (the exact miss docs/repo-identity.md calls 'fails silently'), and the user-facing error message sends them to a file and a verb that do not exist. Three shims for one function means three places to update when the index API changes.

*Fix (small):* One `RepoIndex` type exported from lib/repo-index.ts keyed by a branded `SerializedIdentity` type; delete the two re-export shims; fix the freshness.ts message to name `rt repos register`.

#### R038 · P3 · rot · `lib/daemon/handlers/types.ts:36`
**HandlerContext is a grab-bag of eleven unrelated fields; every handler factory receives all of it**

settings.ts needs nothing from ctx, endpoint.ts needs log+repoIndex, worktree.ts needs cache+log+repoIndex plus a separate opts object; tests must stub eleven fields (`as unknown as HandlerContext` in rt-client-commands.test.ts) to construct any handler. Adding a field (identity, refreshStatusRef were both added this way) is a daemon.ts edit plus a types.ts edit plus every stub. The FSWatcher map leaks hooks-guard internals into the status handler.

*Fix (medium):* Give each factory only the deps it names (Pick<> or a small interface), and let daemon.ts compose per-factory dep objects from the subsystem instances.

#### R039 · P3 · rot · `lib/daemon/handlers/worktree.ts:25`
**'repoName' means serialized identity everywhere and needs a comment in five files to say so**

Every handler carries a `parseIdentity(repoName) === null` guard and an apology comment because the name of the field contradicts its contents. The next handler author either forgets the guard (silently empty results, per docs/repo-identity.md) or uses `repoName` for display (leaking the wire form into UI, the bug system-processes.ts:103-107 already fixed once).

*Fix (medium):* Rename the field/parameter to `repo` typed as a branded `SerializedIdentity` (parseIdentity returns it), so the type carries the guarantee and the comments and per-handler guards collapse into the payload decoder.

#### R040 · P3 · gap · `lib/daemon/handlers/worktree.ts:517`
**worktree:dispose passes a stale snapshot record into disposeTree without re-reading under the lock**

`rt worktree dispose --owner agent-7` collects three records synchronously, then disposes them serially; each dispose takes seconds to minutes (status, ancestry, lsof, rename). By the time tree 3 is locked its record can be minutes stale: the reactor may have disposed it and replenish re-created a fresh on-deck tree at the same name/path. The stale rec (state claimed, old branch) passes guard 1, and with --force skips the rest: the brand-new on-deck tree is killed and trashed and its row removed. Harmless today only because rt owns both trees; the sibling paths already do the right thing.

*Fix (trivial):* Inside the lock, findByPath(loadRegistry(repoName), rec.path) and refuse 'changed' unless kind/state/branch/owner match the snapshot.

#### R041 · P3 · rot · `lib/daemon/handlers/worktree.ts:598`
**The `#adopt` synthetic lock excludes nothing but other adopts; adopt and freshen handlers also bypass the reconciler hold**

withTreeLock keys on the string; provision/dispose/freshen/reconcile lock on tree paths and never consult `${repoPath}#adopt`, so the comment's guarantee is false. adopt's own reconcileRepoRegistry can run concurrently with the reconciler's pass on the same repo (both scrapTree the same orphan, both prune) and with repos:locate (adopt is not under withReconcilerHeld), which is exactly the prune-during-relocate that the hold was added to prevent. Today the epoch check limits damage to a retried pass; the next writer added under the assumption in the comment will not be so lucky.

*Fix (small):* Run adopt and worktree:freshen inside withReconcilerHeld (they are reconciler-shaped passes), delete the `#adopt` key, and fix the comment.

#### R042 · P3 · rot · `lib/daemon/home-snapshot.ts:538`
**Push failure loop logs warn and rewrites the kv record on every attempt with no backoff or dedupe**

Two machines share one home repo (the design intent) and one is behind: `git push origin HEAD` is rejected non-fast-forward forever. Retry fires every pushDelaySec*5 (5 min), and every fs event in the home repo re-arms `schedulePush` at 60s via the `pushPending` branch, so with a busy home dir the daemon spawns git, writes a kv row and emits an identical warn roughly once a minute, indefinitely. Commit/add failures in the same module dedupe by message (lastLoggedCommitError, lastLoggedAddError); push does not. There is no exponential backoff cap and no "give up until a new commit" state.

*Fix (small):* Dedupe the warn by message like the commit path; back off the retry geometrically to a cap (e.g. 1h); skip persistPushRecord when the error text is unchanged.

#### R043 · P3 · gap · `lib/daemon/home-snapshot.ts:683`
**No git identity check: home-repo snapshots fail every cycle with a single deduped warn**

A fresh Mac with git from CLT but no user.name/user.email (the setup plan has no row for it) restores or clones a home repo fine, then every settings change triggers a debounced `git commit` that exits 128 'Author identity unknown'. The failure is logged once, retried every 20 s forever, and the only user-visible trace is `home.backup` reading 'N commit(s) not pushed' with no cause; `rt home init`'s initial commit (lib/home/init-exec.ts:89) fails the same way on the create path.

*Fix (small):* Add a `tool.git-identity` plan row (or have home.init pass `-c user.name=... -c user.email=...` from the team/user profile) and surface lastCommitError in the home.backup row detail.

#### R044 · P3 · rot · `lib/daemon/hooks-guard.ts:49`
**hooks-guard silently reverts any other tool's core.hooksPath within 100ms, forever**

A repo that once ran `rt hooks` keeps hooks.json + a shims dir under ~/.mattstack/rt/repos// indefinitely (the CLI doc says it never deletes them). The user later adopts husky v9 (`prepare` sets core.hooksPath=.husky/_) or lefthook, or deliberately runs `git config core.hooksPath` themselves. Within 100ms of the write (fs watcher) or 60s (poller) the daemon rewrites it back to rt's shims and logs one warn line the user never sees. Husky v9's runner in `.husky/_/h` is bypassed because the shim execs `.husky/` directly. The user's hook tool appears broken with no visible cause; the only tell is a daemon log line. This is also the one place the daemon writes into a target repo, so the stealth rule in CLAUDE.md is carrying an exception that is not surfaced to the user.

*Fix (small):* Only guard when the current value is unset or still points at an rt-owned path (an old shims location); if it points anywhere else, stop guarding that repo and emit a notification ("rt hooks disabled for : core.hooksPath now set by another tool"). Add `rt hooks off` cleanup that removes hooks.json so the guard has nothing to guard.

#### R045 · P3 · rot · `lib/daemon/hooks-guard.ts:84`
**Hooks-guard fs.watch has no error handler, is created unguarded at boot, and watchers are never retired**

fs.watch throws synchronously on EMFILE/ENOSPC; because startWatchingRepo is not guarded, one such throw at daemon.ts:475 propagates out of startDaemon() and the process dies before signal handlers are installed, and it repeats on every launchd restart while the fd pressure persists. Separately, the FSWatcher has no `error` listener, so an asynchronous watcher error (a repo directory removed or its volume unmounted while watched) surfaces as an uncaught exception. Watchers for repos that left the index or were relocated via repos:locate are never closed, so watchedConfigs and open kqueue descriptors only grow across the daemon's life.

*Fix (small):* Wrap watch() in try/catch with a warn, attach `watcher.on("error", ...)`, and have refreshWatchedRepos close watchers whose configPath is no longer in the index.

#### R046 · P3 · rot · `lib/daemon/hooks-guard.ts:100`
**Hooks-guard fs.watch handles are never closed when a repo leaves the index or moves**

`rt repos prune`, `rt repos locate` (which calls refreshWatchedRepos) or a manual delete removes/moves a repo. Its kqueue watcher on the old `.git` directory and the debounce closure capturing the old path stay alive for the daemon's lifetime; if the old directory is recreated (a re-clone at the same path), the stale closure runs `git config core.hooksPath` against it with the OLD repoName's shims dir. `watchedRepos` in `status` over-reports. Bounded by repo churn, so a leak in practice only over long uptimes.

*Fix (small):* In refreshWatchedRepos, diff watchedConfigs against the current index and close+delete watchers whose config path no longer maps to an indexed repo; call it from the 60s hooks scan as well.

#### R047 · P3 · gap · `lib/daemon/pollers.ts:32`
**No tests cover pollers.ts wiring or the refresh cycle's in-flight/timeout behavior**

The in-flight guards, the latch-on-hang behavior, and runCapture's timeout semantics are all unguarded by tests, so the P1 findings above (a wedged lsof latching the guards, a timeout reporting exitCode 0) regress silently on any refactor; there is also no test that a refresh cycle finishes when one repo's GitLab call never settles.

*Fix (small):* Add a subprocess.test.ts case asserting runCapture returns within timeoutMs+grace with a non-zero/timed-out result for a `trap "" TERM` child and a pipe-holding grandchild; add a pollers test with injected scanners asserting a never-settling scan does not block later ticks once a deadline is added.

#### R048 · P3 · rot · `lib/daemon/socket-server.ts:24`
**`@ts-expect-error` on the unix-socket idleTimeout with an unpinned `@types/bun: latest` will break `tsc --noEmit` in CI the day bun-types declares it**

A contributor on a fresh clone runs `bun install` (or the lockfile is bumped) and resolves a bun-types that adds idleTimeout to UnixServeOptions. The directive becomes TS2578 "Unused '@ts-expect-error' directive", tsc fails, and CI is red for a reason unrelated to any change. The comment itself asks for the suppression to be removed "once bun-types catches up", which is a manual step nobody will notice until the break.

*Fix (trivial):* Pin `@types/bun` to the exact version matching the runtime (1.3.13), or replace the directive with a typed cast (`{ ... } as Parameters<typeof Bun.serve>[0]`) that stays valid whether or not the property is declared.

#### R049 · P3 · rot · `lib/daemon/worktree-reconciler.ts:637`
**Reactor `fired` key list grows without bound**

Every merged or closed MR that had a tree adds a `disposed:::` key forever. After a year on an active team repo the KV blob is tens of thousands of strings, parsed and re-serialized on every 5-minute pass for every repo. No correctness failure, but a monotonic cost with no reaper.

*Fix (trivial):* Drop fired keys whose MR is no longer present in cacheEntries for that repo (the branch-cache GC already bounds that set), or keep fired keyed per repo and rebuild alongside mrState.

#### R050 · P3 · rot · `lib/notifier.ts:28`
**notifier.ts and two handlers import repoLabel through repo-arg.ts, pulling the CLI picker module into the daemon graph**

The static graph trace shows lib/daemon.ts -> notifier.ts -> repo-arg.ts -> repo.ts -> git.ts: the daemon bundles and evaluates the interactive repo/worktree picker module (process.exit paths, console.log, execSync git helpers, dynamic rt-render/ink references) for a one-line string formatter that lives in the leaf repo-label.ts. Any future module-scope side effect added to repo.ts (it already computes RT_ROOT from import.meta.url) runs inside the daemon; any daemon code that reaches for a repo.ts helper by accident gets a process.exit-on-error CLI function.

*Fix (trivial):* Import repoLabel from ./repo-label.ts at the three sites; add repo.ts, repo-arg.ts, rt-render.tsx, fzf.ts to a no-eager-import guard for lib/daemon/** (extend lib/__tests__/no-eager-tui.test.ts).

#### R051 · P3 · gap · `lib/setup/validators/mac.ts:29`
**Intel Macs are unsupported but the setup plan never says so**

The release is arm64-only and deps.lock is parsed only for arm64, but the only hardware/OS gate in the plan is the macOS 14 floor. An Intel Mac user downloads mattstack.app, macOS refuses to launch it ('You can't open the application because it is not supported on this Mac') or the tarball rt prints 'Bad CPU type in executable', and there is no plan row, verify row, or download-page check to explain it. Rosetta cannot run arm64 binaries on x86_64.

*Fix (trivial):* Add a `tool.arch` required row (`uname -m` == arm64) with an explanatory detail, and gate the download/marketplace listing on architecture.

#### R052 · P3 · rot · `lib/state/busy.ts:44`
**busy.ts writes CLI-process busy warnings into the daemon log surface via a second pino-roll writer**

`rt run` enrichment in a CLI process (enrich.ts:105 writeEnriched -> store.put -> persistOrWarn) hits SQLITE_BUSY. The CLI opens its own pino-roll stream on logs/daemon..log and appends a line carrying the CLI pid under the daemon surface; `rt daemon logs` attributes it to the daemon, and two processes now apply the 14-file retention independently against the same rolling set. The CLAUDE.md logging seams say CLI output belongs to installCliLogging's surface.

*Fix (small):* Inject the sink: have busy.ts accept a logger provider set by the daemon at boot (default: a `state` child of whichever surface the process runs, via cli-logging in the CLI), so the module never imports daemon-logger itself.

#### R053 · P3 · gap · `lib/state/db.ts:180`
**chat_messages has no retention path; room summaries scan the whole room per call**

N agents posting across weeks (the spec wants history 'scrollable a month back' but nothing bounds it). listRooms, which the viewer polls and `rt chat rooms` calls, runs four queries per membership including a MAX(posted_at) that walks every row of the room (no index on posted_at), and the mention count LIKE-scans all unread rows. Archived rooms keep every row forever. Growth is slow (~200B/message) but unbounded, and the WAL/checkpoint cost and the per-call scans grow with it.

*Fix (small):* Add a daily sweep (alongside the events sweep) that deletes messages older than a configurable age with a per-room floor, exposed as `rt chat prune`; add an index on (room, posted_at) or maintain last_posted_at on chat_rooms at post time.

#### R054 · P3 · gap · `lib/state/db.ts:258`
**agents table grows without bound; no finished-age prune and no index for the unfiltered list**

Every `rt agent start` inserts a row and nothing removes finished ones. With shepherdr fan-outs of dozens of agents a day the table reaches tens of thousands of rows in months; `rt agent list` without a repo filter sorts the whole table on every call and the handoff picker slows with it.

*Fix (trivial):* Prune rows with finished_at older than N days in the daily sweep; add an index on created_at (or finished_at) for the unfiltered list.

#### R055 · P3 · gap · `lib/state/db.ts:377`
**No backup, restore, or integrity check for state.db; quarantine-and-cold-start is the only recovery**

state.db now holds non-regenerable rows (chat_messages, run_history, agents, the worktree registry kv blob). A corrupt file is quarantined and everything cold-starts with chat history gone; there is no `rt state backup/restore`, no `VACUUM INTO` snapshot, and `PRAGMA quick_check` never runs, so partial corruption inside a table surfaces as handler errors long after the point where a copy could have been taken.

*Fix (medium):* Add `rt state backup` (VACUUM INTO a stamped copy) and `restore`; run `PRAGMA quick_check` at daemon boot and warn; take a daily rotating copy from the daemon sweep.

#### R056 · P3 · rot · `lib/state/db.ts:440`
**V*_SCHEMA concat is hand-maintained; a constant left out of it never applies and the stamp hides it**

A lane adds V9_SCHEMA and bumps SCHEMA_VERSION but does not append the constant to this expression. The migration stamps 9 with the new table absent, nothing errors at open, the first store query throws 'no such table', and because the stamp is already 9 no later open will retry. db.test.ts:107 replays only the constants that are already in the concat.

*Fix (trivial):* `const SCHEMAS = [V1_SCHEMA, V2_SCHEMA, ...]` executed by join, plus a test that greps this module's source for every `CREATE TABLE IF NOT EXISTS ` and asserts each name is present in sqlite_master after a fresh openStateDb.

#### R057 · P3 · gap · `lib/state/presence-store.ts:288`
**Presence and membership writes bypass both busy wrappers, so a 250ms BUSY surfaces as a raw error to the agent**

A CLI process holds the write lock for more than 250ms (a legacy import, `rt repos locate`'s transaction, a large enrichment batch) while an agent's hook runs chat:pulse or `rt chat sign-in` runs signIn. The daemon-flavor connection gives up and the handler throws 'database is locked' to the client; sign-in (a write whose loss is permanent by the spec's own classification) is not retried, and the pulse hook reports an error that is neither the 'reclaimed' notice nor a retryable signal.

*Fix (small):* Wrap signIn/signOut/pulseSession in runCriticalWrite and the cache-class writes (setAway, leaveRoom, markRead, prunePresence) in persistOrWarn, consistent with the policy table in busy.ts.

#### R058 · P3 · rot · `rt-tray/LaunchAgent.plist:40`
**Stale lifecycle rationale in the plist, boot-reconcile docblock and tray comments will mislead the next tuning change**

The ExitTimeOut=30 justification cites tunnels, child processes and a 900KB cache flush, all removed (shutdown.ts:39-41 says so itself); the log paths cite ~/.rt, which RT-46 retired; boot-reconcile claims a `start`-command orphan detection that commands/daemon.ts no longer has (start only POSTs to the tray); the tray comment asserts KeepAlive=true while render-launchagents.sh writes SuccessfulExit=false. Someone tightening ExitTimeOut, moving logs, or reasoning about restart behavior from these comments will draw the wrong conclusion, as the exit-0-on-signal behavior already shows.

*Fix (trivial):* Rewrite the four comments to state the current facts (cleanup is synchronous; KeepAlive is SuccessfulExit=false; logs under ~/.mattstack/rt/logs; eviction is the only orphan handling).


## Appendix C ... Claims investigated and refuted

Listed so a future audit does not re-raise them.

- `lib/daemon/api-server.ts:103` ... Port 9401 is a hard constant; EADDRINUSE at boot is an uncaught exception under launchd KeepAlive, giving a silent respawn loop
  - Refuted: The finding's central mechanism (EADDRINUSE at boot -> uncaughtException -> process.exit(1) -> silent 10s launchd respawn loop -> flapping rt.sock) does NOT occur on the prod path the finding explicitly targets ("users who are not the author"). Prod's launchd daemon is the compiled binary launched as `Contents/MacOS/rt --daemon` (LaunchAgent.plist:12-16), which enters via cli.ts:87-92 INSIDE async `__main()`, after `await import("./lib/daemon.ts")` at cli.ts:91. `startDaemon()` (daemon.ts:385, synchronous) throws when `startApiServer` calls `Bun.serve({port:9401})` (api-server.ts:103-104) with no try/catch (daemon.ts:469). Because that sync throw happens in the async continuation of an async function, it rejects `__main()`'s promise rather than propagating as a synchronous top-level exception; the call site `__main().catch((err)=>{throw err})` (cli.ts:58) re-throws inside a `.catch` callback, producing an UNHANDLED REJECTED PROMISE, which routes to the `unhandledRejection` handler installed at daemon.ts:392 (installCrashHandlers). That handler (daemon-logger.ts:242-244) logs at `error` and does NOT call process.exit. It never reaches the `uncaughtException` handler (daemon-logger.ts:237-240) that the finding relies on. With no exit, the socket server already bound at daemon.ts:468 keeps the event loop alive, so the process stays up as a degraded half-daemon (unix rt.sock serving CLI, REST/WS on 9401 absent). Therefore: no exit(1), no KeepAlive respawn loop, no rt.sock flapping. The finding's described failure is refuted for prod. The exact mechanism DOES hold for the DEV flavor only, whose shim execs `bun run /lib/daemon.ts --daemon` (Sources-daemon-shim/main.swift:269), making lib/daemon.ts the entry so `import.meta.main` is true (daemon.ts:517) and startDaemon() runs synchronously at top-level -> sync EADDRINUSE IS an uncaughtException -> exit(1) -> dev-plist respawn loop. But dev is the author's own machine, outside the finding's stated scope. The TRUE parts (hardcoded 9401 at daemon-config.ts:72, plist never passes RT_API_PORT so the env knob is dead under launchd, no port probe/fallback anywhere, Bun.serve throws EADDRINUSE synchronously) describe a real but different and milder defect: silent degradation, not a respawn loop.

- `lib/daemon/handlers/cache.ts:44` ... Unauthenticated GET /api/cache?maxAgeMs=0 forces a full multi-repo refresh from any web page
  - Refuted: Re-derived by the lead reviewer: GET /api/cache builds its payload from URL query params, so maxAgeMs arrives as the STRING "0"; the cache:read handler's numeric type check rejects it and no refresh is forced (the same coercion gap S085 documents). The route is therefore a stale-read, not a forced multi-repo refresh; the exposure that remains is the unauthenticated read itself, covered by S006/Phase 3.

- `lib/home/snapshot-owners.ts:69` ... Zone names containing pathspec magic characters break every auto-commit with add-failed
  - Refuted: Traced the full path: commands/home.ts:1071-1099 (`homeClaim`) takes a free-form CLI positional straight into `claimZone` -> `normalizeZone` (lib/home/snapshot-owners.ts:69-75), which only rejects empty/leading-"/"/backslash/"."/".." segments ... no check for pathspec magic or glob chars, so `rt home claim ':scratch'` is indeed accepted and stored as `:scratch/`. lib/daemon/home-snapshot.ts:657 does build `:(exclude)${zone}` by raw string concatenation for both `git add` (line 658) and `git commit` (line 683), sourced from `plan.excludedZones` = `Object.keys(owners.zones)` (lib/daemon/home-snapshot-plan.ts:121,126) with zero further sanitization. So the mechanical setup in the finding is accurate.

But the claimed *consequence* ... git raising "Invalid pathspec magic" (exit 128) on `:(exclude):scratch/`, causing "add-failed" and "every snapshot cycle fails... auto-commits silently stop for the whole home repo" ... does not reproduce. I ran the exact commands against the real git binary (Apple Git 2.50.1, same as this environment) in scratch repos:
- `git add -A -- . ':(exclude):scratch/'` -> exit 0, correctly excludes only that path.
- Also tried `:(exclude):(bogus)x/`, `:(exclude):/foo/`, `:(exclude)?x/`, `:(exclude))x/`, `:(exclude)(x/`, `:(exclude)x,y/` -> all exit 0, no pathspec-magic error.
Git's pathspec grammar consumes the `:(magic-list)` parenthetical exactly once; everything after the closing paren is taken as a single literal/glob pattern and is never re-scanned for a second leading `:` magic signature. A colon (or any of these other characters) inside the *pattern* portion is therefore inert, not an error trigger.

There IS a real, but different and narrower, defect adjacent to this: normalizeZone doesn't reject glob metacharacters (`*`,`?`,`[`), so they ARE interpreted by git as wildcards. For a FILE-kind zone with a literal glob-shaped name (no trailing slash, e.g. a real file named "*.log"), `:(exclude)*.log` over-matches (confirmed: excluded both "notes.log" and "logs/x.log" in a scratch repo ... matches the finding's "excludes far more" half). But for the more common DIR-kind zone (trailing slash always appended by normalizeZone's default `kind: "dir"`), the same glob shape becomes a complete NO-OP: `:(exclude)*.log/` matched zero files in testing, meaning the "claimed" zone loses its protection and gets silently auto-committed by the daemon ... the opposite failure direction from what's claimed, and with no error, no add-failed, no lastCommitError anywhere (exit 0 throughout).

- `lib/doppler-config.ts:54` ... Doppler config writer uses a fixed .tmp path and read-modify-write with no lock
  - Refuted: Traced every caller of writeDopplerConfig/loadDopplerConfig (lib/doppler-config.ts:49,61): the only call site anywhere in the repo is lib/daemon/doppler-sync.ts's reconcileForRepo (lines 15,54,68), and that function is called from exactly two places (grep confirmed no others): lib/daemon/cache-refresh.ts:212 (the daemon's periodic tick) and lib/worktree/create.ts:135's createTree. The finding's premise labels lib/worktree/create.ts "a CLI process," but createTree is never imported by any commands/*.ts ... its only importers are lib/daemon/handlers/worktree.ts:44 and lib/daemon/worktree-reconciler.ts:39, both daemon-internal. The CLI's `rt worktree create` sends an IPC request that the single already-running daemon process executes; there is no second OS process touching this file at all on that path.

Within that one process, reconcileForRepo (lib/daemon/doppler-sync.ts:30-71) is declared async but contains zero `await` ... confirmed by grep and by checking getSetting (packages/rt-client/src/settings/resolve.ts:501) is a plain sync function, not a Promise. So loadDopplerConfig() -> the addScopedEntry loop -> writeDopplerConfig() runs as one uninterrupted synchronous stretch (readFileSync, in-memory mutation, writeFileSync+renameSync) with no point where Bun's single-threaded event loop can switch to another logical caller. Two logical invocations (one from the cache-refresh timer, one from an IPC-triggered createTree) can only ever run one-after-the-other, each doing its own fresh load immediately before its own write ... so there is no lost-update window between two same-process callers, contrary to the finding's "one write is lost, or a rename races a half-written tmp" claim.

The finding's other premise ... dev and prod daemon flavors briefly alive together ... is specifically what the (already-implemented) flavor-exclusivity design guards against. lib/daemon.ts:103 awaits parkUntilIntended() before any subsystem (cron, cache-refresh interval, socket server at :468) arms. lib/daemon/park.ts's loop parks a wrong-flavor daemon indefinitely (never reaching cache-refresh) and additionally standoffs (:63-77) even a correctly-intended daemon until a live different-flavor socket holder drains ... so a new flavor cannot arm cache-refresh while the old flavor is still serving. This closes the specific cross-flavor race the finding names.

What remains is the narrower "user runs `doppler setup` between the daemon's read and write" TOCTOU. That class of bug is real in principle (no lock, full round-trip write) but the window is the synchronous span between the top-of-function readFileSync and the bottom-of-function renameSync for one repo's reconcile pass ... microseconds under normal load, not a window a human running a CLI command by hand can realistically land in. This is a much weaker/narrower exposure than "the daemon's 5-minute cycle... races" implies.

