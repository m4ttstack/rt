/**
 * The full-screen compound-PageShell showcase's canonical path. It renders
 * without the site chrome (it brings its own mini-rail app shell), and
 * '/demo' routes straight to the same screen -- nav entries link the short
 * path, while deep links to this one keep working.
 */
export const PAGE_SHELL_DEMO_PATH = '/demo/page-shell';

/** The top-level demo path, as linked by the header nav and rail. */
export const DEMO_PATH = '/demo';
