/**
 * The Tokyo brand colour names live in `@mattstack/mantine-tokyo`; this file
 * is the kit's designated re-export point, rather than editing `mantine.d.ts`
 * or `colors.ts` directly. See `AGENTS.md` section 4.
 */
export type { TokyoColorName as AppCustomColors } from '@mattstack/mantine-tokyo';
