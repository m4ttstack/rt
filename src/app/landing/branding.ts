// The template's working name, displayed in the landing header/footer. This
// file is listed in create-cli/create.ts's TOKEN_FILES, so scaffolding a new
// app replaces the token below with the new app's name automatically.
export const APP_NAME = 'chat';
