/**
 * The Tokyo brand colour names now live in `@mattstack/mantine-tokyo`; see
 * `AGENTS.md` section 4 ("Adding brand colors when re-theming") for why this
 * file is the designated re-export point rather than editing `mantine.d.ts`
 * or `colors.ts` directly.
 */
export type { TokyoColorName as AppCustomColors } from '@mattstack/mantine-tokyo';
