/**
 * The Tokyo brand theme now lives in `@mattstack/mantine-tokyo` (a second
 * app consumes the same package), so branding here is a re-export. See
 * `AGENTS.md` section 4 for the three-file theme split this slots into
 * (`base-theme.ts` / `app-theme.ts` / `theme.ts`), and the package's own
 * `theme.ts` for the actual values.
 */
export { tokyoTheme as appTheme } from '@mattstack/mantine-tokyo';
