---
title: rt tools
sidebar_label: tools
---

# rt tools

`rt › tools`

Install or run the setup verb for a tool from a setup plan row

## Usage

```bash
rt tools
```

## Subcommands

| Command | Description |
| --- | --- |
| [`install`](install) | Install a tool (brew, vendor curl|sh, apple-clt, or the bundled copy) |
| [`setup`](setup) | Run a tool's own post-install setup (herdr integration, fast-browser runtime, editor extension) |

{/* generated by scripts/gen-docs.ts; edit prose in _partials, not here */}