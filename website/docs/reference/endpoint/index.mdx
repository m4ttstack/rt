---
title: rt endpoint
sidebar_label: endpoint
---

# rt endpoint

`rt › endpoint`

Dev-endpoint claims (ports allocated for intercepted commands)

## Usage

```bash
rt endpoint
```

## Subcommands

| Command | Description |
| --- | --- |
| [`lookup`](lookup) | Does this worktree hold a dev-endpoint claim for a role? |
| [`release`](release) | Manually free a worktree's dev-endpoint claim(s) (escape hatch for a claim liveness can't clear) |

{/* generated by scripts/gen-docs.ts; edit prose in _partials, not here */}