---
title: rt endpoint release
sidebar_label: release
---

# rt endpoint release

`rt › endpoint › release`

Manually free a worktree's dev-endpoint claim(s) (escape hatch for a claim liveness can't clear)

## Usage

```bash
rt endpoint release <worktree> [flags]
```

## Arguments & flags

| Flag / Arg | Type | Default | Description |
| --- | --- | --- | --- |
| `<worktree>` | text |  | Worktree whose claim(s) to release |
| `--role` | text |  | Only release this role; omit to release every role for the worktree |
| [`--json`](/guides/common-flags) | boolean | `false` | Emit machine-readable JSON instead of a plain line |

_See code: [commands/endpoint.ts › endpointRelease](https://github.com/m4ttstack/rt/blob/main/commands/endpoint.ts)_

{/* generated by scripts/gen-docs.ts; edit prose in _partials, not here */}