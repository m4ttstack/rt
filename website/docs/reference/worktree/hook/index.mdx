---
title: rt worktree hook
sidebar_label: hook
---

# rt worktree hook

`rt › worktree › hook`

Claude Code worktree hook: install, remove, or inspect the WorktreeCreate/WorktreeRemove wiring

## Usage

```bash
rt worktree hook
```

## Subcommands

| Command | Description |
| --- | --- |
| [`install`](install) | Write the hook pair into ~/.claude/settings.json |
| [`uninstall`](uninstall) | Remove rt's hook entries from ~/.claude/settings.json |
| [`status`](status) | Report whether the hook is installed and healthy |

{/* generated by scripts/gen-docs.ts; edit prose in _partials, not here */}