---
title: rt worktree
sidebar_label: worktree
---

# rt worktree

`rt › worktree`

Worktree-wide operations

## Usage

```bash
rt worktree
```

## Subcommands

| Command | Description |
| --- | --- |
| [`each`](each) | Run a command in each worktree (--all | --parked, else pick) |

{/* generated by scripts/gen-docs.ts — edit prose in _partials, not here */}