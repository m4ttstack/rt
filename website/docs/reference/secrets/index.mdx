---
title: rt secrets
sidebar_label: secrets
---

# rt secrets

`rt › secrets`

sops-encrypted secrets under ~/.mattstack/user/secrets/

## Usage

```bash
rt secrets
```

## Subcommands

| Command | Description |
| --- | --- |
| [`set`](set) | Write a secret (creates the domain file, or one key within it) — value prompted, never a CLI arg |
| [`list`](list) | List a domain's secret names (never prints values) |
| [`rotate`](rotate) | Replace a secret's value, or (--team with no domain/key) re-encrypt every team domain file to its current recipients — value prompted, never a CLI arg |

{/* generated by scripts/gen-docs.ts; edit prose in _partials, not here */}