---
title: rt state
sidebar_label: state
---

# rt state

`rt › state`

rt's own state.db: backup, restore, and integrity

## Usage

```bash
rt state
```

## Subcommands

| Command | Description |
| --- | --- |
| [`backup`](backup) | Write a stamped state.db backup (VACUUM INTO) and prune backups past retention |
| [`restore`](restore) | Restore state.db from a stamped backup copy (refuses while the daemon is running unless --force) |

{/* generated by scripts/gen-docs.ts; edit prose in _partials, not here */}