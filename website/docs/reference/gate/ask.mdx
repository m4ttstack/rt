---
title: rt gate ask
sidebar_label: ask
---

# rt gate ask

`rt › gate › ask`

Open a gate with the daemon-side ceremony (subject resolution, presentation, nudge)

## Usage

```bash
rt gate ask [flags]
```

## Arguments & flags

| Flag / Arg | Type | Default | Description |
| --- | --- | --- | --- |
| `--questions` | text |  | JSON array of questions (&#123;value,label&#125; option objects preferred; bare strings accepted) |
| `--context` | text |  | Optional decision context; over 8KB it is omitted, never an error |
| `--kind` | text |  | Gate kind (default question) |
| `--subject` | text |  | Explicit subject; omitted, the daemon resolves this session's run, else its agent record's subject |
| [`--json`](/guides/common-flags) | boolean | `false` | Accepted for symmetry; output is always JSON |

_See code: [commands/gate.ts › gateAsk](https://github.com/m4ttstack/rt/blob/main/commands/gate.ts)_

import Notes from '@site/docs/reference/_partials/gate/ask.mdx';

## Examples & notes

<Notes />

{/* generated by scripts/gen-docs.ts; edit prose in _partials, not here */}