---
title: rt doppler
sidebar_label: doppler
---

# rt doppler

`rt › doppler`

Per-repo Doppler template + sync into ~/.doppler/.doppler.yaml

## Usage

```bash
rt doppler
```

## Subcommands

| Command | Description |
| --- | --- |
| [`init`](init) | Capture existing Doppler entries for this repo into a template |
| [`sync`](sync) | Apply the template across all worktrees (manual trigger) |
| [`status`](status) | Show template vs. actual config per worktree |
| [`edit`](edit) | Open the template in $EDITOR |

{/* generated by scripts/gen-docs.ts — edit prose in _partials, not here */}