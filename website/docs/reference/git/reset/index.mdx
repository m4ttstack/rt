---
title: rt git reset
sidebar_label: reset
---

# rt git reset

`rt › git › reset`

Safe reset with divergence detection

## Usage

```bash
rt git reset
```

## Subcommands

| Command | Description |
| --- | --- |
| [`origin`](origin) | Sync with origin/current-branch (after remote rebase) |
| [`soft`](soft) | Unstage everything, keeping your edits (to undo a commit, use rt git undo) |
| [`hard`](hard) | Discard every working-tree change (to undo a commit, use rt git undo) |

{/* generated by scripts/gen-docs.ts; edit prose in _partials, not here */}