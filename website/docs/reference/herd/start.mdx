---
title: rt herd start
sidebar_label: start
---

# rt herd start

`rt › herd › start`

Start a herd: registry row, chat room, herdr workspace, gate subscription

## Usage

```bash
rt herd start [flags]
```

## Arguments & flags

| Flag / Arg | Type | Default | Description |
| --- | --- | --- | --- |
| `--name` | text |  | Herd name; seeds the room and workspace labels |
| [`--repo`](/guides/common-flags) | text |  | Repo the herd works (default: the current repo) |
| `--hidden` | boolean | `false` | Run the workspace in a hidden herdr session |
| `--session` | text |  | Shepherd session id (default: CLAUDE_CODE_SESSION_ID) |
| [`--json`](/guides/common-flags) | boolean | `false` | Emit the herd record as JSON |

_See code: [commands/herd.ts › start](https://github.com/m4ttstack/rt/blob/main/commands/herd.ts)_

{/* generated by scripts/gen-docs.ts; edit prose in _partials, not here */}