`rt glitter` opens a GitHub Desktop style git client in the terminal you run it from: changes with per-line checkboxes, diff, commit, history, stash, and the repo, worktree, and branch foldouts. It needs an interactive terminal and a repo rt tracks.

The [Glitter guide](/guides/glitter) walks through the board, the staging model, and every key.
