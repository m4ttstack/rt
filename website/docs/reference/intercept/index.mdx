---
title: rt intercept
sidebar_label: intercept
---

# rt intercept

`rt › intercept`

Generic dev-command interception (PATH shims, port claiming)

## Usage

```bash
rt intercept
```

## Subcommands

| Command | Description |
| --- | --- |
| [`status`](status) | Shim + rule health for command interception |
| [`install`](install) | (Re)write PATH shims for every registered intercept command |
| [`uninstall`](uninstall) | Remove every generated intercept shim |

{/* generated by scripts/gen-docs.ts; edit prose in _partials, not here */}