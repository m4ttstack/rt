---
title: rt mr
sidebar_label: mr
---

# rt mr

`rt › mr`

Merge request operations (GitLab) — `pr` works too

## Usage

```bash
rt mr
```

**Aliases:** `pr`

## Subcommands

| Command | Description |
| --- | --- |
| [`open`](open) | Open a bare MR on the current branch via glab |
| [`describe`](describe) | Draft an MR description with an agent (streams to stdout) |
| [`ship`](ship) | All-in-one: push + describe + open (the daily driver) |

{/* generated by scripts/gen-docs.ts; edit prose in _partials, not here */}