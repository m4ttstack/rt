node_modules
dist
storybook-static
docs
bun.lock
packages/ui/scripts/treeshake-probe/dist
packages/tui-kit
# Design canvases are regenerated from a shared CSS block per app
# (see each app's design/README.md provenance table); never reformatted.
apps/*/design
# board's mr-board wrapper skills: src/__tests__/skills-resolve.test.ts
# byte-compares their YAML frontmatter (double-quoted `slots:`); prettier's
# singleQuote rewrite breaks that assertion.
apps/board/skills/*/SKILL.md
# deck's core/generated-fresh.test.ts byte-compares these against
# buildBoardArtifacts()/buildGatewayCss() output; prettier reformatting
# would desync the committed twin from what `bun run build:board` emits.
apps/deck/core/generated
# deck's src/registry/bundle-catalog.test.ts pins this fixture's sha256, and
# repo-tools' byte-identical twin pins the same digest; reformatting it would
# break the parity both tests exist to hold.
apps/deck/src/registry/__fixtures__/deps-lock-serve.fixture.json
# generated from @radix-ui/colors by packages/tokens/scripts/generate-radix.ts; radix-fresh.test.ts byte-compares it
packages/tokens/src/radix.ts
# generated by packages/tokens/scripts/generate-ramps.ts; the freshness gate byte-compares it
packages/tokyo/src/ramps.ts
# Runtime state dirs (gitignored as directories; a symlinked state/ is not matched by that pattern)
apps/*/state
apps/*/state.*
