<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleIconFile</key>
    <string>AppIcon</string>
    <key>CFBundleExecutable</key>
    <string>@@APP_NAME@@</string>
    <key>CFBundleIdentifier</key>
    <string>@@BUNDLE_ID@@</string>
    <key>CFBundleName</key>
    <string>@@APP_NAME@@</string>
    <key>CFBundleDisplayName</key>
    <string>@@DISPLAY_NAME@@</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0.0</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>LSMinimumSystemVersion</key>
    <string>13.0</string>
    <key>LSUIElement</key>
    <true/>
    <key>NSUserNotificationAlertStyle</key>
    <string>alert</string>
    <!-- MSDaemonLabel: the launchd Label DaemonLifecycle.swift kickstarts and
         the plist basename it loads ("\(label).plist"). Templated by build.sh
         next to CFBundleIdentifier -- never hardcode this, it diverges by
         flavor (com.rt.daemon prod / com.rt.daemon.dev dev). -->
    <key>MSDaemonLabel</key>
    <string>@@DAEMON_LABEL@@</string>
    <!-- MSDevBuild is added by build.sh via PlistBuddy (a real bool, not a
         sed token) so it can't be stringified by accident. Gates
         UpdateChecker and the dev menu-bar mark. -->
</dict>
</plist>
